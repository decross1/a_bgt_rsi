# Prospective Mia 384-versus-1536 diversity-cap diagnostic

Status: plan and supervised window frozen before requests; no cap-study model request has been issued at this witness. This is an optional reused-development-fixture study, never a rescore of the admitted primary panel.

Evidence to bind before freezing: the five `diverse_select` task IDs in `experiments/diversity_selection_v1_2026-09-14.json` (raw SHA-256 `0cf6b1e426f1572df3f2b7433e48f776c87137258041ac5c3c4e09150916cc02`); primary v2 plan SHA-256 `9616a6f5b982585570fb1bcd6de76aa754fcb722225f90ecded3d737838ec0b8`; admitted Flash primary run SHA-256 `68193bdb172405ea07022b5e8f25de19a4a384825717dc6dc597f0d06eb28667`; same qualified reduced-47K/V2/MTP3 Mia parent and spec as the admitted primary window. The primary Flash run's five diverse-select cells scored 0/5; all 15 generators and five selectors returned, and 11/15 generators were length-stopped with 384/384 completion tokens in reasoning and no visible answer. The historical generators had a 20-second timeout, so they are descriptive references only.

Freeze five task pairs, two conditions per task, with each task's order alternating between cap384 and cap1536 before requests. Each condition retains the exact three generator messages, `explore_medium` resolved sampling and thinking policy, seeds 101/211/307, and original source/grader bytes. Change only generator `max_tokens` (384 versus 1536) while giving both conditions the **same 60-second generator timeout**. Retain the selector's dynamic prompt builder, `deterministic_off` policy, seed401, cap128, and 20-second timeout. The selector prompt is derived from that condition's own three returned/unknown proposals; freeze the builder SHA and replay the rendered message hash after the generator raw responses are available. Do not mutate the old diversity manifest: its control/diverse equal-budget invariant would be broken. A separate immutable diagnostic plan owns these overlays.

Declared denominator: five underlying task pairs, ten condition-cells, 30 generator calls and ten selector calls, 40 scheduled calls total. Record every issued timeout and partial response; record unissued slots as not_run after cancellation/cutoff. A complete paired result requires both conditions and all 40 call receipts plus independent raw-SSE, message/request, selector-prompt and unchanged-grader replay. Report per cap 15 generator finish reasons, reported reasoning/completion tokens, visible-final and valid-proposal counts; five selector outcomes and creditable protocol/task passes; paired task-level differences. Neither same seed nor alternating order proves deterministic bytes. No held-out, population, causal model-quality, or portfolio-gate rescue claim.

Hard request ceiling: 30×60 + 10×20 = **2,000 seconds**. Give the evaluator 2,200 seconds for bounded grading and persistence. With the reviewed Flash controller's required 1,500-second setup allowance, about 200 seconds of controller overhead slack, and 600-second exact-restoration reserve, freeze a worker/supervisor wall ceiling of **4,500 seconds (75 minutes)**. The supervisor can then spend an additional 600 seconds on exact emergency restoration if the worker did not prove restoration, for an operational worst bound of roughly **5,100 seconds (85 minutes)** plus bounded process-stop overhead. This emergency path is a safety limit, not a successful complete result. If setup overruns, the worker receives less than 2,200 seconds and unissued cells remain explicit; no partial run is called a complete five-pair result. Live monitor and current preferred 20 GiB host-available floor remain code-owned, and no additional GPU workload shares the window.

Minimum implementation: in a new isolated worktree, add one literal `diversity_cap` kind to a source-frozen copy of `lab_window.py`, plus a dedicated plan/runner/replay module patterned on the reviewed context/fresh evaluators. Reuse existing resource lease, MemoryMonitor, Nara quiescence, candidate canary/qualification, exact restoration, `adapters._execute_diversity`, `harness._execute_outcome`, private transport/evidence, and original diversity grader. The new controller source tuple and plan must be frozen before the first request. Do not edit already issued primary/context/fresh windows or their evaluator sources.

Interpretation boundary: within this new equal-60-second pair, cap is the intended difference. Relative to the admitted primary cap384/20-second cells, **both timeout policy and cap budget have changed**; any historical comparison is a later-budget intervention, not a cap-only rescue. A cap1536 response that still times out at 60 seconds is unknown for final-content benefit. The test does not answer native 64K capacity or overturn Flash's primary portfolio API-contract failure.

Timing provenance: elapsed and per-call durations are the producer's recorded monotonic-clock diagnostics. Raw-response replay independently checks content, token usage, finish reasons and grades; it does not independently replay a physical clock. A final source review identified that arbitrary resealed duration values lack an upper-bound admission check. The source and prepared window remain unchanged, and timing is not a condition of the cap-quality verdict. The no-overwrite review receipt is `mia-diversity-cap-v1-source-review.json`, SHA-256 `c0959dbbd62fbcce7b6b244f66324324412e9e0671881e5794cdae5b2c2359d8`.

Eight-hour deadline is 2026-09-16 00:09:45 UTC. At 2026-09-15 21:30 UTC, about 160 minutes remain. The 75-minute normal wall can fit alongside one 45–50-minute Mia game pilot if the new source is frozen/reviewed during that pilot. To leave about 13 minutes for admission/reporting even after a full 10-minute emergency restoration, this diagnostic must launch by roughly **22:31 UTC**. A launch near 22:45 UTC could finish a normal run before the deadline but would not leave enough room for the registered emergency path and review. A later launch, implementation drift, or inability to secure an exact 600-second restoration reserve should defer the diagnostic rather than reduce timeout or omit declared calls after looking at results.

## Frozen execution witness

- Isolated source branch: `codex/lab-diversity-cap-20260915`, HEAD `e77a892`.
- Plan raw SHA-256: `f1250b9c1965f9be99790153f63263c4240ed88aca44fc535abb7d8130c90f45`.
- Window raw SHA-256: `765768788240c6f25ded459c98534aa771b06cb9b2408c33d3c0c43ea8e44287`.
- Evaluator SHA-256: `b4144701927e6b1e012dddd5965de87e58b235b4d2e9b196e90c55404d395f98`.
- Isolated controller SHA-256: `f8625250b6efbf6eb188615ef653ba057b7dd2e4ae9e8f71b6dc782ccc92fff0`.

The controller extension is kept in the isolated branch and archived as [a patch](../notes/research/2026-09-15-lab-eight-hour/diversity_cap_controller.patch). The frozen original controller stays unchanged. Run all study commands from the isolated checkout with `/home/decross1/projects/a_bgt_rsi/.venv-chroma/bin/python`; its exact path is part of terminal admission.
