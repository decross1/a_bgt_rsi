# Manual qualification of corrected weekly review inputs

Prepared 2026-09-14 after the first weekly review returned `REVISION_REQUIRED`,
and before calls using the corrected snapshot.

The owner requested completion of the implementation and evaluations, using
subscription-only frontier sessions and at most 120 Spark GPU-minutes per
week. The first scheduled-style cycle used its two allowed review attempts.
Its report and canonical week claim remain final and are not reset.

This is a separate, bounded implementation-qualification pass: at most one
Codex proposal and one Claude adversary call, each at most 300 seconds and
600 seconds combined, with no retries or paid fallback. These additional
manual validation calls must be reported separately from the ordinary weekly
two-attempt limit. The independent topic annotation calls are separate again.

The concrete defect being retested is missing review evidence. The snapshot
now includes actual public arm settings, ordering, resource limits, relevant
preregistration text and explicit semantic-grading requirements. The target
is the registered public game/science portfolio, whose single inference delta
is Qwen `xhigh` versus `medium` effort. It has deterministic local graders and
trusted parent-side observations for executed code; it does not need a
frontier judge. Public visible tasks, one seed and their claim limits must be
acknowledged. No result establishes production superiority.

Before calls, freeze the corrected code commit, exact target manifest and
current-week source-packet hashes in an external admission receipt. Use a new
qualification directory beneath the existing weekly artifact root. Do not
overwrite, retry, or reinterpret either response from the first weekly cycle.

Only a new valid `CONTINUE_TRIAL` card may enter the registered dispatcher.
It must match all eight tasks, both arms, seed 0, the manifest hash and a
2,310-second reservation with a 2,280-second payload. Admission also requires
the canonical remaining weekly budget, idle serving and the existing resource
locks. Any rejected or invalid response remains terminal. The owner has not
authorized production promotion, service changes or recurring activation.

Success here means the corrected evidence supports a valid reviewed
development experiment and its bounded execution produces reproducible
receipts. It does not require a favorable model result. Failure is preserved
and reported; a new scientific claim cannot be manufactured to pass the test.
