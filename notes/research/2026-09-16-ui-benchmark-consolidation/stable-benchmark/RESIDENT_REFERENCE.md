# Stable canary 1.0.0 resident commissioning run

**Execution status:** complete; replay, resource, source and restoration admission verified. **Measurement status:** commissioning only; four task-contract defects confirmed. Admission establishes the execution evidence chain; it does not independently validate every benchmark item.

The declared role bundle used Gemma `precise` for capability tasks and actor steps, and Qwen `critic_current` (`xhigh`) for critic steps. The public canary is small and uses one seeded run. It does not establish broad scientific performance, full repository-repair ability or an end-to-end research outcome.

| Construct reported by the original frozen grader | Recorded credited units |
| --- | ---: |
| Scientific reasoning/evidence | 2/4 |
| Functional code repairs | 2/4 |
| Tool selection/execution plus final response contract | 1/4 |
| Public goods | 2/2 |
| Vickrey auction | 1/2 |
| Cournot | 1/1 |
| Proper-scoring reporting | 1/1 |
| Actor–tool–critic micro-workflows | 1/3 |

These rows preserve the original receipt; they are not a pooled quality score. Two evidence tasks had citation-contract defects and two code responses encountered undisclosed sandbox restrictions. A source-controlled measurement notice binds this definition and withholds comparative quality claims. The correction is prospective release 1.1.0; no candidate improvement or production promotion is supported by this run.

The run issued 27 calls, 5,312 input tokens and 3,918 output tokens in 139.951927 evaluator seconds. The whole lifecycle took 142.286823 seconds. Minimum monitored MemAvailable was 35.111946 GiB across 173 samples, with no guard breach. Nara was restored; the exact resident containers were unchanged.

Flash remained explicitly unissued: zero calls and no quality score. There is no matched pair for this cohort.

## Confirmed task-contract defects and observed operational failures

- Three native-tool tasks made the expected calls and then returned visible Gemma channel markers before their final JSON. Their original strict response failures remain unchanged.
- One Qwen critic exhausted its 1,536-token cap at `xhigh`, with finish reason `length`, 6,954 reasoning characters and an empty visible answer.
- One actor returned two JSON objects instead of the single requested intermediate artifact.
- Two code candidates hit sandbox syntax/call restrictions absent from the model-visible contract. The `.sort()` and `lambda` rejections cannot establish functional coding mistakes. Release 1.1 discloses the existing restrictions without weakening the sandbox.
- The support oracle omitted DOC-B even though its 30-day qualifier is needed for the full claim. The abstention oracle required an unnecessary second source, while the prompt did not specify bare citation IDs. Release 1.1 uses the full sufficient ABC set and minimal E set with explicit citation syntax.
- One Vickrey action was valid but had two units of exact own-utility regret.

No response has been repaired, rescored or relabeled in the frozen run. Any corrected task contract must be prospective and separately versioned.

## Provenance

- Comparison: `stable-benchmark-20260916-a`.
- Definition SHA-256: `75de9dc0dc324a4559332f88ae6e5ae861ba0d6f683334d85395d082bbaf04df`.
- Resident run SHA-256: `2efa93513ba3ea1eabd416f80206dad634b4b9267b09149e43c8c0a9556bc5e1`.
- [Exact post-run verification receipt](resident-reference-verification.public.json), SHA-256 `88b3c87086097b83d3e0761d15cf812284c658447ee8b87fbba6ba3080250a6b`.
- Raw responses and replay artifacts remain in the registered local artifact tree. The root integrator independently reran the pure comparison verifier after restoration and confirmed both arm statuses.
