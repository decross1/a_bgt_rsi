# Four-hour checkpoint — 07:41 UTC

The authorized window remains active until **11:41:19 UTC**.

PR [57](https://github.com/decross1/a_bgt_rsi/pull/57) is merged and deployed as
canonical commit `798ed4a`. The UI backend was refreshed on port 8700. The exact
model processes, Nara, frontend, and sampler remained unchanged; all eight
pre-existing tracked dirty files retained their bytes.

The deployed dossier has one compact overview and descriptive iteration titles.
The workspace separates current campaign work from archived diagnostics. Payoff
study B finished at 03:35 UTC; its 08:00 eligibility expiry is no longer presented
as continuing execution. The benchmark shows its complete 21/21 resident
reference alongside the unissued candidate. A 07:00 rejected coordinator plan
now remains visible, with zero dispatched actions and a trace link. Its three
rejected planner attempts used a topic identifier rather than an exact
preregistered seed. This UI change does not relax the campaign contract or
create a new research result.

Validation: 1,617 frontend tests, the scoped backend/producer checks,
TypeScript, production build, and source reachability passed. Fourteen built
browser routes passed without page errors, failed chunks, or horizontal
overflow; a subsequent deployed pass verified Now, dossier, and benchmark
views. Pages load on demand. The main JavaScript entry is approximately 310 KB,
down from 1.32 MB; route/shared chunks load separately. This is an entry-size
comparison, not a claim about total cold-page transfer.

Corrected frozen canary 1.1.0 has one admitted resident reference: 21 units,
28 calls, 151.1 seconds of evaluation, and a 154.3-second supervised lifecycle.
Capability outcomes are 11/18 and system micro-workflows 2/3, reported separately.
The three native-tool failures have correct calls/values but visible Gemma
channel framing; two code tasks violate disclosed Lambda restrictions; one
Qwen critic exhausts its fixed cap; two strategic actions incur exact regret.
These are different failure mechanisms, not one model-quality score.

Flash closure is final for this work window. Its last startup hit the registered
host-pageout guard before inference; it has no score on canary 1.1. Historical
admitted results remain available without rescoring. Retain Gemma 32K and Qwen
16K. Sunday 05:30 UTC is the review-only cadence; another startup requires a
material new hypothesis and prospective qualification.

The next bounded test changes only Gemma's reasoning-parser flag, using two
fresh matched containers and the unchanged 1.1 task/grade contracts, followed by
restoration of the exact original container. It remains **unregistered and
unissued**. Independent source review has caught registration/presence-recording
defects, a readiness-URL allowlist mismatch, missing continuous Nara observation,
and recovery paths requiring correction. No production model setting has changed.
The controller, UI observation, source freeze, and public preregistration must
pass before launch. If that cannot fit the remaining window with its full
two-hour bound and restoration reserve, the proposal stays unissued.

Local receipts are under
`a_bgt_rsi_v2_artifacts/2026-09-16/ui-benchmark-eight-hour/`:
`progress-clarity-deployment-receipt.json`,
`ui-audit/post-progress-clarity/`, and
`ui-audit/post-progress-clarity-deploy/`.
