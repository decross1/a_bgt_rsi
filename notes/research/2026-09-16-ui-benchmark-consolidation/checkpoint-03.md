# Three-hour checkpoint — 06:41 UTC

The eight-hour window remains active until 11:41:19 UTC.

PR [56](https://github.com/decross1/a_bgt_rsi/pull/56) is merged and deployed as canonical commit `20cace8`. The existing frontend remains on port 5173; backend port 8700 was refreshed without restarting the local models. All eight pre-existing tracked dirty files retain their prior bytes.

The dossier has one compact exact-record overview, descriptive iteration titles, and campaign scope preserved through navigation. Benchmark progress now selects corrected release 1.1.0, with release 1.0.0 accessible as commissioning evidence. Required review notices are hash-bound. Missing release/catalog evidence, reference/candidate confusion, and stale runtime warnings received adversarial regression coverage.

The first resident run exposed four task-contract defects: two citation oracles and two responses rejected by undisclosed sandbox restrictions. Original outcomes remain unchanged. The corrected definition was published and preregistered before inference in `c840466`, after causal source `979f961`, using the same task lineages, role policies, seed, caps, and October 14 review boundary. The corrected resident window entered evaluation at 06:42:15 UTC; terminal results are not claimed at this checkpoint. Flash is explicitly unissued and no further Flash startup is permitted in this window.

Validation before deployment: 3,613 core tests and 41 subtests, 1,245 backend tests, and 1,608 frontend tests passed; five core tests skipped. Targeted checks cover subsequent small fixes. Typecheck, build, source inventory, shell syntax, and browser checks passed. The existing bundle-size warning remains.

The Sunday 05:30 UTC hook now projects the current benchmark and recognizes the already completed W38 provider review without repeating calls. It does not automatically execute or promote a trial.

The next UI pass addresses two newly verified clarity gaps. Payoff representation B completed at 03:35 UTC; 08:00 is its eligibility expiry, not evidence that it is still running. Its admitted instrument diagnostic has no binding to the current campaign and cannot be shown as thesis support. The latest coordinator promotion check advanced zero findings, so the UI should distinguish a completed action from research progress. Those changes will use exact receipts and preserve the current campaign and study records.
