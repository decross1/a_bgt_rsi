# Dossier and benchmark follow-up review

Recorded September 16, 2026, before the corrected resident reference execution.

The dossier now keeps an explicit current-campaign context through list, detail, and scope-switch navigation. Historical bookmarks remain readable. The top has one exact-record overview with a descriptive title, question, observed outcome, recorded critique/learning, and next step; campaign context is compact and distinct. A record that is absent from the current human-action queue does not acquire verdict controls from its ID.

The benchmark page leads with release status and per-construct reference results. Definition details and history are expandable. Release 1.0.0 has a source-bound commissioning notice; its original outputs and verified receipts remain available separately from corrected 1.1.0. Missing catalog or required review evidence cannot silently restore current-release or quality claims. An admitted candidate is never labeled as the reference.

Global runtime reporting inspects every registered release for unfinished windows. A release-pointer change cannot hide an older unfinished window, and an unresolved benchmark warning remains visible through failed or stale refreshes until verified successor evidence replaces it.

The existing Sunday 05:30 UTC hook adds a bounded, non-evidentiary benchmark snapshot. A verified terminal same-week provider review is reused without another provider call or report rewrite. The September 14 W38 review is terminal; September 20 remains W38. An incomplete historical context still requires explicit recovery under its original plan. The hook does not automatically execute or promote a model trial.

Independent reviews found and resolved missing-catalog fallback, missing-required-review claims, candidate/reference mislabeling, runtime-warning identity loss, stale-warning disappearance, and ambiguity in two prospective benchmark response contracts. The current campaign and archived record views were checked in desktop, narrow, dark, and keyboard browser states. The final dossier screenshot has exactly one overview and one instance of each overview section.

Validation: full core suite 3,613 passed, five skipped, 41 subtests; full backend suite 1,245 passed; full frontend suite 1,608 passed. Final targeted tests covered the revised benchmark contracts, historical hashes, multi-release runtime selection, weekly history bounds, and source-scoped detail transitions. Frontend typecheck/build, shell syntax, component inventory, and diff checks passed. The existing large frontend bundle warning remains.

Screenshots and browser receipts: `a_bgt_rsi_v2_artifacts/2026-09-16/ui-benchmark-eight-hour/ui-audit/post-followup/`. The corrected definition and both arm manifests were published before model execution in commit `c840466`, following frozen causal source `979f961`. Benchmark 1.1 results are not yet claimed in this review.
