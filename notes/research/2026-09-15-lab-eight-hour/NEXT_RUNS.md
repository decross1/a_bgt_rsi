# Next measured improvements after the eight-hour window

Production remains Gemma 32K + Qwen 16K with existing per-caller policies. Flash is a viable challenger; the current evidence is insufficient for promotion under the registered rule. None of the following proposals changes that decision or claims a trading result.

| Priority | Measured reason | Smallest next experiment | What would count as progress |
| --- | --- | --- | --- |
| 1. Native arithmetic assistance | Both models failed every combined arithmetic check in the matched game pilot. The six-pair calculator instrument was implemented but closed unissued. | Use a **new** window for the frozen direct/native-`shared_return` instrument. Follow its [operations guide](../../../experiments/payoff_tool_arithmetic/OPERATIONS.md); never reuse the closed ID. | Actual parsed tool calls, exact emitted arguments, separate strict-format and arithmetic grades, and an admitted six-per-arm comparison. A null or parser failure is actionable evidence, not a missing denominator. |
| 2. Executable coding repair | Fresh repairs were strict 0/6 for both models, but the predefined framing diagnostic passed 6/6 for each. | Give a narrowly versioned patch-framing adapter a new contract and fresh repair tasks; preserve strict/raw results separately. | Hidden tests and regressions pass under the new preregistered contract. Do not retroactively rescore the older strict panel. |
| 3. Usable reasoning budget | Eleven of fifteen Flash diversity generators exhausted 384 tokens in reasoning with no final answer. The paired cap follow-up stopped before any evaluator call. | Register a new supervised window for the same 384/1536 paired design, with its fixed time bounds and a recorded host paging baseline. | Compare completed final answers, exact task success, timeouts, tokens and correct-task time. Preserve the paging gate; distinguish host-wide paging from candidate cgroup memory. |
| 4. Strategic behavior after arithmetic | Mia won 4/6 joint-objective zero-regret episodes versus Gemma's 0/6, but Gemma won 3/6 own-objective episodes versus Mia's 0/6. | Only after the arithmetic result, cross model and calculator support on fresh disclosed finite games, stratifying own/joint objective, focal seat and opponent rule. | Compare action validity and regret to an analytical/scripted best response. Keep arithmetic help separate from optimal-action leakage and from speculation about internal attention. |
| 5. Context allocation | Current Gemma and Qwen capacities passed their supported controlled bands; Qwen 32K and a quality-tested 64K lane remain unmeasured. | A separate memory-admitted Qwen 32K lane; then selected 64K capability tasks where retrieval plus shorter context has a matched comparator. | Better task outcomes or non-inferior quality with acceptable total time and memory. Capacity alone is not a gain. |

Do not run all five at once. Register one causal question, preserve the strongest valid incumbent comparator, and use the smallest panel that can decide whether a larger experiment is justified. Model, runtime, policy, context and tool changes remain separate surfaces.

The ordinary research pipeline consumed its three successor topics. Iterations 007–009 produced debate and journal records with pending science gates; none produced a new source-bound experiment outcome. The next queue should be replenished from **admitted empirical results** and explicit unresolved questions, not by regenerating renamed versions of the same hypotheses. In particular, the 009 red-team `fatal_flaw` correctly rejects an unobserved internal-attention mechanism as presently untestable. A scripted finite-game behavioral contrast is the measurable replacement.

For the owner's one-month market-research objective, the sequence should be evidence-dependent:

1. Establish arithmetic/tool reliability and reproduce at least one objective-stratified action result against a scripted control.
2. Specify a finite market-game mechanism—such as inventory and adverse-selection incentives—with disclosed assumptions, executable baselines and falsifiable behavior. Passing a public-goods task does not establish that market mechanism.
3. Use the separate H1 paper-data lane to test a predeclared observable consequence against matched exposure/momentum controls and explicit cost assumptions. Four-minute captures cannot validate executable quote fills or sub-minute market-making behavior.
4. Expand prospective paper evaluation only after complete timestamped source coverage and an interpretable result. The September 16 forward interval and the frozen payoff-B study are future measurements at this handoff, not completed evidence.

The next review should produce a concrete disposition for each attempted experiment: admitted improvement, admitted null/regression, runtime abort, or unissued due to availability. Keep those states visible on the Benchmarks page. No promotion is implied by a finished engineering task, a novel-sounding hypothesis, or a faster token stream.
