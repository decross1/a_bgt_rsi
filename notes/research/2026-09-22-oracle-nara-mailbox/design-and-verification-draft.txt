ARCH: VERIFIED LIVE STATE (read-only, 2026-09-22 07:53Z): a 262K context eval is running right now (bench.flash_context_eval.driver --tiers 262144 --take-resident-offline, guard --mem-available-floor-gib 8, started 07:53:27Z). flash-resident.service has been inactive since 07:53:25Z, and nara-daemon.service since 07:40:09Z. The 64K tier failed with "candidate_exit_restart_oom_or_identity_change", but its monitor's minimum MemAvailable was 116 GiB, so that failure was NOT memory pressure (a_bgt_rsi_v2_artifacts/2026-09-22/context-eval/report.json). No ui-session worktree is live (git worktree list). The owner's first goal, context bring-up, is therefore Phase 0 and gates builder and prompt sizing.

DESIGN: one lab-side, two-way, append-only, hash-chained event log that both agents write through fixed-actor transports. Nara executes through a work lane that sits OUTSIDE the research coordinator and reuses the D-062 packet machinery. The page is a deterministic projection of the log, never model prose.

(1) STORE. A new module orchestrator/oracle_mailbox.py (store + fold + CLI: post, receipt, approve, list, show) writes run_state/oracle_nara_mailbox.jsonl under fcntl lock run_state/.oracle_nara_mailbox.lock. It lives in a separate module because the lab_channel fence test pins its CLI to exactly timeline|turn|delegate (tests/test_lab_channel.py:379-382). The run_state/ location keeps the log inside premerge's protected prefix (tools/premerge_check.sh:62), so no packet builder can edit it.

(2) WHY NOT REUSE THE EXISTING CHANNELS.
- The Pi mailbox is hard-bound to session 01a0bbce (oracle-mailbox.ts:25). Its source is pinned to codex-oversight and its authority to advisory_only (:316-317). It stays the owner/Codex->Pi advisory lane.
- lab_channel is conversation-only. Its delegate() hard-codes human authority: 'source':'human', 'authorized_by':'human:lab_channel', and a human mirror row (orchestrator/lab_channel.py:424,432,448).
- authorize_fix_queue has no author, no receipts, no consumed marker, and an empty test_cmd skeleton (packet_dispatcher.py:356-402).

(3) WRITERS, one actor per transport.
- oracle:hq is the Codex HQ CLI `post --as oracle:hq`.
- oracle:pi is the i

SCHEMA: FILE: run_state/oracle_nara_mailbox.jsonl, append-only and gitignored. It is included in the daily backup snapshot (lesson from Oracle's D1 ledger-durability gap). Schema file: schema/oracle_nara_mailbox.schema.json (new; not a spine file).

COMMON ENVELOPE (every row): {schema_version:'oracle-nara-mailbox/v1', seq:int monotonic, prev_sha256:hex|null, row_sha256:sha256(canonical row minus row_sha256), ts:ISO-UTC, actor:'oracle:hq'|'oracle:pi'|'oracle:planner'|'nara'|'human:<id>'|'system', to:'nara'|'oracle'|'owner', kind, msg_id, thread_id, in_reply_to:msg_id|null, expires_at (required for plan_item/question; plan_item <=7d), provenance:{transport:'cli'|'planner_reconciler'|'daemon_lane'|'ui_argv', agenda_id?, revision_sha256?, focus_receipt_sha256?, session_id?, wrapper_request_id?}, body}.
- msg_id is content-addressed: '<actor>-'+sha256(actor+kind+canonical body+provenance.revision_sha256)[:16]. Re-posting identical bytes is an idempotent no-op; for planner items the index is folded into the body.
- Row limit is 16 KiB, with body text fields at most 8 KiB (the Pi mailbox limits).

ORACLE -> NARA KINDS:
- plan_item. body {title, objective, why_now, task_class in {documentation, tests, repository_code, deterministic_replay, draft_specification, result_projection, ledger_hygiene}, allowed_write_paths[], artifact_path, acceptance:{test_argv:[str], test_file, must_fail_before:true}, budget:{wall_clock_minutes<=30, max_attempts<=2, max_diff_lines<=400, max_output_tokens}, priority, depends_on[]}.
- withdraw. body {target_msg_id, reason}.
- answer. body {text, re_msg_id}.
- integration_receipt, oracle:hq only. body {target_msg_id, branch, head_sha, review_verdict in {merged, changes_requested, rejected}, merge_commit|null, review_ref}.
- page_update, phase 3. body {section 

NARA_ROLE_CHANGES
 - Nara becomes the lab's implementor. Its daemon gains a work lane (new orchestrator/nara_work_lane.py) that runs one Oracle plan_item per wake, deterministically admitted, outside the coordinator's fixed research menu. That menu (coordinator_actions.py:101-293) and the campaign schema (schema/research_campaign.schema.json:155-164) stay unchanged. Nara remains the only scheduler of its own work: Oracle's plan is an inp
 - Spawning: each admitted item becomes one D-062 task packet. The packet runs in a fresh pkt/<id>-<nonce> worktree with a local-Flash builder (tools/qwen_builder.sh with QWEN_ENDPOINT=http://127.0.0.1:30080/v1/chat/completions and QWEN_MODEL set to the served id from config/model_deployment.json, called by absolute main-checkout path). The builder is local only, per D-014/D-061. Every spawn is appended to run_state/pac
 - Write scope: Tier-P paths only (workers/ tools/ tests/ docs/ bench/ experiments/, notes/ for lab documents), minus the fence files and the scientific registry manifests. Nara never edits orchestrator/, ui/, schema/, cron/, config/, run_state/ semantics, CLAUDE.md or DECISIONS.md, and never merges or pushes to main. Its output is a validated pkt/* branch that Oracle HQ reviews and merges under D-079.
 - Reporting: Nara writes receipts and reports to the mailbox, rows to week1.run.jsonl with agent 'nara', and a kind:'event' mirror row to memory/lab_channel.jsonl. It no longer needs a 'nara' actor added to the lab_channel participant registry.
 - Initiation (phase 2): Nara can start messages to Oracle. It sends a question, a capability_request (for lab, UI or Nara fixes that belong to Oracle), or a proposal (focus change, campaign close, scientific kill). This closes the 'no Nara->Oracle path' gap.
 - Ledger organization and hygiene (phase 2): a Tier-P CLI or worker appends validated idea_ledger events: agenda_item_consumed, and cluster_killed limited to hygiene codes superseded_duplicate and non_research_artifact (workers/idea_ledger.py:54-62; D-075). Nara also reaps stale runs with the existing active_run.reap_stale_runs (orchestrator/active_run.py:262) and archives non-destructively with tools/research_archive.
 - Budget: every lane item is charged to the existing coordinator_budget.jsonl under class 'sdlc'. That share is already configured at 10% and unused (coordinator.py:110-116), and the charge counts toward the daily cap of 60.
 - improve_system (D-066) stays dark. Changes to Nara's own capabilities become Oracle's area. Nara may send a capability_request instead of self-modifying, which also preserves 'Nara cannot amend its own contract' (DECISIONS.md D-040).

ORACLE_ROLE_CHANGES
 - Oracle becomes the plan-setter, the integrator and the lab/UI/Nara-capability developer. It is one attributed principal with three registered transports, and each has a different grant, so the owner must ratify this. oracle:planner is the headless twice-daily planner; it drafts plans and has no tools. oracle:hq is Codex HQ; it develops and integrates, and today it is the only Oracle form with written lab-repo authori
 - Planner: a new immutable release adds 'Nara' to the owner enum. The live release 4f154d6 checks this at personal_agent/daily_planning.py:294 and states it in the prompt at :67 ('Owner is Oracle or Codex'). The release also reads the mailbox summary from research_ops_status and sizes context_tokens (hard-coded 32768 at :44) and the 24,000-byte packet cap (:259) to the served window from Phase 0. The planner stays prop
 - Integration: Oracle HQ reviews each validated pkt/* branch with the framework code-review skill, the full suite and one env -u MOCK_LLM smoke (CLAUDE.md workflow rule 4). The owner's standing requirement of an adversarial review loop before merge is carried forward (D-080). HQ then merges under D-079 and posts an integration_receipt, which is the only thing that produces a page accomplishment.
 - Lab/UI/Nara-capability development: Oracle HQ owns orchestrator/, ui/, schema/, cron/, the daemon unit spec, the packet dispatcher and Nara's builder and fence. It writes ui/ only when no ui-session worktree is live (none is live now); otherwise it hands the UI session a spec (CLAUDE.md:102-111). It stays the single serial integrator for the spine (nara.py, tool_registry.py, iteration_record schema).
 - Frontier fence: oracle:hq is Codex, a frontier model. Its plan_items are restricted to engineering and operations classes. Research-content items (hypotheses, protocols, thesis memos) must come from oracle:planner or oracle:pi on local pinned weights (D-061), and admission holds violations.
 - Memory (phase 3): record the owner's 2026-09-22 direction verbatim as an observe or decide event in oracle_system/state/events.jsonl, with subjects owner_goal and lab_purpose. Resolve D1 (241 ledger lines on disk versus 23 at HEAD) by committing the ledger or backing it up durably. Add an owner-goals/lab-purpose section to oracle-brief and the planner packet. Observe terminal mailbox receipts into the ledger. Checkpo
 - Pi mailbox (independent repair, phase 3): replace the hard-coded OWNER_SESSION_ID constant (oracle-mailbox.ts:25) and the session_id in daily-ops.json with a rebindable session-binding file. Clear or re-send the processing_failed seven-day-review envelope; the owner or an operator must do this from the bound session or by rebinding. Raise the Pi models.json maxTokens and reserveTokens together with the served window,

UI_PAGE_OWNERSHIP
 - Agenda / sealed plan: owned by oracle:planner through the existing sealed agenda plus the digest-verified ui/backend/daily_ops_agenda.read_pending_agenda. Unchanged.
 - Work cards: Oracle owns the content and Nara owns the status. Card title, what and benefit come from plan_item rows, bound to the agenda revision and the focus receipt sha, like the existing binding in daily_ops_work_plan.py:129-175. Status is projected from Nara receipts: admitted->authorized, claimed/attempted->in_progress, held/blocked/rejected/quarantined->blocked, validated->done with an 'awaiting integration' l
 - Accomplishments: produced by Nara's work but projected only from an oracle:hq integration_receipt with review_verdict=merged. That receipt carries the artifact sha, test digest, premerge_ok and merge commit, and the accomplishment gets status 'complete' (daily_ops.py:169-188). Completion is never inferred from report prose (daily_ops.py:6-9).
 - Goals and improvements: owned by Oracle (oracle:hq or oracle:pi) through a phase-3 page_update mailbox row validated against the existing summary contracts (_GOAL_OWNERS and _IMPROVEMENT_STATUSES, daily_ops.py:64-66). An improvement flips to implemented or verified only from Nara receipts plus the integration receipt. This replaces hand edits to run_state/daily_ops_brief.json goals. generated_at stays preserved, per 
 - Agents panel: projected by the system. Nara's status comes from lane receipts (working, idle, blocked) plus the systemctl probe (daily_ops_bridge.py:803-820). Oracle's status comes from the planner latest.json and the Pi mailbox heartbeat. Neither agent writes its own status text.
 - Research focus / thesis: owner or operator only, through the select_focus compare-and-swap (research_focus.py:321-374, execution_authorized always False). Oracle and Nara may only post proposal{focus_change}, which the page shows as pending.
 - Messages: owner-to-Oracle stays as it is (target must be oracle, daily_ops.py:489). A new read-only Oracle<->Nara thread panel shows the mailbox fold. Add 'nara' to _ACTORS (daily_ops.py:70) so projected Nara rows validate. Owner-to-Nara posting, if the owner wants it, is phase 3 as a human:ui note or hold through blessed argv (the D-046 pattern), never direct file writes.
 - Decisions (approve, skip, modify, reprioritize, hold): owner only. The approve and hold mailbox kinds require a human:* actor; models never approve (HANDOFF.md:29; daily_ops_work_plan.py:3-7).
 - Channel page: mailbox events are mirrored into memory/lab_channel.jsonl as kind 'event' rows, with no lab_channel code change. The UI turn endpoint still cannot pass an author, which preserves D-067's anti-impersonation property.
 - ui/ source code: owned by Oracle HQ, which writes it only when no ui-session worktree is live (CLAUDE.md workflow rule 2). Nara never writes ui/; premerge must add ui/ to Nara-lane packets' out-of-scope list unconditionally, not only while a UI session is live (tools/premerge_check.sh:63-65).

PHASES
 - PHASE 0 - Context bring-up (prerequisite, in flight now). Goal: the largest context tier W that passes bench/flash_context_eval with no restart or identity change, inside the 10 GiB host reserve (report host_reserve_gib 10) and the owner-declared memory floor.
- Current facts: 64K failed with candidate_exit_restart_oom_or_identity_change while minimum MemAvailable was 116 GiB. Investigate the identity/restart cause b
 - PHASE 1a - Build dark (Oracle HQ codes; all flags default off; tests under MOCK_LLM=1).
- NEW orchestrator/oracle_mailbox.py: store, hash chain, fold, transition validator, and CLI post/receipt/approve/list/show with --as validated against the actor registry. Tests: tests/test_oracle_mailbox.py covers idempotent msg_id, chain break detection, illegal transitions refused, human-only approve, and 16 KiB cap.
- NEW sche
 - PHASE 1b - First supervised loop (after the owner ratifies D-082..D-084 and Phase 0 has exited).
- Oracle HQ runs `oracle_mailbox post --as oracle:hq` with ONE documentation or tests plan_item, for example a docs/ or bench/ file with a red-first pytest.
- The operator runs `env -u MOCK_LLM python -m orchestrator.nara_work_lane run-one` in the foreground on real Flash.
- The receipts must reach admitted, then claimed,
 - PHASE 1c - Arm the daemon (owner-run service change).
- The owner adds a drop-in to ~/.config/systemd/user/nara-daemon.service.d/ setting NARA_MAILBOX_LANE=1, NARA_PACKET_AGENT_CMD='["bash","/home/decross1/projects/a_bgt_rsi/tools/qwen_builder.sh"]', QWEN_ENDPOINT and QWEN_MODEL, then restarts nara-daemon.
- Run a finite trial: at most 3 items over 48h, at most 1 active.
- Stall detector and loop_alert stay on.
- Exi
 - PHASE 2 - Nara to Oracle, plus lab-ops scope.
- Kinds question, capability_request and proposal.
- Oracle read paths: a nara_mailbox section in research_ops_status and an `oracle_harness nara-mailbox` read command.
- Ledger hygiene CLI in Tier-P: agenda_item_consumed and hygiene-only cluster_killed.
- Wire reap_stale_runs and research_archive as lane task_class ledger_hygiene.
- Widen repository_code to workers/, too
 - PHASE 3 - Oracle manages page, plan and memory.
- New planner immutable release: 'Nara' owner, receipts input, window-sized context.
- Lab-side agenda->plan_item reconciler (oracle:planner, digest-bound, held until owner approve unless D-084 says otherwise).
- page_update kind for goals and improvements.
- UI Oracle<->Nara thread panel.
- Pi mailbox rebinding file, and a fix for the 503 relay.
- Oracle memory work: v
 - PHASE 4 (evidence-gated) - Research-bearing and parallel work.
- Local-weight Oracle items of class draft_specification. Example: the focus's next_action thesis-disposition memo, which is exactly what unblocks focus_before_new_topics (coordinator.py:1470-1478).
- Multiple builder sub-agents per item only after the Phase 1-3 metrics are in.
- Promotion to Oracle stage 4 or 5 per oracle docs/ARCHITECTURE.md:40-55, with

DECISIONS_TO_RECORD
 - D-082 (lab, owner-ratified, with the owner's 2026-09-22 direction quoted verbatim, recorded by Codex without impersonating the human per D-078:3865-3867): Oracle<->Nara role split and runtime implementor lane. It EXECUTES D-062's deferred 'coordinator-runtime dispatch ... its own future gated decision' (DECISIONS.md:3106-3107) for the work lane only: Tier-P paths, 1 active item, per-item wall-clock/attempt/diff/token
 - D-083 (lab): Mailbox contract. It fixes the schema, the actor registry (oracle:hq, oracle:pi, oracle:planner, nara, human:*), the fixed actor per transport, the hash chain as tamper-evidence rather than authentication, and the human-only approve and hold kinds. It extends D-046 (DECISIONS.md:2267+) so non-human actors write through blessed CLIs with actor stamps, while direct memory/* writes stay rejected.
 - D-084 (lab): Admission and standing mandate.
- Allowed classes versus always-held classes (from AUTONOMY_EXECUTION_CONTRACT.json).
- Whether oracle:hq and oracle:pi items auto-admit under a finite, expiring mandate.
- Whether planner-sourced items need per-item owner approve. Recommended: yes, until evidence.
- That engineering items bypass focus_before_new_topics while scientific work does not.
- That lane spend cha
 - D-085 (lab): Page ownership matrix. It supersedes docs/daily_lab_workspace.md:5 ('Nara is the observed research runner') and the ui/backend/daily_ops.py:489 rationale 'Nara is observed state', and adds 'nara' to _ACTORS (:70). It amends docs/daily_lab_workspace.md:39-41 ('curated ... not model-generated') and daily_ops_work_plan.py:3-7 so model-produced cards and accomplishments are allowed only as receipt-bound dete
 - CLAUDE.md amendment (owner): the writer and file-ownership matrix for two autonomous writers. Oracle HQ owns the spine, orchestrator/, ui/, schema/ and cron/. Nara-lane packets own Tier-P paths. This amends CLAUDE.md:61-68 (one primary plus one UI session) and :70-76. Keep 'no ui/ writes while a ui-session is live' and the single serial spine integrator.
 - Oracle charter SCOPE amendment (only Derrick can ratify; human_confirmed is reserved in the harness, oracle_harness/integrity.py:402-404). It replaces charter.authority.a_bgt_rsi 'observe directly; coordinate changes with Nara'. It also amends oracle docs/ARCHITECTURE.md:34-38 (no spine writes or lab merges) and :127-130 (cross-system mutations are Red). It must name which Oracle transport holds lab, UI or Nara-capab
 - Oracle-side D1: commit Oracle's ledger or back it up durably (241 lines on disk, 23 at HEAD). Also record whether Pi gets any lab repo-write grant; today oracle AGENTS.md:5 names only Codex sessions.
 - Keep, explicitly recorded: research_focus.select_focus stays 'never called by a model' (research_focus.py:336). Oracle and Nara only propose focus changes, and execution_authorized stays False.
 - Phase 0 runtime decision (owner): the served window W, and the memory floor (the 262K eval runs with an 8 GiB floor against the 20 GiB floor in the planner and shadow driver). Also whether to use FP8 KV. This is a model/runtime change gated by MODEL_TOPOLOGY_POLICY.md.
 - Service activation (owner-run): the nara-daemon drop-in env for NARA_MAILBOX_LANE and NARA_PACKET_AGENT_CMD, and any new planner immutable release. Persistent service changes are Red (oracle AGENTS.md:14-18).
 - Re-scope the unratified SEVEN_DAY_PLAN.md:42 and AUTONOMY_EXECUTION_CONTRACT.json executor, which is the oracle-agent worker today. Route lab and experiment tasks to Nara's lane, and keep the oracle-agent worker for oracle_system tasks.

INVARIANTS
 - Kill switches win. run_state/pause_coordinator, the d049_ratified sentinel, pause_frontier and pause_weekly_upgrade are checked before any lane action. The lane runs inside _run_pass's existing gate ladder (nara_daemon.py flock, sentinel, pause, preflight). While paused, the mailbox may queue but nothing executes. No agent removes a pause or touches a sentinel.
 - Nara never merges, pushes to main, or edits its own fence. The fence is the dispatcher, the lane, the mailbox module, premerge_check.sh, qwen_builder.sh and the daemon, all of which move into the premerge protected list. Nara also never edits CLAUDE.md, DECISIONS.md, the spine, ui/, schema/, config/ or run_state/. No self-escalation (D-040 MUST-NOTs; 'Nara cannot amend its own contract').
 - The runtime is local only. Builders and research-content authors run on the pinned local Flash. Frontier CLIs remain falsifiers only (D-061). oracle:hq (Codex) may author engineering work orders but never research hypotheses or protocols that Nara ingests. Every frontier call is logged to run_state/frontier_calls.jsonl.
 - Human gates and human-only records stand. Approve and hold are human:* only (override_log.py:42-44 pattern). No model-authored approval authorizes work. Agreement between models, or transport success, is never approval. Scientific execution, registration, promotion, capital, credentials, model/runtime and service changes are always held.
 - Attribution is honest but unauthenticated. Every row names its actor, and each transport can only write its own actor. The hash chain proves order and integrity, not identity (D-067 lesson). No new process may pose as Oracle after a session change (daily_lab_workspace.md:83-86).
 - Append-only. The mailbox, idea_ledger, run log, packets.jsonl, spawn.jsonl and Oracle's events.jsonl are never rewritten. 'Organizing ledgers' means new events or derived projections only. A rewrite needs one-off owner ratification with backups (D-048).
 - Validation is never coerced. Admission refusals and illegal transitions are recorded with reason codes. A missing receipt is not a pass. Accomplishments require an integration receipt. The page never infers completion from prose.
 - Merges go only through the verify gate: framework code-review on the commit range, the full suite, one real env -u MOCK_LLM smoke, and the owner's adversarial-review-before-merge requirement. Same-weights Oracle review of Nara's work is not independent (MODEL_TOPOLOGY_POLICY.md:40-41). Objective checks carry the weight.
 - The focus never authorizes execution. select_focus stays an operator compare-and-swap. The campaign pointer changes only by authorized adoption. Nara's hygiene kills are limited to non-scientific codes.
 - Firewall (D-014): the mailbox is a bounded runtime artifact in a_bgt_rsi. It never carries framework-brain or agent_system memory content into Nara's runtime. Oracle's memory is built from verbatim owner directives, the charter and events, never by summarizing the human's reflective writing (inviolate rule 9).
 - Logging (rule 6): every lane step appends to week1.run.jsonl with agent 'nara' or 'oracle:<transport>'. Fallbacks such as builder unavailable or lease busy are explicit, logged and time-capped. Version pins stay verbatim, and MOCK_LLM discipline holds.
 - Resource safety: the lane takes a shared GPU lease, so an exclusive holder such as the context eval or flash_resident blocks it. It also checks a memory floor before invoking the builder. 'Killing' never includes stopping flash-resident, UI or Oracle services.

RISKS
 - Single Flash request slot (max_running_requests 1). The lane runs a builder for up to 30 minutes while holding the daemon flock, so cron cycles skip. The planner timer (08:00/18:00 PT), Pi prefills and channel turns queue behind it and may time out. Mitigations: a shared lease on .weekly-upgrade-gpu.lock, per-item token caps, lane quiet windows around planner slots, and an explicit 'deferred:lease' receipt rather tha
 - Phase 0 is unresolved. The 64K failure was not memory pressure (MemAvailable minimum 116 GiB), so the identity/restart cause is unknown. The 262K eval runs with an 8 GiB floor. Evals stop nara-daemon (flash_resident.py:233) and leave it stopped, which is how it is now. Builder prompt sizing, the planner release and the Pi compaction settings all depend on W, so a premature cutover would mis-size every consumer.
 - Self-escalation through Tier-P. tools/premerge_check.sh and tools/qwen_builder.sh sit under tools/ and are not protected today (premerge_check.sh:54-61). A packet could weaken its own gate. They must join PROTECTED_EXACT and the admission deny list before arming.
 - Fence enforcement is builder-side only today. files_in_scope is enforced by qwen_builder.sh, not by the dispatcher or premerge. A different agent_cmd could write anywhere under the non-protected paths. The dispatcher-side diff-vs-scope check is required.
 - Known dispatcher defects: worktree reuse (packet_dispatcher.py:101-106), shell=True host commands (:71-75), done despite a nonzero agent rc, the uncommitted red test trap (self_improve.py:783-786), and no consumed marker in consume_authorize_fix_queue. Arming before these are fixed re-runs or falsely closes work.
 - Task_class mis-declaration. A 'documentation' item could carry scientific registration or protocol content. Path fences catch file locations, not semantics. Mitigations: registry-manifest deny patterns, owner approve for planner items, and audit sampling by Oracle HQ.
 - Focus-hold bypass drift. Letting engineering items run while focus_before_new_topics is on could re-create the 09-02 off-field drift. Items must stay tied to the selected focus receipt or be pure lab-ops, and a stale focus sha leads to held.
 - Attribution spoofing. Any local process can run `post --as oracle:hq`. Labels are unauthenticated, and the hash chain gives tamper-evidence only. Accept this as a known limit (D-067), or add owner-key signing later without exposing ~/.local/state/oracle-lab-ui/owner.key.
 - Stale daemon code. The memory rule is to restart nara_daemon after any orchestrator/ merge (it ran pre-7dcc695 code for 14 days). The daemon, lane and dispatcher are Oracle-owned and change often, so every HQ merge touching them needs a restart and a version receipt.
 - Worktree sprawl. About 60 worktrees already exist (git worktree list). Nonce pkt worktrees per attempt need a bounded cleanup policy that is non-destructive for unmerged validated branches.
 - Accomplishment inflation or false-green. Same-weights review is not independent, so without frontier or Codex adjudication a Flash-built, Flash-reviewed change could look complete. Only oracle:hq integration receipts produce accomplishments.
 - Oracle identity confusion. If the owner means Pi-on-Flash as 'the Oracle', it has no lab write grant and its mailbox is dead: bound to session 01a0bbce and blocked by processing_failed. Its 32K compaction failures (the 4096 summary cap) make it unreliable for multi-file development. Development authority must be named explicitly (D-082 and the charter amendment).
 - The owner UI's 'Ask or change the plan' stays 503 until the Pi mailbox is rebound or unblocked. That is independent of this mailbox but will be read as 'the Oracle is offline'.
 - Mailbox growth and fold cost on the UI refresh path (a 2-second throttle, bridge :824). Bound the fold to the active window, or cache it by file signature.

VERIFY: REVISE BEFORE RATIFICATION. The overall shape is sound and most citations check out: a separate lab-side log, a lane outside the coordinator menu, a deterministic page projection, human-only approve/hold, Nara never merging, focus changes kept to operator CAS, and D-061 fencing. Phase 1 as written is still not safe to arm and would not reliably close the loop, for five reasons. (1) Model-written code and tests run unconfined on the host, and containment is pushed to Phase 2, against D-081's containment gate, the practice used in LAB011, and the AUTONOMY contract. (2) The `--as` CLI lets any same-uid process forge human approvals, Nara receipts and HQ integration receipts; the hash chain does not prevent this. (3) Placing the lane after _run_cycle means every mailbox wake runs a research cycle, and the lane's own writes wake the daemon again. The lane also holds the cron flock for up to about 150 min and a GPU lease that does not exclude the planner. (4) Nothing defines who authors the red-first acceptance test or keeps it unchanged, and the planner cannot emit admissible items at all. (5) Work cards depend on a volatile pending agenda. On governance, D-078..D-081, AGENTS.md, the CLAUDE.md Codex section, oracle AGENTS.md rules 4-6 and the deployed v7 runtime pins are all uncommitted, and HEAD's oracle AGENTS.md still forbids silent edits to a_bgt_rsi. Phase 0's cutover list misses the hard pins at agent_wrapper/deployment.py:149 and flash_resident.py:61-64, which would make Na
 x [P1 SAFETY, blocks 1b/1c] Model-written code runs on the host with no confinement. The dispatcher runs the acceptance test in the worktree with shell=True (packet_dispatcher.py:71-75,178,260) and the builder runs `bash -c "$PKT_TEST_CMD"` (qwen_builder.sh:289-295). Only 4 env keys are stripped (packet_dispatcher.py:233-235), and ~/.codex/auth.json is readable by the same uid. Test code that imports builder output can
 x [P1 SAFETY] Approval and completion rows can be forged. With a CLI `--as` flag 'validated against the actor registry', the check covers the label, not the caller. Pi, a builder-run test, or any other same-uid process can post `approve --as human:x`, `receipt --as nara state=validated`, or `--as oracle:hq integration_receipt merged`, which forges an owner approval or an accomplishment. override_log.py:42-44 is also on
 x [WRONG ASSUMPTION: lane placement] _maybe_dispatch_one_packet (nara_daemon.py:442) is reached only after three things: work_exists is true, the total-cap precheck with NARA_CYCLE_BUDGET=6 passes (:297-300,427-431), and a full research _run_cycle has run (:434). A 'mailbox:N' reason therefore forces a coordinator cycle on every wake. Under focus_pending each of those writes a queue-hold row to coordinator_cycles.jsonl
 x [SELF-WAKE LOOP] The lane writes to files the daemon watches: the mailbox (new watch), memory/lab_channel.jsonl (nara_daemon.py:116) and run_state/packets.jsonl (:120). wait_for_wake fires on any mtime change (:146-148) and resets the backoff (:107-108), so every receipt schedules another wake, and each wake runs a research cycle (previous finding).
 x [CONTENTION NOT DESIGNED] dispatch_packet is synchronous under the coordinator-cron flock (nara_daemon.py:396-449). Red-first, agent and verify each get the full wall_clock (packet_dispatcher.py:163,178,245,260), so 30 min x 2 attempts can hold the flock (the hourly cron is skipped) and the single Flash slot (max_running_requests 1) for about 150 min. The planner takes only LOCK_SH on the same GPU lock (daily_plannin
 x [WRONG ASSUMPTION: builder 'retargeted by env'] qwen_builder.sh calls the endpoint with raw curl and no chat_template_kwargs, bypassing agent_wrapper. generation_policy.py:63-78 sets enable_thinking explicitly for Flash. With thinking on, max_tokens 6144 can be spent on reasoning and leave `content` empty, which becomes an unparseable reply (qwen_builder.sh:223-240). Skipping the wrapper also loses calls.jsonl, the l
 x [MISSING: acceptance-test authorship and integrity] plan_item names acceptance.test_file, but nothing defines who writes the test or where it lives. The planner cannot write files (ProtectHome=read-only, no tools), and body text is capped at 8 KiB. The pkt worktree is cut from HEAD (packet_dispatcher.py:101-116), and the builder assumes the test already exists and is out of scope (qwen_builder.sh:104-114). tests/ is 
 x [PLANNER->NARA PATH INFEASIBLE] Sealed tasks carry only title, why_now, artifact, validation and owner (daily_planning.py:50,65-67,289-295; daily_ops_agenda.py:175). They have no task_class, allowed_write_paths, test_argv or budget, so a deterministic reconciler cannot produce an admissible plan_item without an LLM or a human. The planner prompt limits every task to research-focus prerequisites (daily_planning.py:56-
 x [PAGE PROJECTION FRAGILE] read_work_plan fails closed without a verified pending agenda (daily_ops_work_plan.py:154-156) and requires matching agenda revision and focus receipt (:157-163). Cards also require benefit, cost, conviction and worth_time (:30-33), which plan_items do not carry. An HQ item without revision_sha256, the twice-daily reseal, agenda expiry (daily_ops_agenda.py:342-346) or an owner approval will 
 x [WRONG ASSUMPTION: lab_channel mirror] 'event' rows are 'DERIVED AT READ TIME ... NEVER stored' (lab_channel.py:5-7,264), and stored kinds are human|oracle|nara|pi (:21). Storing event rows breaks that contract. They also enter the 12-row transcript tail of the Gemma turn context pack (lab_channel.py:55,320-322), which repeats the stale-tail failure D-067 fixed (DECISIONS.md:3303-3306) and puts Codex-authored plan te
 x [FENCE TOO WIDE] Tier-P (self_improve.py:103-104) contains Nara's own gates. experiments/exp008_qat_eval/preflight_mem.sh is the daemon and cron memory gate (nara_daemon.py:57,162-167). workers/idea_ledger.py KILL_CODES (:54-62) is the validator that limits hygiene kills. tests/conftest.py holds the D-048 live-artifact guard. bench/flash_* holds the runtime eval and cutover drivers. experiments/*/PREREGISTRATION.md a
 x [AUTHORITY NOT AT HEAD] Every authority the design cites is uncommitted. D-078..D-081 exist only in the working tree (`git show HEAD:DECISIONS.md | grep -c D-079` = 0). AGENTS.md is untracked, CLAUDE.md's Codex-authority section is an uncommitted diff, and oracle_system AGENTS.md rules 4-6 are modified. HEAD's oracle rule 5 still reads 'Changes to a_bgt_rsi route through Nara's lab channel and governed packet machine
 x [DIRTY MAIN AS BASE] pkt worktrees are cut from HEAD, but the deployed runtime is uncommitted: the v7 pin and the host_reserve 20->10 change (agent_wrapper/deployment.py:152 diff; human/sessions/2026-09-21.md: 'the deployed v7 pin exists only in the working tree'). Red-first runs in the dirty main checkout (packet_dispatcher.py:178) while verify runs in the HEAD worktree (:260), so the two checks see different code. 
 x [PHASE 0 EXIT MISSES HARD PINS] context_length is pinned to exactly 32768 in agent_wrapper/deployment.py:149,168, and flash_resident.py:61-64 pins profile_sha256. A 64K or 262K profile raises in selected(), so _preflight_ok returns False and Nara refuses every pass (nara_daemon.py:156-161). The same thing happened on 2026-09-21, when a validator changed under the running daemon and it refused every pass for about 9 h
 x [PHASE 0 BREAKS GOAL 2] A larger W lowers steady MemAvailable. CONTEXT_CAPACITY_AUDIT.md:47 projects about 19.2 GiB idle low-water at 262K. That is under the planner's hard 20 GiB floor (daily_planning.py:313-316), run_shadow_task.py:36, the AUTONOMY contract's required_available_memory_gib 20 and the payoff preregistrations at 20 GiB (session note 2026-09-21, item 2). Promoting 262K would switch off the planner, whi
 x [OVERCLAIM, rule 4] The inference that '64K failed but was NOT memory pressure' is unsupported. report.json shows monitor samples:1, so the 116 GiB minimum is effectively a pre-load reading. The documented startup trough is a draw of about 95 GiB (FLASH_MEMORY_MARGIN_OPTIONS.md:18), and the OOM counters are initial values. The cause should be reported as undetermined.
 x [MISSING: crash recovery] flash_resident stops nara-daemon on every transition (flash_resident.py:233,344), and systemd stop kills the builder mid-attempt. That leaves a 'dispatched' row in packets.jsonl with no closing row and a mailbox item in claimed/attempted with no terminal state. Under the one-active-item rule the lane then deadlocks. The design needs lease expiry, an abandoned/unknown_effect state and worktre
 x [NONEXISTENT COMPONENT] The 'daily backup snapshot' does not exist. crontab has only the weekly cron/snapshot-chroma.sh (Sunday 04:30) and the hourly run-coordinator.sh. run_state/*.jsonl is not ignored as a pattern (.gitignore:68-103 lists individual files), so the mailbox also needs a .gitignore change that the plan does not list.
 x [CONTRADICTS DOCUMENTED DECISION] Having the lane machine-write run_state/spawn.jsonl conflicts with 'spawn.jsonl stays a hand-kept discipline; the semantics are deliberately not mixed' (packet_dispatcher.py:13-15; LOOP_V1.md:236). spawn.jsonl and packets.jsonl are both tracked in git, so every lane item dirties the checkout HQ has to merge into.
 x [MISSING: integration logistics] The live daemon runs in the only checkout that has main checked out, and that checkout is dirty. The plan has no step to restart nara-daemon or the UI after merges that touch workers/ or orchestrator/, although memory records a daemon running stale code for 14 days. HQ is an interactive session with no trigger, so validated branches wait until the owner opens one. 1c's '≤3 items/48h' 
 x [SCHEMA MISMATCHES] plan_item objective allows 8 KiB, but the task_packet objective has maxLength 2000 (schema/task_packet.schema.json:28), so an oversized objective raises PacketDispatchError instead of producing a refusal. test_argv needs a schema/ change to test_cmd (:51), which Phase 1a does not list, and the builder still runs `bash -c` regardless. msg_id is derived from the body and excludes expires_at, so re-p
 x [ADMISSION NOT DETERMINISTIC] 'oracle:hq draft_specification about research content' requires a semantic judgement. task_class is self-declared, so research content can enter as documentation under docs/. The deterministic rule would be: hold every oracle:hq draft_specification and exclude research directories by path. The focus-receipt check applies only 'when present', so HQ can bypass it by omitting the field. The
 x [BUDGET] _charge_daily_ledger swallows every exception (coordinator.py:257-269), so a failed lane charge is silent, which violates rule 7. The charge unit per item is undefined. The lane also stays gated by the total-cap precheck that is coupled to research spend (nara_daemon.py:297-300).
 x [LEDGER HYGIENE DOES NOT FIT PACKETS] reap_stale_runs moves and unlinks run_state files (active_run.py:292-297), and idea_ledger events append to memory/. Neither can be done as a Tier-P pkt branch, and both conflict with 'Nara never edits run_state/memory'. ledger_hygiene needs a separate in-process executor with its own contract.
 x [MINOR] The planner packet keeps only lab.research_focus (daily_planning.py:250-252), so the Phase 2 nara_mailbox section in research_ops_status does nothing for the planner until the Phase 3 release. The bridge refresh runs on GET at most every 2 s with bounded reads (daily_ops_bridge.py:822-825; daily_ops.py:47), so re-verifying an unbounded hash-chained log from genesis on each refresh breaks that discipline; a bo
 x [PHASE 1a LARGER THAN NEEDED] These are not needed to close the loop: the hash chain (no protection against same-uid writers); 14 kinds, when Phase 1 needs only plan_item, receipt, approve, hold and withdraw; integration_receipt (derive it from git); the _ACTORS change (only the Phase 3 thread panel needs it, and the frontend union at ui/frontend/src/api/dailyOps.ts:9 is not updated); the lab_channel mirror; the cons
