# Claude red team (subagent, 2026-09-14) — key findings, condensed by primary session

K1 Idle resource / wrong metric: Gemma busy ~5 min/day since 08-31 (~130 calls/day); Qwen 2 calls in Sept (Jun 592, Jul 23, Aug 126). Last prod skeptic_attack on Qwen 2026-08-17; funnel 97 L0 + 3 L1 clusters; last evidence_level_changed 2026-08-24; coordinator ACTIVITY_CLASS_OF (orchestrator/coordinator.py:99-108) has NO action that runs the L0->L1 literature/skeptic pass. 380 coordinator:budget_refusal rows since 09-01, zero speed-caused refusals. Binding constraint = missing rung, not tok/s/temperature/vLLM.
K2 Existing weekly frontier agenda: 5 firings, 43 proposals, ALL status=proposed; memory/frontier_agenda.status.jsonl does not exist (agenda_cli never run); same 4 items proposed every week; 246 coordinator bubbles unanswered. A second weekly loop terminating at PROMOTE_PENDING_OWNER accumulates the same way.
K3 D-066 polarity: Gemma proposes, frontier only debates (both must affirm); D-061 frontier NEVER generates. Packet SDLC Tier P only (workers/ tools/ tests/ docs/ bench/ experiments/); version pins + cron/serve-models.sh are Tier S = refused, not escalated (docs/packet_sdlc.md:127). So model + runtime surfaces can never flow through the packet loop; they are owner-ratified by construction -> "weekly loop" for those surfaces is a weekly memo.
K4 Temperature evidence points the other way: workers/idea_judge.py CALIBRATION_TEMPS=(0.2,0.7); experiments/judge_calibration/results.json (2026-08-18, 224 calls) verdict_flip_rate 0.0268, precision 0.30 at both. Generator already 0.7/0.95 (hypothesize.py:205). D-076 elected redteam prompt at temp-0 greedy; D-077 attribution rests on B=0, kappa_old 0.943; a critic profile at 0.7 voids both batteries and the sha-pinned instrument (tests/test_redteam_critic.py). Falsifier: bench/redteam_cal 24 fixtures x {0.0,0.2,0.7} x 3 repeats on Gemma.
K5 N=3+selector: Qwen 16.5 tok/s, 6144 caps -> ~370 s/call; qwen38 blew the 45 s redteam wall 18/24 (D-076); 11.1% empty-at-cap -> ~30% chance one of three empty; selector (judge) failed calibration (precision 0.30); hypothesize.py ALREADY generates 1-3 candidates and self-selects in one call, so E2 compares production with a costlier restatement of itself. Falsifier: 20 topics x (1 vs 3 -> redteam select) on Gemma; score survival through literature pass.
K6 Judge circularity is live: last 7 days 570 frontier screen calls over 6 distinct prompts; promotion_near_misses 197 frontier vetoes across 6 iteration ids, methods(Claude) veto ALL 197, novelty(Codex) pass 109/inconclusive 88; finding_promotion.py:649-681 re-screens every survivor hourly with no prompt_sha256 cache. Same vendors would be weekly analysts + cross-reviewers + judges. Falsifier: label-swap test.
K7 Reproducibility ill-posed: Gemma NOT byte-deterministic at temp-0 (9-10/12 divergences, MoE-Marlin reduction drift, docs/external_spark_brief_review_2026-08-17.md:122-133); Qwen is (0/8). MTP acceptance falls as temperature rises -> E1 policy change registers as E4 runtime change; local MTP acceptance never measured. project_health_2026-09: Reproducibility RED (only exp001 has experiment.lock), Dependencies RED (no lock), Tests RED (7 failing: test_critic_cal x4, test_readjudication x3). Frozen manifest on unlocked env is not frozen.
K8 Challenger runtime duplicates D-073 (SGLang+DSpark track blocked on T1 license, T2 isolation, T3 instruments, T4 adoption); the 64-78 tok/s figure is single-source (0xBakeer) vs third-party measured 34-38; pin reasons: D-022 (v0.21.0 clears 40 tok/s floor via MTP), CUDA 13.2 gibberish, MARLIN line, role_setups §3 amendment triggers (guided output on Qwen; #40756 soak; v0.27.2 w/ #51812). 41 GiB available vs 30 GiB margin -> third server needs a solo window (D-072 windows ~5 h of owner time with loop paused).
K9 Benchmarks off-field: project's own ground-truth tier (exp001..exp012 synthetic games w/ known equilibria) absent; portfolio B fixtures authored by the agents being scored (Goodhart); portfolio D needs human anchors from a human with 246 unanswered bubbles; 3.8 card agentic numbers ran under Claude Code harness; agentic benchmarks need containers/egress the sandbox line (D-014, NemoClaw proxy) does not grant.
K10 Vendor limits: ISO week 35 177/177 Claude calls failed (OAuth expiry, invisible 10 days); Codex timeouts 26% last 7 days (92/348), median 105 s, p90 = 180 s cap; screen burned 10.9 frontier wall-hours last week on 6 prompts; four-team eval paused at 90% monthly spend 09-02. G1 ratified for "hypothesis/corpus text leaves the box" (LOOP_V1.md:322-325); weekly snapshot of manifests/hashes/memory ledger/failure clusters/coding fixtures is a scope expansion with no ratification row.

CUT: six-benchmark portfolio C-F (keep A + repo's own labeled instruments: bench/redteam_cal 24 labeled, bench/critic_cal, synthetic-game experiments); A->B->A three-round review (workers/frontier_review.py + self_improve.review() exist); 12-field schema + 7-gate ladder (experiments/PREREG_*.md pattern suffices); CTT/RSR/SUH (no noise floor; report raw vector with CIs); weekly cadence for runtime/model (trigger-driven, monthly human check); Session B until D-073 T1 clears + a trigger fires; explore(1.0)/critic(0.7) as production arms; 64K lane (Qwen serves 16K; 128K crosses unverified #40756 crash zone); weekly JSON contract (consumer is a human -> DECISIONS entry / session-note subsection).
MVP (~150 lines, one session): profile= kwarg on call_sync/call_with_tools resolving sampling + reasoning_effort via extra_body.chat_template_kwargs (wrapper has NO passthrough today) stamping profile name into the 14-field record; table of 17 call-site temps with rationale + qwen_builder.sh drift fix; one preregistered experiment on the calibrated instrument. No cron, no frontier, no new benchmark.
MISSING: meta-metric (acted-on/made; precedent 0/43); cost of NO_CHANGE; named reader + deadline; incumbent inventory; reasoning_effort knob absent at call site (only serve-level --default-chat-template-kwargs, Tier S; role_setups §7 item 6 never executed); noise floor; rollback drill (D-074 rollback never exercised); ToS/egress statement + G1 amendment; green tests precondition (7 red in eval instruments); MTP x sampling coupling; D-063 budget class share; daemon-restart rule after any orchestrator/ or wrapper merge; field-native success definition (synthetic tier ground truth).
PRIORITIES: 1 make L0->L1 rung execute (hand-run lit/skeptic pass on 3 L1 + 10 oldest L0 in one window; record survival + wall). 2 stop hourly re-screen waste (~10-line prompt_sha256 cache in finding_promotion.py; 10.9 h -> <1). 3 instrument before profiling (profile= + reasoning_effort passthrough; 5 fixtures on Qwen low vs xhigh). 4 one temperature experiment on bench/redteam_cal (24 x {0,0.2,0.7} x 3 on Gemma, preregistered). 5 convert Session B to trigger watchlist (check DSpark license, #52197 in stable tag, v0.27.2).
STEELMAN: sampling policy is uninstrumented drift (real hygiene gain); causal-separation is the lesson already paid for (window #1 retraction, window #2 cap confound, D-070, D-076); unmeasured headroom exists and GPU is idle (reasoning_effort, MTP acceptance, prefix caching on GDN never measured locally).
