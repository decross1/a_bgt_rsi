{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/decross1/a_bgt_rsi/schema/calls.jsonl.schema.json",
  "title": "Gemma 4 call record",
  "description": "One line of logs/dayN.jsonl. Every call to the model through wrapper.py produces exactly one record matching this schema. Each record is a research observation; reproducibility depends on metadata completeness.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "timestamp",
    "request_id",
    "model",
    "model_version",
    "temperature",
    "top_p",
    "seed",
    "prompt_messages",
    "completion",
    "usage",
    "latency_ms",
    "host_metadata",
    "caller_tag",
    "parent_request_id"
  ],
  "properties": {
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 / RFC 3339, UTC, when the request was issued."
    },
    "request_id": {
      "type": "string",
      "format": "uuid",
      "description": "uuid4 uniquely identifying this call."
    },
    "model": {
      "type": "string",
      "minLength": 1,
      "description": "Model name as served, e.g. 'gemma-4-26b-a4b-nvfp4'."
    },
    "model_version": {
      "type": "string",
      "minLength": 1,
      "description": "vLLM container image digest, e.g. 'sha256:04563c30...'."
    },
    "temperature": {
      "type": "number",
      "minimum": 0,
      "maximum": 2,
      "description": "Sampling temperature sent in the request."
    },
    "top_p": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Nucleus sampling top_p sent in the request."
    },
    "seed": {
      "type": ["integer", "null"],
      "description": "RNG seed sent in the request; null when no seed was set."
    },
    "prompt_messages": {
      "type": "array",
      "minItems": 1,
      "description": "Full chat message array exactly as sent.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["role", "content"],
        "properties": {
          "role": {
            "type": "string",
            "enum": ["system", "user", "assistant", "tool"]
          },
          "content": { "type": "string" }
        }
      }
    },
    "completion": {
      "type": "string",
      "description": "Full text of the model's completion."
    },
    "usage": {
      "type": "object",
      "additionalProperties": false,
      "required": ["input_tokens", "output_tokens"],
      "properties": {
        "input_tokens": { "type": "integer", "minimum": 0 },
        "output_tokens": { "type": "integer", "minimum": 0 }
      }
    },
    "latency_ms": {
      "type": "number",
      "minimum": 0,
      "description": "Wall-clock request round-trip in milliseconds."
    },
    "host_metadata": {
      "type": "object",
      "description": "Per-backend host metadata. Heterogeneous across backends (vllm-gemma exposes cuda_driver + vllm_image_tag; ollama-coder exposes ollama_base_url; anthropic exposes prompt_cache). Existing fields remain optional for back-compat with pre-multi-backend records.",
      "additionalProperties": true,
      "properties": {
        "backend": {
          "type": "string",
          "description": "Backend discriminator, e.g. 'ollama', 'anthropic'. Absent on legacy vllm-gemma records."
        },
        "cuda_driver": {
          "type": "string",
          "description": "CUDA driver version on the serving host (vllm-gemma). Optional."
        },
        "vllm_image_tag": {
          "type": "string",
          "description": "vLLM image tag, e.g. 'vllm/vllm-openai:v0.21.0' (vllm-gemma). Optional."
        }
      }
    },
    "caller_tag": {
      "type": "string",
      "minLength": 1,
      "description": "Identifies what issued the call, e.g. 'test_50_calls'."
    },
    "parent_request_id": {
      "type": ["string", "null"],
      "description": "request_id of the parent call in a chain; null until call chains begin on Day 4.",
      "oneOf": [
        { "type": "string", "format": "uuid" },
        { "type": "null" }
      ]
    },
    "run_id": {
      "type": "string",
      "minLength": 1,
      "description": "Run-mode identifier (experiment trial-loop, autoresearch sweep, or LOOP_V0 iteration) this call was issued under. OPTIONAL — set by wrapper.set_run_id; absent on calls made outside a run-mode context and on all pre-2026-06-06 records."
    },
    "backend": {
      "type": "string",
      "minLength": 1,
      "description": "Backend registry name that served this call (e.g. 'vllm-gemma', 'vllm-qwen', 'ollama-coder', 'anthropic'). OPTIONAL — absent on all pre-2026-06-10 records. Distinct from host_metadata.backend (a heterogeneous discriminator): this is the top-level blessed UI-attribution field."
    },
    "max_tokens": {
      "type": "integer",
      "minimum": 0,
      "description": "Generation cap (max_tokens) the caller requested for this call. OPTIONAL — a request param, not a logged-by-default field; absent when the caller passed no cap and on all pre-2026-06-06 records."
    },
    "profile": {
      "type": "string",
      "enum": [
        "deterministic", "planner", "precise", "coding_precise", "dialog",
        "scientist", "critic", "critic_current", "critic_medium", "critic_low",
        "qwen_card_thinking", "qwen_card_coding", "qwen_card_instruct",
        "gemma_card", "gemma_card_thinking", "explore"
      ],
      "description": "Named experimental generation profile resolved for this call. OPTIONAL; absent for legacy calls with no profile."
    },
    "reasoning_effort": {
      "type": ["string", "null"],
      "enum": ["low", "medium", "xhigh", null],
      "description": "Effective Qwen reasoning effort. null when the selected backend has no effort ladder or thinking is disabled. OPTIONAL; absent from legacy calls."
    },
    "sampling_extra": {
      "type": "object",
      "additionalProperties": false,
      "description": "Validated effective request controls beyond temperature/top_p/seed. OPTIONAL; absent from legacy calls.",
      "properties": {
        "top_k": {"type": "integer", "minimum": 0},
        "min_p": {"type": "number", "minimum": 0, "maximum": 1},
        "presence_penalty": {"type": "number", "minimum": -2, "maximum": 2},
        "repetition_penalty": {"type": "number", "minimum": -2, "maximum": 2},
        "chat_template_kwargs": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "enable_thinking": {"type": "boolean"}
          }
        }
      }
    },
    "finish_reason": {
      "type": ["string", "null"],
      "description": "Server-supplied finish reason, or null when unavailable. OPTIONAL; absent from legacy calls."
    },
    "reasoning_chars": {
      "type": ["integer", "null"],
      "minimum": 0,
      "description": "Character length of server-supplied reasoning text, or null when unavailable. It is not a fabricated reasoning-token count. OPTIONAL; absent from legacy calls."
    },
    "retrieval_context": {
      "type": ["array", "null"],
      "description": "Per-call retrieval provenance (P2). null when no retrieval ran. A list when one or more retrievals contributed to the prompt — typically generator and novelty-evaluation calls. Optional field; absent on records that pre-date Day 3.5.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["doc_id", "content_hash", "chunk_offset", "chunk_length"],
        "properties": {
          "doc_id": {
            "type": "string",
            "minLength": 1,
            "description": "Stable document identifier, e.g. 'arxiv:2401.12345'."
          },
          "content_hash": {
            "type": "string",
            "minLength": 1,
            "description": "Hash of the chunk content used in the prompt, e.g. 'sha256:...'."
          },
          "chunk_offset": {
            "type": "integer",
            "minimum": 0,
            "description": "Byte or token offset of the chunk within the source document."
          },
          "chunk_length": {
            "type": "integer",
            "minimum": 0,
            "description": "Byte or token length of the chunk."
          }
        }
      }
    }
  }
}
