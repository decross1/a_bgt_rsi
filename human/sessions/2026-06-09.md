# Session — 2026-06-09

## Focus: de-risk + harden the path to β (Dynamic Workflow)

Adversarially critiqued the 2026-06-08 α→β→γ direction, then ran a Dynamic
Workflow (`wf_22f76752-fcf`) to **de-risk β without building it** (β's autonomy
amendment is human-gated and NOT in effect). Target chosen with the human:
*de-risk + harden* — design + safe execution, no β code, no autonomy switch.
Three decisions taken in the plan dialogue: design **and** prototype an
independent novelty skeptic; **shelve** exp008 per D-039 + build the memory
guard; keep the proven host spine untouched.

### Base loop is proven (reframes the critique)
The adversarial worry "α leapt ahead of an unproven base loop" is **false**:
47 real iterations, reference-passing landed, exit criterion exceeded. α is a
clean in-scope forward investment. The real risk is the **path to β**.

## What ran
- 4-phase workflow: Assess (β-gap, autonomy-boundary, skeptic-design,
  mem-guard-spec) → Build (preflight_mem.sh, novelty_skeptic.py+test) →
  adversarial Verify → Synthesize.
- **Harness incident (logged):** the two heavy nested output schemas
  (`GAP_SCHEMA`, then `GUARD_SPEC_SCHEMA`) sent their agents into
  StructuredOutput truncation-loops (A1 looped 40×). Fixed by flattening to
  string arrays + cutting required fields + front-loading. The resume's
  **prefix-invalidation** re-ran most agents (changing the first call
  invalidates the cache downstream); A4 then looped on the un-lightened
  GUARD_SPEC. Resolution: stopped, **hand-harvested all 9 clean structured
  outputs, and self-synthesized** (the primary session's job). Lesson for next
  workflow: keep StructuredOutput schemas flat/shallow; put narrative in string
  fields, not nested object arrays.

## Findings by track

### 1. β substrate — A1 audit, **V-A1 verdict = FAIL on completeness**
Core thesis CONFIRMED (independently re-probed): nara-sandbox is genuinely
isolated — Python 3.13.5, 6 pip packages, **zero** apparatus deps (no
chromadb/openai/torch/BGE/anthropic), no repo, no venv, no chroma_db, /mnt
empty. So "exec our workers in the sandbox" is impossible and **D-031's
'NemoClawRuntime is a mechanical swap' is falsified**. β = a real **port**:
package Nara as an OpenClaw **agent bundle** (prompt + tool manifest + the
existing `inference` provider) driving a **host-side tool plane** that wraps the
unchanged Python (Chroma + BGE-M3 + run_state/memory stay host-side behind
tools; `nara.py` stays the host executor). The closed action menu becomes the
OpenClaw tool allow-list + NemoClaw policy preset (γ mechanism arriving at β).
**Weeks, not days**, sequenced host-tool-plane-first (lands under current rules).

**Hard blockers V-A1 caught that A1 under-weighted (independently verified):**
- **Egress proxy denies every host endpoint right now** (only `inference.local`
  permitted; `example.com` and `host:8000` both 403). The host tool plane —
  "the bulk of β" — has **no reachable network path today**. Not an open
  question; a live blocker.
- **A1's marquee evidence was wrong:** from inside the sandbox `:8000` returns
  HTTP 000 (no route), *not* `policy_denied`. The model is unroutable (network
  isolation) + a proxy allow-list — not ":8000 is policy-gated."
- **D-014/D-013 conflict:** OpenClaw's default agent harness is `claude-cli`
  (Anthropic, enabled-by-default). D-014 forbids the apparatus runtime from
  authenticating to Claude; D-013 pins to Pi. The β bundle must explicitly use a
  local-model harness and reconcile both. (No Anthropic creds present today, so
  claude-cli is non-functional — but the default is wrong for us.)
- **Gateway `:18789` is structurally dead:** no listener inside the container,
  `Ports={}` (nothing published). Worse than D-031's "cosmetic healthcheck";
  D-031's "gateway up, /health ok" claim is **stale**.
- Minor: GPU passthrough *works* in-sandbox (deps/weights/30 GiB cgroup cap are
  the real limits, not GPU); chroma store is 5088 chunks / 14 collections incl.
  `day3_*` test-fixture pollution (constrain the retrieve tool to the curated
  allow-list); `ui/backend/app.py` is a read-side precedent but off-limits to
  the primary session (new tool plane must live outside `ui/`); LOOP_V0.md
  line 229 + START_HERE.md still assert the falsified "mechanical swap."

### 2. Autonomy trust boundary — A2
`validate_plan` is a **quality** boundary (a parser/filter trusted **host** code
applies to untrusted model text — meaningful only because the privilege lives in
the host). At β, **Nara IS the agent and owns execution**, so an in-agent
`validate_plan` is self-policing — advice, not a boundary. D-040's rationale
conflates this with security. The real β security boundary is **NemoClaw
permission presets + OpenShell isolation + egress proxy** (runtime-enforced,
outside the agent). The alpha proof covered only **4 cheap schema-checkable
actions**; the three D-040 powers with real blast radius (build-tools,
spawn-coding-agents, commit/push) are **not schema-constrainable** — a JSON
arg-schema can't vet arbitrary code/effects.

**Must exist before β is armed:** (1) `NemoClawRuntime` actually implemented
(today a stub) + the v0.0.55 preset primitive's shape confirmed; (2)
`validate_plan` **relocated** outside Nara's process (host-side coordinator or
NemoClaw policy hook); (3) a verify-gate Nara structurally cannot bypass +
git credential scoped to `nara/auto/*` (physically can't push main); (4) the
free-memory pre-flight guard (built this session) as a runtime resource cap;
(5) egress allow-list with no trade endpoint; (6) revocation/kill-switch +
first-class grant/spawn logging. **MUST-NOTs enforced by capability absence**
(no trade tool granted; monotonic subset-only grants), never by downstream
schema checks.

### 3. Independent novelty skeptic — A3 design, B2 build, **V-B2 pass_with_caveats**
`workers/novelty_skeptic.py` (415 L — **justified**: 107 L are docstrings/prompt;
executable core clones `novelty_classify.py`) + `tests/test_novelty_skeptic.py`
(18 tests). **Plumbing PROVEN independently:** reads neighbors + Gemma's verdict
via `iteration_cache` (reference-passing), one `call_sync(backend=…)`, validates
against the closed 4-enum and **never coerces** a near-miss (rule 4), stamps
`skeptic_backend`/`skeptic_model_version` on every path so a consumer can tell an
independent witness from a gemma-persona self-check, spine + run_state untouched.
18/18 green under `.venv-chroma`. Justification is **D-035** (Co-Scientist:
different-model critic is load-bearing), not just D-033.

**Load-bearing caveat — cross-model independence is currently UNOBTAINABLE:**
- **qwen route is down.** `vllm-qwen` (:8001), though dispatched-to correctly,
  returns **empty content on every call** (`finish_reason=length`, content=None,
  output_tokens pinned at the cap). The MTP/reasoning model burns the whole
  `max_tokens=512` on reasoning tokens, emitting nothing on the content channel.
  3/3 rows → honest `unclear` fallback. **This is a qwen serving/config + worker
  `max_tokens` problem, not a worker-logic bug** — but it means the 37-row
  independent back-test can't yield a real agreement signal via qwen until fixed.
  (This is the long-standing "Qwen structured-skeptic reliability" carryover.)
- **gemma-persona** route works (proves plumbing) but is the **same weights** —
  NOT independent; CI/plumbing only, never an independent witness.
- **claude** (anthropic, off-box, zero GB10 contention → safest on this
  hardware) is the recommended standing route, but **deferred to the human**:
  API credits funded? + API-credits-vs-max-plan-subscription **ToS** question +
  **the D-014 tension** (Claude in the apparatus runtime — does D-035's anthropic
  backend already carve the exception, or does D-014 block it?).
- Minor: `skeptic_model_version` mislabels the vLLM-served qwen as
  `ollama/…` (reuses OllamaBackend for the :8001 endpoint) — provenance-hygiene
  fix for the human, not a blocker.

Back-test set: **37 of 47** loop_memory rows back-testable (novel 19 / redisc 9 /
nonsense 7 / unclear 2 — novel-dominated, small-N → *directional, not proof*).
Harness must re-wrap FLAT memory rows into the `.result.*` envelope.

### 4. Memory guard + exp008 — A4 spec, B1 build, **V-B1 pass_with_caveats**
`experiments/exp008_qat_eval/preflight_mem.sh` independently confirmed to
**PREVENT the arm-C OOM**: replaying ~60 GiB-free with a 48/56 GiB need → REFUSE
(exit 1) every time (the freeze needed ~78 GiB free to admit). Reads
`/proc/meminfo` **MemAvailable** (never nvidia-smi = N/A on this unified box;
never MemFree — live gap 10.7 vs 32 GiB), 30 GiB OS margin (generous,
freeze-justified), swap-blind (swapping IS the failure mode), fails closed
(exit 2) on unreadable mem/bad input, floors conservatively. **Not yet wired**
into serve_qat.sh (B1's spawn-contract correctly scoped that as the integrator's
spine edit). exp008 → **SHELVE** (draft below).

---

## DECISION DRAFTS (human ratifies into DECISIONS.md — I do not edit it)

**D-039 disposition — SHELVE the exp008 live run.** No interpretable/deployable
result is reachable here: (1) no vLLM-native W4A16 QAT for 26B-A4B → no
production swap even on a win (NVFP4 pin stands); (2) arm C froze the box and the
new guard now mechanically refuses it here; (3) arm B carries an
uninterpretable llama.cpp-vs-vLLM engine confound (arm C was the only
disambiguator); (4) N~10 resolution is directional only. **Revisit** only on a
dedicated-GPU box, or with production paused behind the guard, or if a
vLLM-native W4A16 path ships. Harness preserved as-is; NVFP4 pin untouched.

**D-041 (draft) — β is GATED on an independent (off-box) novelty skeptic +
the memory guard.** Extends D-035 (Co-Scientist). Single-model novelty scoring
is mitigated only by human sampling; β removes the human, so an independent
skeptic is a **hard β prerequisite**, not a nicety. Recommended standing route:
**claude (off-box, zero contention)** primary + **qwen (on-box, behind the
memory guard)** local fallback; **gemma-persona excluded** (not independent).
Open for the human: the claude-route ToS + **D-014 reconciliation**.

**D-031 / D-008 / LOOP_V0.md amendment (draft) — "mechanical NemoClawRuntime
swap" is falsified.** Retire NemoClawRuntime-as-dispatch-subclass as the β path.
β = OpenClaw agent bundle + host-side tool plane around the unchanged host spine.
PyRuntime stays the host default. Also correct LOOP_V0.md line 229 + START_HERE.md.

---

## Integrator TODO (primary session, behind the verify gate; nothing committed yet)
1. Wire `preflight_mem.sh` into `serve_qat.sh` cmd_up before each `docker run`
   (`source …/preflight_mem.sh; case arm in B) need=16;; C) need=56;; esac;
   preflight_mem_guard "$need" || exit 1`). Confirm arm-C gate figure: **56** (the
   util-0.46 reserved budget) vs 48 (bare weights) — recommend 56.
2. Raise `novelty_skeptic.py` `max_tokens` (512 → e.g. 2048+) so a reasoning
   model leaves room for content — but treat the **qwen empty-content serving
   issue as the primary fix** (chat-template / reasoning-parser / max-model-len);
   re-probe `:8001` after.
3. Fix `vllm-qwen` provenance label (`ollama/…` → vLLM) — small wrapper hygiene.
4. Verify gate: `code-review` + full suite green (744 + 18 skeptic + guard tests)
   run under **`.venv-chroma/bin/python`** + one real `env -u MOCK_LLM` smoke;
   only then commit (single merge authority). Append the B2 run-log row.
5. Deferred (NOT this session): wire skeptic as a 6th LOOP_V0 step + iteration
   schema + UI chip; the 37-row claude+qwen back-test (after qwen/credits fixed).

## Ops / queued for human
- **qwen restarted** (right model: `qwen3.6-27b-nvfp4-mtp` NVFP4-MTP on :8001, UI
  capturing it) but **emits empty content** — needs the serving fix above before
  it can be the independent skeptic.
- **NemoClaw gateway `:18789` down / sandbox unhealthy is REAL** (not cosmetic) —
  blocks any in-sandbox β smoke; needs human eyes before I touch NemoClaw.
- **Rotate `SEMANTIC_SCHOLAR_API_KEY`** (human supplies the key).
- Memory is tight (qwen+gemma → ~29 GiB free / 31 available, 6 GiB swap) — fine
  for steady state; no third large model without the guard.

## Next cycle — PLANNED + ATTESTED (2026-06-09 planning session)

Full plan staged at `~/.claude/plans/fuzzy-singing-stream.md`. Focus (human-chosen):
**Nara's autonomous morning loop + a NemoClaw seam-probe + the agent-governance harness**
(all in one combined cycle; ordered must-have-first since it's >2h).

**Track 1 (core) — autonomous morning loop on the host.** Un-blind the coordinator
planner: add `topic_suggestions` to `coordinator.assess_state` (newest arXiv paper from
`papers_recent` via `chroma_query`, fallback loop-memory gap) + persist `bubble_up` to
`memory/coordinator_bubbles.jsonl`; then run a **single human-triggered**
`env -u MOCK_LLM python -m orchestrator.coordinator --execute --once` (in-bounds — not
continuous) that assesses → auto-picks a topic → runs ONE real LOOP_V0 iteration → bubbles
it, surfaced in the UI. (Coordinator has NEVER run `--execute`; planner was topic-blind.)
Qwen-dependent promotion/skeptic layer stays degraded until the Qwen empty-content fix.

**Track 2 — NemoClaw seam-probe (time-boxed).** Live re-probe gateway `:18789` (settle the
"up" vs de-risk "down" conflict); script `nemoclaw … policy-add` to allow a host endpoint +
verify a sandbox→host curl — clears the de-risk #1 blocker. Bundle is next cycle. Stop+log
if any step is sudo/human-gated.

**Track 3 — governance harness, ALL 6 ITEMS HUMAN-ATTESTED this session:**
- **P0-B** ✅ run-log `agent`(req)/`skill_used`(opt) on inviolate rule 6; thread `agent`
  through `runtime.PyRuntime.log_event`. → **D-043** (links commit `2690b5b`/FR-003), reconcile framework proposal **P-008**.
- **P0-A** ✅ spawn ledger `run_state/spawn.jsonl` as **DISCIPLINE** (hand-appended at
  launch/fan-in), NOT fail-to-launch; tighten CLAUDE.md rule 3.
- **P1** ✅ merge-gate (CLAUDE.md:64): name the **framework `code-review` skill** over the
  local commit range, not the `/code-review` GitHub-PR builtin (no-op → falsely clean).
- **P1** ✅ `narrate`-required at workflow synthesize (+ conditional `propose`) + `brain-recall`
  in the build-agent spawn-contract `skill_subset` — **dev-time only; runtime never reads the
  brain**; sequence AFTER P0-A. (Brain ops target the FRAMEWORK brain `agent_system/memory/brain/`.)
- **P2** ✅ **D-042** referenced-only record (orchestrate/experiment/repro-check/plan-research
  = governed Dynamic-Workflow + verify-gate substitution).

**Adversarial review of the handoff (done):** all load-bearing claims CONFIRMED vs disk.
Corrections: anonymous workflow rows are lines 886–904 (not –981); the **consumer has no
brain** (it's framework-only; skills symlinked at `.agents/skills/`); **D-041 collided** —
resolved as **D-041 = β-skeptic-gate (de-risk), D-042 = referenced-only, D-043 = run-log bump**.

**Still pending human ratification (separate from the above attestations):** the de-risk
decision drafts — **D-039 disposition = exp008 SHELVE**, **D-041 = β gated on independent
skeptic**, and the **D-031/D-008 "mechanical swap" amendment**.

## — EXECUTED as Dynamic Workflow `wf_641dcae3-bdd` (2026-06-09)

Ran the plan as a 4-limb build/draft/probe workflow + serial integration. **Landed
(verify-gated: framework code-review skill + suite 804 passed/1 xpassed excl. the
pre-existing `test_dispatch_coding_agent.py` debt + a real `--execute` smoke):**
- **Autonomous morning loop WORKS.** `coordinator --execute --once` (single human-triggered,
  in-bounds) auto-picked the newest `papers_recent` topic, the planner (un-blinded by the new
  `topic_suggestions` in `assess_state`) proposed `run_loop_iteration`, and a real LOOP_V0
  iteration ran end-to-end → **iter-2026-06-09-001** (`seed.source="coordinator"`, novel/survives,
  journal `064.md`). New: `orchestrator/morning_topic.py` (+12 tests); coordinator
  `topic_suggestions` + execute-only `_persist_bubble_up` (→ `memory/coordinator_bubbles.jsonl`).
- **Governance harness landed + DEMONSTRATED on this run.** Inviolate rule 6 gains
  `agent`/`skill_used` (threaded via a `_current_agent` ContextVar in `runtime.py`, zero edits to
  the 13 nara.py callers — proven: the iteration's rows carry `agent="coordinator"`); CLAUDE.md
  rules 3 (spawn-ledger-as-discipline + brain-recall), 4 (framework code-review disambiguation),
  5 (narrate-required); **DECISIONS D-042/D-043** + a **D-041 reserved** placeholder.
  `run_state/spawn.jsonl` shows the full `{spawned}→{completed}` lifecycle for all 5 limbs (P0-A);
  per-limb `agent`-attributed run-log rows (P0-B). +`tests/test_runlog_agent.py` (xfail→xpass).
- **Live-surfaced bug fixed:** the coordinator dispatches `source="coordinator"` but the
  `iteration_record` enum lacked it (invisible until the first real `--execute`) → added to
  `schema/iteration_record.schema.json`.

**Rolled over (handoffs):**
- **Picker relevance (research-quality, loud flag):** the newest-paper heuristic picked an
  **off-domain** topic ("FASE: …Code Quality") and the loop self-scored it **novel/survives** —
  with the Qwen skeptic degraded, that's a low-confidence verdict. Domain-filter `papers_recent`
  (cs.GT/econ.TH) and/or gate the iteration on topical relevance before trusting autonomous verdicts.
- **NemoClaw (Limb D, read-only):** gateway `:18789` is **dead** (no listener/no published ports);
  `policy-add` mutates+rebuilds the sandbox — both **human-gated**. Seam *proven* though:
  sandbox→host:8000 returns **200 via hostname** (raw IP 403; SSRF keys on hostname). Drafted
  policy YAML captured in the workflow result for the human's apply.
- **Deferred (conscious):** the nara.py who-executed split (iteration body → `nara`) — naive
  set/reset has a ContextVar-nesting footgun; do it via save/restore next. `assess_state` now
  loads chroma per call (perf; consider laziness). Qwen empty-content fix still pending.

## NEXT SESSION — plan (set 2026-06-09)

Three threads. UI render work is the UI session's; everything else is the primary session.

### A. Critic / retrieval honesty (the loud finding — do this FIRST, it's a research-integrity bug)
The 2026-06-09 autonomous iteration (`iter-2026-06-09-001`) exposed a real failure mode,
verified on disk: an **off-domain** topic (FASE / code quality) retrieved **9 game-theory
foundational books + 1 arXiv** (all topically irrelevant), and the loop scored it
**`novel/survives`** — novelty said "novel" *because* the game-theory corpus omits semantic
entropy; the critic said "survives" *because* that irrelevant corpus "contains no contradiction."
**Both verdicts are artifacts of irrelevant retrieval.** ml-intern **was invoked but returned
`papers_stored: 0`** (silently) — so the critic judged on local-only literature. Fixes:
1. **Topical-relevance signal + low-confidence gate (inviolate rule 4 applied to the loop):**
   compute hypothesis↔neighbor similarity; when retrieval is topically thin/irrelevant, novelty
   and critic MUST emit **low-confidence** — "no contradiction in an irrelevant corpus" is NOT
   "survives." This is the critic improvement.
2. **ml-intern 0-papers is not silent:** surface it as a degraded external-search signal; the
   critic must know it judged local-only. **Investigate WHY 0 papers** for the FASE query (S2
   query/threshold, or the `SEMANTIC_SCHOLAR_API_KEY` is expired/rate-limited — key is present,
   len 44, but pending rotation).
3. **Domain-filter the topic picker** (`papers_recent` → cs.GT/econ.TH) so the morning loop
   doesn't run on off-domain seeds at all. Connects to D-041 (β-skeptic-gate) + the deep-research
   verdict (running now: `wf_ae343aa7-073` — does external lit show the FASE claim is a
   **rediscovery**, not novel? fold its cited finding into the critic design).

### B. NemoClaw — minimal "Nara on NemoClaw" slice (β)
Gated on human ops (below). Smallest demonstrable: a sandbox OpenClaw agent that calls ONE
host-side tool through the gateway and runs an assess→plan→noop cycle.
1. **(Human, BLOCKING)** recover gateway `:18789` (dead — `nemoclaw` restart / re-onboard, maybe
   sudo); authorize the egress `policy-add` (rebuilds the sandbox). Limb D's drafted policy YAML
   is in `wf_641dcae3-bdd`'s result.
2. **(Primary) host tool plane:** a minimal host FastAPI/MCP service exposing 1–2 existing workers
   (`get_apparatus_state`, `retrieve_literature`) as tools, bound to a host hostname:port reachable
   via the proxy (Limb D proved sandbox→host:8000 = 200 via hostname).
3. **(Primary) OpenClaw agent bundle:** Nara system prompt + tool manifest → host tool plane via
   the gateway, using the **local inference provider** (NOT the default `claude-cli` — D-013/D-014).
   Smoke: agent lists + calls one tool end-to-end.

### C. UI autonomy observability — EMIT half (primary-session prerequisites)
Full spec: [`docs/ui_autonomy_observability_plan.md`](../../docs/ui_autonomy_observability_plan.md).
The coordinator loop runs "dark." Primary lands the data emission; a concurrent UI session renders it.
- `active_run` `kind="coordinator"` (not `ad_hoc`) + per-phase narration (assess/plan/validate/dispatch,
  the chosen topic + why); `run_state/coordinator_cycles.jsonl` (the cycle arc: plan, per-action
  outcome incl. errors, iteration, findings, bubbles); **failed dispatches never silent** (a row,
  not absence); qwen-degraded + ml-intern-0-papers health signals; a `retrieval.relevance` signal.

### What I need from you (human)
- **NemoClaw ops:** recover the `:18789` gateway; authorize/run the egress `policy-add` (sandbox
  rebuild); sudo for `nemoclaw onboard` if needed.
- **Rotate `SEMANTIC_SCHOLAR_API_KEY`** — likely why ml-intern stored 0 papers (external lit blind).
- **Ratify the pending decisions:** D-039 (exp008 SHELVE), D-041 (β-skeptic-gate), D-031/D-008
  ("mechanical swap" amendment) — and confirm D-040→D-042 numbering with the D-041 reservation.
- **Claude-skeptic:** confirm API credits + the API-vs-subscription ToS / D-014 question (for the
  independent skeptic the critic-honesty fix wants).
- **Start a concurrent UI session** to implement the RENDER half once the EMIT half lands
  (`env -u MOCK_LLM claude --worktree ui-session`).
