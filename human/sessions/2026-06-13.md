# Session — 2026-06-13: clear the two honest-pending real runs from 06-10

> Primary session. Human directive: "go ahead with the two pending runs — I am
> still thinking about what I want to accomplish this session. We will go into
> plan mode at the end of this." So this session is scoped to the two PENDING
> real runs surfaced at resume; the session focus proper is decided in the
> plan-mode handoff that follows.

## Resume-state reconciliation (start of session)
- HEAD `1c5ddaf`, tree clean (only untracked: `logs/coordinator-soak.log` from
  the 06-10 S3 soak). No `human_gates_pending`; dev-session deferred queue empty.
- Services: vLLM `vllm-gemma4` :8000 + `vllm-qwen` :8001 both up (weeks old,
  MARLIN pin confirmed); tool plane :8077 was running **pre-D-051 code** (health
  listed only the old 2 tools). The earlier "GPU idle" reading was an
  `nvidia-smi` query artifact, not a down server.
- Two honest-PENDING items from 06-10 (both needed a GPU-idle window): the
  D-051 submit+poll real smoke, and the D-050 residual-1 revision cycle + the
  single allowed re-run.

## What ran

### 1. D-051 submit+poll smoke — host-side clauses PASS
Restarted the tool plane on current code (`/health` now lists all four tools).
Drove `submit_loop_iteration` → **9 ms** (<2 s ✓); `poll_run` **converged**
running→finished over ~2m21s with honest intermediate state (registry/heartbeat,
`steps[]` board walking the chain, a dynamic `redteam→failed` chip, `stale:false`);
poll's finished envelope **equals the `loop_memory.jsonl` record**
(iter-2026-06-13-001: novel/survives/lowconf=false/journal 075) ✓. Run-log carries
`nemoclaw_agent` accepted/finished events bracketing the 40-row chain. The 4th
clause — sandbox MCP drive — was NOT run; the runbook scopes it as the
out-of-bounds stretch path (sandbox egress / gRPC). Verdict appended to D-051;
the seam is proven at the host boundary, sandbox drive is the one open item.

### 2. D-050 residual-1 revision cycle + re-run — rule FAILED, gates STAY DARK
The one rule-7 revision allowance was spent. Two fixes; defect (b) was a
MISDIAGNOSIS corrected by instrumentation:
- **(a)** broadened the topicality skeptic ON-side prompt (canonical/behavioral/
  evolutionary GT = ON regardless of AI framing; OFF discriminator kept sharp).
- **(b) is NOT a critic wiring bug.** Instrumentation proved `critic_loop_v0`
  carries the restating citation correctly; the real defect was a **battery
  reporting bug** — `CaseScore`/`score_case`/`per_case` never copied
  `contradicting_paper_id` through, so the C2 criterion read a structurally
  always-`None` field. Fixed (3-line passthrough + regression test). The 06-10
  "0/4 with citation" was partly that measurement artifact.

Full suite **1162 green** under `MOCK_LLM=1`. The single pre-registered re-run
(`battery_20260613T043130Z`, real, 22 cases, ~35 min): **C1 PASS** (fase_off_01
R0b-gated, recall 8/8, 0 ungated off-domain); **C2 FAIL** (restated-with-citation
**2/4** now *visible* — redisc_01 + canary_02 carry cites — but redisc_03 stayed
survives); **C3 FAIL** (over-gating **relocated** to novel_on_01 + novel_on_03;
on-domain low-conf set 3→5); **C4 FAIL** (acc **0.6591** < 0.70; base 0.6364,
06-10 was 0.6818). **Gates STAY DARK; residuals 1+2 close as NOT MET.**

**Structural finding:** the independent REFUTE-framed topicality skeptic
over-condemns on-domain novelty in BOTH runs — prompt text moves *which* cases
it condemns, not *whether*. That is D-045 residual-5 confirmed structural;
reopening requires a NEW decision (the real question: is an adversarial
topicality skeptic the right instrument at all?).

## Disposition / state
- Env gates `NARA_TOPICALITY_SKEPTIC` / `NARA_RESTATE_SKEPTIC` stay OFF by
  default (verified: no cron/sentinel/shell sets them) → runtime byte-identical
  to the no-seam apparatus.
- **Battery reporting fix STAYS live** (measurement correctness, test-pinned).
- Topicality prompt change stays in the now-dark seam (better, inert while dark;
  NOT blessed for activation).
- Working tree (uncommitted): `DECISIONS.md` (D-051 smoke + D-050 re-run
  verdicts), `battery.py` (+passthrough), `topicality_skeptic.py` (prompt),
  `tests/test_lit_falsification_battery.py` (+regression); new run artifacts
  `runs/battery_20260613T043130Z.{json,md}`; `run_state/tool_plane_submits/`.
  **Not yet committed** — awaiting the framework code-review gate + the human's
  call (session continues into plan mode).

## Next (for plan mode)
- Commit decision: run the framework `code-review` gate on the 4-file diff +
  commit the verdicts/fixes (these are append-only history worth preserving).
- D-051: sandbox MCP drive at a sandbox-coordinated window (the last open clause).
- Lit-pipe: the over-gating question is now a NEW-decision item (adversarial
  topicality skeptic may be the wrong instrument). S3's promotion-bar near-miss
  calibration also still open.
- Untouched from 06-10: D-049 ratification (human's call); pre-resolution
  Polymarket snapshot workstream; UI session executes its handoff.
