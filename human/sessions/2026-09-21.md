# 2026-09-21 — Flash outages, 10 GiB reserve, test isolation

## Scope

Operations and maintenance only; no research, benchmark or study work. The owner
asked to bring Flash back up, lower its host reserve to 10 GiB for SSH use, find
why the model went down, bring the Nara daemon and runner back online, and do
cleanup except for the unmerged Codex worktree (leave a note).

## What happened

| UTC | Event |
|---|---|
| 02:59 | Monitor stopped Flash: `physical_reserve_breached` (a VS Code Remote-SSH login took MemAvailable from 23.5 to 19.8 GiB). |
| 04:09 | Owner-requested latch clear and one start on v6; ready 04:21. |
| 05:08 | Owner decision: reserve 20 to 10 GiB. Config and validator changed while v6 ran; the long-running Nara daemon held the old validator and refused every pass until 14:10. |
| 14:10 | v6 stopped cleanly, handed off to helper v7 (10 GiB monitor, 8 GiB guard). |
| 14:22 | Guard stopped the v7 load on four kernel `NV_ERR_NO_MEMORY` lines. Investigation: a false positive; the engine was ready at 14:23:13 when it was killed. |
| 19:04 | Owner-requested latch clear and one start with the new prelaunch page-cache gate; ready 19:17, no kernel lines. Nara daemon active 19:17. |

Evidence: `a_bgt_rsi_v2_artifacts/2026-09-18/flash-personal-recovery/`
`reserve-breach-20260921/`, `reserve-10gib-cutover-20260921/`,
`v7-false-positive-restart-20260921/` (errata files correct two earlier claims).

## Code and docs changed (uncommitted)

- `orchestrator/flash_resident.py`: v7 pin; `check_ready` at 10 GiB; state records
  `bundle_sha256` and `host_reserve_gib`; cross-helper start/cleanup refuses with
  "helper handoff required"; guard exit code recorded; prelaunch page-cache
  eviction (unprivileged `posix_fadvise`) and a `MemFree` >= 100 GiB gate with a
  bounded 35-minute wait; it refuses without arming the latch. (The 19:04 start
  used an earlier 4 GiB `Cached` gate that review showed could strand Flash.)
- `agent_wrapper/deployment.py`, `config/model_deployment.json`: reserve 10.
- `orchestrator/research_campaign.py`: the canonical root honors
  `DEFAULT_CLOSURE_DIR` (no production behavior change) so tests can isolate it.
- `tests/conftest.py`: tests no longer read live campaign pointer, closures or
  research focus, and no longer write the live Nara log. The tracked suite went
  from 70 failures to 5563 passed, 0 failed in the canonical checkout (one
  more test depended on the UTC clock via daily budget pacing; now pinned).
- Tests for the pin, handoff, gate and study fail-closed contract; docs:
  `docs/FLASH_RESIDENT.md`, `docs/MODEL_TOPOLOGY_POLICY.md`, `ARCHITECTURE.md`.
- Frozen helper `sglang_session_s3_v7.py` (outside Git; v6 kept for rollback).
- Pruned 54 git worktree records whose `/tmp` directories no longer exist.

## Owner decisions pending

1. **Guard policy.** Helper v7 stops Flash on any kernel `NV_ERR_NO_MEMORY` line.
   The `mem_desc.c:1359` form is a logged, retried big-page attempt (203 lines on
   two boots, no `system_mem.c` final failure, no engine ever failed), and it
   stopped one of nine SGLang draft-stage loads. Making it non-fatal before
   readiness is a host-safety trade-off: public GB10 reports show the same line
   before compaction livelocks and hard resets, and this host was under memory
   pressure at 14:21. The auto-mode classifier blocked Claude from making it, and
   an adversarial verifier argued against it. It is the owner's call.
2. **Payoff studies** preregistered the 20 GiB deployment and now fail closed on
   new dispatches until amended or re-registered.
3. **Oracle daily planning** keeps its own 20 GiB MemAvailable floor. Its 15:00
   slot failed with `ModelUnavailable` during the outage; a manual rerun is refused
   outside its schedule window. Next run 01:00 UTC.
4. **Commit** the changes above; the deployed v7 pin exists only in the working tree.

## Note for the Codex flash-reserve-recovery worktree

`a_bgt_rsi_worktrees/flash-reserve-recovery-20260921` (uncommitted, 03:13-03:48Z)
adds automatic clean-reserve-stop recovery to `orchestrator/flash_resident.py`.
It predates the 10 GiB decision: it pins v6, hard-codes 20/16 GiB floors and a
16.0 launch-record check, and keeps `check_ready` at 20. Rebase it onto the v7
pin and `HOST_RESERVE_GIB`, keep the `bundle_sha256` handoff guard and the
prelaunch gate, and derive floors instead of literals before merging. Claude did
not modify it. Also tracked in `run_state/maintenance_todos.json`.

## Runner status

Flash ready; Nara daemon active with a passing preflight; the hourly coordinator
passes its Flash gate from 20:00. Both currently stop at the 2026-09-20 research
focus hold (`focus_pending`), a deliberate research gate that this session did
not change.
