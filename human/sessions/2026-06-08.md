# Session — 2026-06-08

## Strategic convergence: the apparatus becomes an always-on multi-agent system

Rubberbanded the multi-agent direction. The vision (user): **Nara = a secure, always-on OpenClaw agent
in NemoClaw** that, on a schedule, checks whole-system health + in-flight experiments, generates
hypotheses, plans the day, and spawns agents (coding/critic/review) — bubbling up applied/trade ideas.
Sub-agents start as LLM personas, graduate to permission-scoped NemoClaw sub-agents (Nara-granted policy
subsets). The instrumentation shipped 2026-06-06 (active_run/worker_activity/surfaced_findings) is Nara's
"eyes"; NemoClaw policy presets are the constrained-autonomy mechanism; messaging channels are the bubble-up.

**Build path chosen: α (brain on host) → β (always-on in NemoClaw) → γ (permission-scoped sub-agents).**

**Autonomy contract (for D-040, formalized at β):** unattended Nara MAY plan, run experiments/loop
iterations, spawn critic/review/analysis agents, build new tools, improve the system within constraints,
do LIMITED commit/push (tests-green + verify gate; default to a `nara/auto/*` branch, main merges human),
and bubble up proposals incl. trade IDEAS. MUST NOT: execute live trades (human-only), widen its own/
sub-agents' permissions beyond a granted preset, bypass the verify gate, or touch inviolate pins/guardrails.
Essentially Nara inherits the human-session operating discipline. The continuous-orchestrator guardrail
(CLAUDE.md out-of-scope) gets amended at β, NOT now.

## NemoClaw installed (human) + a load-bearing probe finding
NemoClaw v0.0.55 installed; `nara-sandbox` onboarded (gemma-4-26b-a4b, vllm-local, openshell 0.0.44).
**Probe finding that reframed S1A:** the sandbox is genuinely isolated — our repo/venv aren't in it,
Python 3.13 (vs host 3.12), and the model isn't reachable by naive curl (only via OpenClaw's provider
routing). So D-031's "NemoClawRuntime is a mechanical swap" is WRONG: NemoClaw runs OpenClaw *agents*, not
our in-process Python workers. This is exactly the user's vision (Nara-as-agent), not a worker-runtime swap.
So β = package Nara as the OpenClaw agent (not "exec our workers in the sandbox"). D-031/D-008 to be updated.

## Slice α — coordinator brain (Dynamic Workflow `wf_47ff851f-5d1`, 6 agents) — DONE
Built ALONGSIDE the proven scripted nara (opt-in, default UNCHANGED), host, NOT continuous, NOT in NemoClaw,
default dry-run — so it lands cleanly under current rules (β's amendment not needed yet).
- `orchestrator/coordinator_actions.py` — the CONSTRAINED ACTION MENU (run_loop_iteration/promote_findings/
  bubble_up/noop) + `validate_plan` (off-menu/bad-args/over-budget/over-MAX → rejected, never raises). The guardrail core.
- `orchestrator/coordinator.py` — assess_state (reads the instrumentation) → plan (one call_sync over the menu) →
  validate → bounded re-plan (≤2) → dispatch (only with --execute; default dry-run); adopts active_run/run_id; CLI.
- Verify 3/3 PASS: guardrail (off-menu → no_valid_plan, no handler runs) confirmed independently; offline 26 tests;
  **real plan-only smoke on live gemma** — over the REAL system state the planner produced a valid in-budget plan
  `promote_findings(max_candidates=3)` and executed nothing. The planner-without-mis-sequencing is proven.
- nara.py UNCHANGED; suite 718 → **744**.

Deviation (documented): coordinator's active_run uses kind="ad_hoc" (the schema enum lacks "coordinator");
add a dedicated kind when it graduates from alpha (β).

## Queued on the human / next sessions
- exp008 QAT live run (bring up the :8002 scratch container) → disposition D-039.
- β: package Nara as the always-on OpenClaw agent in nara-sandbox + the autonomy contract (D-040) + the
  continuous-orchestrator guardrail amendment.
- γ: permission-scoped NemoClaw sub-agents.
- Carryovers: Qwen structured-skeptic reliability; route experiment calls to logs/calls.jsonl; rotate SEMANTIC_SCHOLAR_API_KEY.

## End-of-session handoff (2026-06-08, late) — committed; taking planning offline

**Landed on main (all verify-gated: pre-commit review workflow + full suite + real smoke):**
- 4-track integration (`19d89d5`/`dd4ed92`): Qwen-skeptic reliability FIX (`max_tokens_per_turn`
  + bounded repair-retry; real smoke `qwen_failures` 6/6→**0/3**, adversary functional);
  experiment-log isolation made COMPLETE — in-chain workers included via `LOOP_V0_CALLS_LOG`
  set before the orchestrator import in all 6 harnesses (the first fix only caught Nara's turns);
  exp008 verdict plumbing (drivers emit analyze-readable metric rows) + guarded `--reference`
  pin path (scoped to `--arm pin`).
- D-040 ratified + reconciled (`bb3b65b`): malformed CLAUDE.md guardrail fixed; **effective at β**.
- exp008 harness hardening (`3b53380`,`02e7702`,`001eac5`): **errored calls no longer scored as 0**
  (rule 4 — a down arm now reads honest INSUFFICIENT, not a fake regression); arm-C startup fixes
  (gpu-mem cap, MM `max-num-batched-tokens`, util 0.46).
- Pre-commit review `wf_744e0f6d-f04` (26 agents): 15 confirmed findings, all fixed. Suite 692→**766**.

**exp008 live run (first attempt) — INCOMPLETE:**
- **pin baseline collected clean** (novelty agreement **0.8**); committed under `runs/` + `RESULTS.md`.
- **arm C could NOT run + FROZE THE BOX.** On the GB10's ~121 GiB **unified** memory, a 2nd ~48 GiB
  model (unquantized QAT) alongside production gemma (~48 GiB) starved the OS — SSH + UI tracking went
  down (~13:40–16:20). Kernel OOM-killed the container; box recovered (no reboot, 24-day uptime,
  production `:8000` intact). `analyze` verdict: **INSUFFICIENT** (qat never served).
- HF check: both QAT repos exist (arm C rev `641f1844…`, arm B gguf rev `dfc00409…`); `config.yaml`
  arm-C placeholders filled (revision + content_hash).
- `tool_call_adherence` is suspect even on healthy pin (0/12) — likely a harness-validity issue
  (no tool specs passed); interpret cautiously / rework before trusting that metric.

**OPEN DECISIONS for next session:**
1. **exp008 path:** arm B (~15 GiB gguf — fits safely, engine confound) **vs SHELVE D-039**
   ("QAT eval deferred — candidate won't fit alongside production on the GB10's unified memory;
   revisit on a dedicated-GPU box or with production paused"). arm C is NOT safe to retry here.
2. **serve_qat.sh pre-flight free-memory guard** (refuse launch below arm-need + OS margin) —
   proposed, not built. Prevents a repeat freeze.
3. **Recovery still pending:** `docker start vllm-qwen` (stopped during the run); the
   `openshell-nara-sandbox` container shows `Up (unhealthy)` — check; rotate `SEMANTIC_SCHOLAR_API_KEY`.
