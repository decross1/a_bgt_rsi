# 2026-09-02 — resume after a 14-day always-on gap; four-team evaluation

Primary session (claude-code-main, Fable 5.1, ultracode). Plan approved by
the owner in plan mode; this note is the committed copy of that plan
(CLAUDE.md "Plan-mode artifacts").

## Resume findings (morning)

- The 08-19 note's headline ("the critic is the blocker") was refuted the
  same night by the critic-cal battery (d1c4a6e): degeneracy guards pass,
  reproducibility indeterminate at n=10; what blocks is the override chain.
- `nara-daemon.service` had run since 08-18 06:41Z on pre-7dcc695 code, so
  every daemon budget-paced wake emitted a false RED `loop_stalled` for 14
  days (358/438 stall signals at daemon minutes; all 94 `loop_gated:budget`
  from the hourly cron). Restarted between wakes at 03:22:48Z; first wake
  03:53Z executed a cycle. Lesson: any merge touching `orchestrator/` must
  restart the daemon.
- Claude frontier reviewer dark 08-22T04:08Z .. 09-01 (305 exit-1 calls,
  empty stderr, stdout discarded by the wrapper). Recovered the moment this
  session refreshed the OAuth credentials (probe: PONG, 3.3 s).
- Funnel 20 -> 76 open clusters (73 L0, 3 L1) in 14 days; 6 kills. 52 of 98
  iterations ran on an off-field ML topic; all 8 open gate threads and the
  hourly bubble-ups are on it, every one novelty=unclear / critic=undecidable.
- Codex reviewer: 34% of calls timed out at 180 s last week.
- Owner decisions still open: D-077 disposition; planning-layer ratification
  (its "would become D-077" collides — D-077 is taken); `fa-43df1c54`;
  `NARA_CONSTRAINT_CONDITION` dark.

## Focus agreed: four review teams (Dynamic Workflows)

Owner choices: cleanup = apply, gated; research = positioning doc + DRAFT
decision entries; UI = work order + design mockups + screenshots.

| Team | Vehicle | Writes | Output (written by the primary) |
| --- | --- | --- | --- |
| 1 Research positioning | `rw-sweep` -> `rw-deepread` -> `rw-synthesis` | none | `docs/related_work_2026-09.md`, `docs/decisions_draft_2026-09.md` (DRAFT-D-078…), `notes/research/2026-09-02-related-work/` |
| 3 Adversarial systems | `sr-find` -> `sr-verify-synth` | none | `docs/systems_review_2026-09.md`, `notes/research/2026-09-02-systems-review/` |
| 4 UI/UX review | `ui-review-team` -> `ui-mockup-panel` | none (screenshots in the scratchpad only) | "## UI session work order" below + a design-canvas artifact |
| 2 Code quality / docs / dead code | `cqd-verify-docfix` -> `cqd-delete` | worktree branches only; the primary integrates on `integrate/cqd` | doc-fix + deletion commits, decision-grade list for the owner |

Order: Team 1 and Team 4-WF1 concurrently, then Team 3, then Team 4-WF2 +
canvas, then Team 2 last. Base = d1c4a6e.

### Contracts (summary; full lines in `run_state/spawn.jsonl`)

- Read-only teams: no file writes/moves/redirects/git ops; never write
  `run_state/`, `memory/`, `logs/`, `ui/`; no live model calls (nothing via
  `agent_wrapper.wrapper`, no HTTP to :8000/:8001, no `chromadb.PersistentClient`,
  pytest only `--collect-only`, GET-only to :8700); never read the 6.57 GB
  telemetry archive.
- Team 2 writers: `isolation:'worktree'`, one commit on the worktree branch,
  `tools/premerge_check.sh` must pass, never touch `journal/`, `human/**`,
  `ui/`, `run_state/`, the spine, pins, `CLAUDE.md`, `DECISIONS.md`,
  `LOOP_V*.md`, `cron/serve-models.sh`; no test deletion; no skip markers.
- Every agent returns a run-log row (`agent: workflow:<wf>/<role>`); the
  primary appends rows and the spawn `completed|escalated` lines.
- Gate ladder before any commit: premerge OK -> python suite at baseline
  (12 pre-existing failures, 2482 passed) -> dead links zero -> backend
  suite at baseline -> framework `code-review` skill -> ff-merge -> vitest +
  tsc identical -> one real `env -u MOCK_LLM` smoke.

### Pre-existing test failures at d1c4a6e (reported, not fixed)

12 failed / 2482 passed / 2496 collected: `tests/test_coordinator_cycle_log.py`
×4 (a live `frontier_vendor_down:claude` signal leaks into the test),
`test_critic_cal.py` ×4, `test_emit_join_contract.py` ×1,
`test_readjudication.py` ×3. The backend suite needs `ui/.venv/bin/python`
(psutil is absent from `.venv-chroma`).

## Landed

- Resume-state reconcile (see top): daemon restarted onto merged code at
  03:22:48Z (first wake 03:53Z executed a cycle); Claude reviewer probe
  green after the credential refresh; memory + run-log rows written.
- Plan approved and committed as this note; snapshot of `git status` +
  ledger mtimes taken before any workflow ran (scratchpad `snap/`).
- UI screenshots: 13 routes + 5 state/interaction frames via the cached
  Playwright chromium (no installs); sanity-checked two frames.
- Team 1 sweep (`wf_ba294def-08d`): 17 sweepers + 2 critics; **324
  deduplicated related-work hits** with fetched primary URLs, saved to
  `notes/research/2026-09-02-teams/rw_targets_raw.json`. XScientist
  (arXiv 2607.12301, Jul 2026) verified real; the Nature version of the
  co-scientist (May 2026), DeepMind's "Conjecture Machines" essay (Jul
  2026) and Kosmos's self-reported rabbit-hole failure mode all surfaced.
- Team 4 review (`wf_90f54f88-e43`): all 39 surface sweeps, 3 owner-flow
  traces, 26 comparable-product card sets, 4 color/docs agents, 57 refuters.
- Workflow scripts for every team (incl. the not-yet-launched deep-read,
  synthesis, systems finder/verifier) copied to
  `notes/research/2026-09-02-teams/scripts/`; progress INDEX there.

## Paused (owner's call at the 90% vendor-spend mark)

The vendor monthly spend limit hit mid-run (~05:20Z): 103/119 UI agents and
the sweep critic died. Both workflows were resumed from cache at 05:53Z
(UI at the plan's reduced configuration) and then stopped at ~06:05Z when
the owner reported 90% usage. Spawn ledger carries `budget_exceeded` lines
with resume pointers; `notes/research/2026-09-02-teams/INDEX.md` is the
per-team ledger. Not started: Team 1 deep-read + synthesis, Team 3, Team 4
mockups + canvas, Team 2 cleanup. Nothing in the repo was written by any
agent.

## Owner decisions waiting

- Whether to resume the four teams when credit allows (resume keys in the
  spawn ledger and `notes/research/2026-09-02-teams/INDEX.md`), and at what
  scale (full vs reduced configuration).
- Confirm "X-scientist" = XScientist (arXiv 2607.12301) before the deep-read.
- Carried from 08-19: D-077 disposition; planning-layer ratification (its
  "would become D-077" collides — renumber); `fa-43df1c54`;
  `NARA_CONSTRAINT_CONDITION` dark.
- From today's reconcile: the 12 pre-existing test failures at d1c4a6e
  (incl. a live-state leak into `tests/test_coordinator_cycle_log.py`); the
  codex screen's 34% timeout rate at 180 s; topic drift (52/98 iterations
  off-field) and the 163 hourly bubble-ups — both are design questions, not
  bugs.
