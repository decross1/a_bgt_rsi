# Session — 2026-06-05

## Focus

Two coupled goals: (1) amend the local operating contract so previous-generation
constraints stop rate-limiting newer shipped Claude Code capabilities —
specifically **Dynamic Workflows** (the `Workflow` primitive shipped 2026-05-28
with Opus 4.8); (2) use that capability to complete the **full Loop v1** (the
four v5 steps LOOP_V0 deliberately skipped: 1.5 meta-review, 2.5 red-team critic,
5 cross-tier replication, 8 human-gate feedback edge).

Plan file: `.claude/plans/elegant-bouncing-gem.md` (approved).

## What landed

### Part A — Framework amendment (D-037)

- **`CLAUDE.md` §"Operating model"** rewritten: the primary session MAY author and
  run Dynamic Workflows. Added a **"Dynamic Workflow discipline"** subsection — the
  inviolate rules made concurrency-safe (parallel limbs / serial spine; spawn-contract
  per build agent; single integrator + single human-authority commit gate; workflow
  run-logging).
- **D-037** logged: authorizes Dynamic Workflows, amends (not revokes) D-030. D-030
  reacted to pre-2026-05 manual multi-worktree matrices; the Workflow primitive is
  bounded (16/1000), observable (`/workflows`), resumable, context-isolated — it
  handles the failure modes D-030 guarded against. Flagged `correction:` — local
  constraints must not cap newer shipped features.

### Part B+C — Loop v1 built via one Dynamic Workflow

First build under D-037. Workflow `wf_0ecc5da4-eae`: **7 agents, ~9.5 min
wall-clock, 334k subagent tokens.** Shape: Build (4 parallel) → Integrate (1 serial)
→ Verify (2 parallel).

- **Build (parallel, all green under MOCK_LLM):**
  - `workers/redteam_critic.py` (Step 2.5) — copies the `critic_loop_v0` sub-agent
    template; status-fallback ladder (timeout/schema_mismatch → `proceed`, never blocks);
    13 tests.
  - `orchestrator/gate_cli.py` + `schema/loop_feedback.schema.json` (Step 8) —
    `append_feedback` + CLI; verdict enum checked twice + argparse reject; 6 tests.
  - `workers/meta_review.py` (Step 1.5) — tail-reads loop_memory, joins loop_feedback
    by `iteration_id`; 8 tests.
  - `experiments/replication_driver.py` (Step 5) — honestly labeled cross-MECHANISM
    stand-in (no semi-synthetic rung exists); 2 tests.
- **Integrate (serial, single spine editor):** `orchestrator/nara.py` (+149) and
  `schema/iteration_record.schema.json` (+33, four OPTIONAL blocks, none added to
  top-level `required`). meta-review pre-step (try/except → `loop_v0_fallback`);
  deterministic ≤2-retry red-team sub-loop (logs `loop_v0_redteam_retry`);
  `gate_status="pending"`; `cross_tier_comparison=` kwarg. `tool_registry.py`
  untouched (both new workers orchestrator-driven). 6 new integration tests.
- **Verify:** adversarial audit **PASS** (500/0, no inviolate-rule violation;
  independently caught the `replication_driver.py:163` kwarg seam). The "MOCK_LLM
  plumbing smoke" agent correctly returned **BLOCKED** — a load-bearing finding:
  `MOCK_LLM` in this repo stubs only embedders, not the brain, so a real chain run
  needs `env -u MOCK_LLM`. It verified plumbing in-process instead and wrote no real state.

### Post-workflow (primary session, the human-authority gate)

- Fixed `replication_driver.py:163` (`experiment_outcome=` → `cross_tier_comparison=`)
  — the one non-blocking seam both I and the auditor flagged.
- Corrected my own over-strict discipline-#1 wording: the ~100-line budget in inviolate
  rule 8 is the *wrapper* budget, not a per-worker cap (workers run 120–390 lines;
  `redteam_critic` at 277 is smaller than its `critic_loop_v0` template). No code churn.
- **Real end-to-end smoke (`env -u MOCK_LLM`, gemma-4-26b-a4b live on :8000):**
  - `iter-2026-06-05-001`: full chain; meta-review synthesized 8 rows → 4 bullets;
    red-team `proceed`/retries=0; novelty=rediscovery, critic=survives; `gate_status=pending`;
    schema-valid. Journal `055.md`.
  - Gated it `valid` via `gate_cli` → schema-valid `loop_feedback.jsonl` row. Negative
    control: out-of-enum `approved` REJECTED, nothing written (rule 4).
  - `iter-2026-06-05-002`: **red-team retry FIRED** (`fatal_flaw` → re-hypothesize →
    `proceed`, retries=1, `loop_v0_redteam_retry` logged). meta-review bullets now cite
    *"valid and surviving results"* — it read the human gate verdict. Journal `056.md`.
  - **The closed learning loop works end-to-end:** outcome → journal → Step-8 human
    gate → Step-1.5 meta-review → conditions next hypothesize.
- **Full suite: 500 passed / 0 failed.**

## Reconciliation note (not drift)

Two test-count baselines appear in the trail: my pre-session baseline was **465**;
the integrator reported **494**. Not a contradiction — the integrator ran its baseline
*after* the 4 build agents had dropped their 29 new component tests into the shared
tree. `465 (original) + 29 (component tests) + 6 (integration tests) = 500`. No tests
edited or coerced.

## Operating state at session end

- Committed: framework amendment (CLAUDE.md, DECISIONS.md D-037) + Loop v1 (4 new
  workers/CLIs, schema, spine wiring, 5 new test files).
- Runtime state (loop_memory, loop_feedback, journal/iterations/055-056) is gitignored
  and local — the first two real Loop v1 iterations live on disk.
- `vllm-gemma4` (:8000) live; `vllm-qwen` (:8001) still up (D-036 substrate, unused here).

## Next-session anchors

- **Real research iterations** through the full Loop v1 now that all four steps are wired
  (the smoke topics were apparatus tests, not deliberate research).
- **Step 5 → real cross-tier**: the replication driver is a cross-mechanism stand-in;
  a genuine semi-synthetic rung (combinatorial-auction rung 2) would give it a real
  second tier.
- **Carryover (still open from 2026-05-29):** Slice 2 ML-Intern fetcher
  (`workers/ml_intern.py` consuming `result.escalation`); `analyze.py` parse-failure
  verdict gate; delete/relocate the stale `tests/test_dispatch_coding_agent.py`.
- **UI:** surface the new `meta_review` / `redteam` / `gate_status` blocks + the
  red-team retry in the active-iteration panel (UI session).

## Second block — exp004 combinatorial-auction rung (2nd Dynamic Workflow)

Workflow `wf_2518c5da-2bc`: **14 agents, ~10 min wall-clock, 514k subagent tokens.**
Build (8 parallel) → Integrate (1 serial) → Verify (5: 3 adversarial correctness
voters + repro-check + real micro-smoke).

- **Built `experiments/exp004_combinatorial_auction/`**: `bundles.py` (2-item, 3-bundle
  model with synergy), `bidder.py` (neutral-prompt bundle bidder), `mechanisms/{vcg,
  first_price,sequential_second_price}.py`, `efficiency.py`, `analyze.py` (**parse-failure
  gated verdict — folds in the long-standing carryover #4**), `mechanism_designer.py`
  (semi-synthetic *seed* probe), `run.py`, `loop_bridge.py`, `notes.md` + 8 unit tests.
- **Step 5 real second leg**: `replication_driver.py` gained `_combinatorial_adapter` +
  `build_cross_rung_comparison` (rung-1 Vickrey single-item → rung-2 combinatorial VCG,
  shared strategyproofness claim). Honestly labeled cross-mechanism-FAMILY, **still
  synthetic tier** — the genuine semi-synthetic (mechanism-design) tier is the
  `mechanism_designer` seed, not yet a validated rung.
- **Verify — all 5 PASS** with independent recomputation: an auditor hand-recomputed the
  VCG Clarke-pivot payments (exact match); `optimal_welfare` verified true-max over 20k
  runs; the parse-failure gate verified to trip at >0.25 (all-zero-residual high-failure
  case → INVALID, not silent YES); deterministic layer byte-identical; real micro-smoke
  on live gemma ran clean.
- **Suite 500 → 570** (8 new test files, +70 tests, 0 failed). Throwaway n=2 micro-smoke
  results cleared (not a real run); a deliberate ≥50-trial run is the next step.

### Scaling finding (feedback on the "~30 min active" goal)

Both workflows finished in **~10 min wall-clock** despite the 2nd having 14 agents (vs 7)
and harder per-agent work (VCG pivots, optimal-welfare search) — and 514k vs 334k tokens.
**Parallelism raised *active/compute* time (~1.5×) but not *wall-clock*:** wall-clock is the
critical path = build(parallel) + integrate(serial) + verify(parallel), each ~3-4 min, and
adding more *parallel* agents overlaps rather than extends it. To reach ~30 min *wall-clock*
of useful work the levers are: (a) **long real per-agent jobs** (a real n≥200 experiment run
is ~15 min in one agent — the n=2 smoke took 9.5s), (b) **more sequential phases** (deeper
dependency chains), or (c) **loop-until-dry** iterative passes. More parallel builders ≠
longer wall-clock. SDLC held at this scale because it was *encoded* (frozen contract +
spawn-contracts + validate gate + repro-check + 3-vote adversarial verify), not assumed.

## Third block — the real exp004 experiment run (a long single job, not a swarm)

Per the scaling finding, the lever for ~30-min *wall-clock* of useful work is a long real
job, not more parallel agents. Ran the real experiment: **n=150 trials × 4 bidders = 600
live gemma calls, 0 errors, 899s (~15 min) wall-clock.**

### Findings (real, 150 trials)

| mechanism | truthful (eps=5) | mean efficiency | mean revenue | verdict |
|---|---|---|---|---|
| VCG | 0.965 | 0.999 | 63.66 | YES |
| first_price | 0.965 | 0.999 | 82.93 | YES |
| sequential 2nd-price | 0.965 | 0.977 | 61.14 | YES |

- **Unprimed LLMs bid truthfully on bundles — 96.5%** (1737/1800 per-bundle bids within
  eps=5), parse-failure 0%. Replicates the rung-1 Vickrey truthful-bidding result. Bidders
  articulate it unprompted ("bidding my true private values…").
- **Honest scope:** `truthful_fraction` is identical across mechanisms *because the neutral
  bidder produces the same bids regardless of mechanism* — it is one finding, not three. The
  mechanism *difference* is in **efficiency** (sequential 0.977 < 0.999: the **exposure
  problem** appears, as theory predicts) and **revenue** (first-price > VCG > sequential
  under truthful bids). Mechanism-dependent *shading* is a separate follow-up (the bidder
  would need to know each mechanism's rules).

### Step 5 cross-rung replication (real, first time)

`replication_driver.py --cross-rung`: rung-1 Vickrey (truthful 1.0, supports) vs rung-2
combinatorial VCG (truthful 0.965, supports) → **agreement=True**. Strategyproof truthful
bidding replicates across auction complexity (single-item → combinatorial), within the
synthetic tier (honest cross-mechanism-family label).

### Tier-2 → Tier-3 bridge (real)

`loop_bridge.py --live` → **`iter-2026-06-05-003`** (journal `057.md`): full Loop v1 chain,
`experiment_outcome` (exp004 VCG 0.965) bridged, meta-review synthesized 8 rows,
**red-team hit `fatal_flaw` and exhausted its 2-retry cap** (the deterministic ceiling
working as designed), novelty=novel, critic=survives, gate_status=pending, schema-valid.

Committed the real results (`results/{trials.jsonl,summary.md,summary.json}`). The bridged
Loop iteration + journal entry are gitignored runtime state.

## Fourth block — next-steps build (3rd Dynamic Workflow) + iter-003 gate

Gated **iter-2026-06-05-003 → valid** (with a note flagging the novelty was overstated as
'novel' when it's a rediscovery of VCG strategyproofness — corrective for meta-review).

Workflow `wf_c4a70caf-a81`: **6 agents, ~11 min**. Build (3 parallel) → Verify (3).

- **exp005 mechanism-aware bidding** (`experiments/exp005_mechanism_aware/`): bidder told each
  mechanism's payment rule (NO theory priming — verified by the auditor's grep), bids DIFFER
  per mechanism; `analyze.py` reports **mean SIGNED residual** (the shading signal) with the
  parse-failure gate. 12 tests.
- **exp006 semi-synthetic mechanism-DESIGN tier** (`experiments/exp006_mechanism_design/`): the
  genuine semi-synthetic rung — LLM designs allocation+payment rules, scored vs the VCG
  benchmark (efficiency, feasibility, matches-VCG); reuse-only of exp004; verdict not coercible
  (infeasible designs lower feasibility_rate, gated to INVALID). 6 tests + loop_bridge.
- **UI surfacing** (worktree-isolated, `ui/`-only — the UI session spawned by the workflow):
  meta_review/redteam/gate_status in the active+resolved panels + an Exp004Panel reading
  summary.json. Merged via `git merge --no-ff` at the gate.

### The verify phase caught a real blocker (adversarial review earning its keep)

All exp005/exp006 contamination/coercion checks passed and the live micro-smoke passed, BUT
the adversarial reviewer found a **suite collection error (exit 2)**: `test_exp004_vcg.py` and
`test_exp004_efficiency.py` injected a `bundles` stub into `sys.modules` (a build-time scaffold
from before `bundles.py` existed) with no teardown; once `test_exp006_design.py` imported the
real `draw_valuation`, it hit the stale stub. **Fix (primary, at the gate):** pop the stub
after the module-under-test import in both files (real `bundles.py` then loads for everyone),
and robustify one tie-break-fragile VCG assertion (`allocation[0]==(0,)` → `assertIn(0, …)`;
the load-bearing pivot facts pay=6/revenue=6 hold under either equal-welfare tie). Suite **570
→ 588**, order-stable across repeated runs.

### Honest scope
exp005/exp006 are built + unit-tested + micro-smoked, NOT yet run at scale — the real headline
runs (mechanism-aware shading; designer efficiency) are the next long single jobs. The n=2
micro-smoke hinted exp005 shading is NOT yet visible (all mechanisms mildly negative residual),
which a real run will resolve.
