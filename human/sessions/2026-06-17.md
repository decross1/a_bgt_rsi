# Session — 2026-06-17: UI hygiene merge + cockpit design probe + tutor build (primary, autonomous run)

> Primary session, continued from 2026-06-15. The day opened with a UI
> housekeeping review and the human's design questions about the `/todo`
> cockpit's tutor card. Two read-only Dynamic Workflows (a housekeeping
> survey + a cockpit design probe) grounded the work; the human made two
> design decisions (→ D-054); this session writes the plan, then runs a
> ~5h Dynamic-Workflow build — P1–P3 the PRIMARY tutor engine, advancing
> to later primary seams if budget remains. A UI session is live (it
> shipped the hygiene pass merged below) and reads its work order in the
> subsection at the end.

## Done already this session

1. **UI hygiene merge** (`4a8f08c`, `--no-ff` from `worktree-ui-session`).
   The UI session's behavior-preserving tidy (`0f6e0f9`): 8 files
   comment/docstring/type cleanup + 1 real change (dead `repo_root` param
   removed from `human_todo.register` + its sole `app.py` call site). Gated
   by the primary per Dynamic-Workflow rule 4: framework code-review clean
   (1 non-blocking nit — `attest.py:168` stale cross-ref to
   `human_todo.register repo_root`); `tsc --noEmit` clean; vitest 1255/1255;
   pytest 493 (24 live-server tests excluded); real `env -u MOCK_LLM` smoke
   (app builds, `/api/human_todo` registers without `repo_root`). Scope:
   `ui/` + `ui_plan.md` only. Logged to `run_state/week1.run.jsonl`.
   **Push HELD at the human's instruction — bundles with this round's
   commits.**

2. **Two read-only Dynamic Workflows** grounded the design:
   - *UI housekeeping survey* (6 readers + critic): the `worktree-ui-session`
     branch is fully merged + clean; suites green; surfaced the
     **cockpit-seam reconciliation gap** — the stub `_SEAM_MODULES` mis-targets
     3/5 seams, and 2 outcomes (spawn_topic/abstain) are session-only, not
     one-shot CLIs.
   - *Cockpit design probe* (5 readers + synth; 4/5 returned structured —
     the forward-looking llm-seam dimension failed to emit StructuredOutput,
     but the two-voice probe covered that ground).

## Probe findings (durable — lifted out of the /tmp workflow output)

**Confidence metric.** A probability scalar in `[0,1]` — the human's
self-rated confidence in a free-text *prediction* typed BEFORE the verdict
form unlocks (the pre-verdict calibration capture, ARCH §6.5.4). UI: a
slider (`step 0.05`, default 0.50) with a 2-decimal readout. **STUBBED
end-to-end** — captured in component state, no durable writer (`gate_cli`
has no calibration subcommand; `/available.actions.calibration = False`; the
`calibration_entry` run-log writer + schema (Day 3.5) are unbuilt). Server
hard-rejects out-of-`[0,1]` with 422 (never coerced; rule 4).

**Finding review vs gate verdict.**
- *Gate verdict* = sign off the whole ITERATION (valid / needs_revision /
  invalid) via `gate_cli`, keyed `iteration_id`, → `loop_feedback.jsonl`.
  The Step-8 human gate.
- *Finding review* = adjudicate one surfaced CLAIM the iteration produced
  (validated / in_review / rejected) via `finding_session --set-status`,
  keyed `finding_id`, → `surfaced_findings.status.jsonl` (effective status =
  last audit row; the base file is never edited in place). validated/rejected
  ALSO dual-write a `loop_feedback` row against the finding's SOURCE
  iteration; in_review writes only the audit row and stays queued.
- Dashboard "N need you" = `gate_verdict + state_gate` only; `finding_review`
  is excluded (cockpit cargo, not the pipeline gate).

**Tutor card today.** A presentational STUB: no backend, no LLM. Renders a
label, a fence note, a "stub" banner, and a **verbatim echo of the finding
title** — that echo IS the "reiterates verbatim" the human flagged.

**D-044 mis-citation (correctness finding → D-054).** The tutor's fence note
cites "D-044 independence," but D-044 is actually the vllm-qwen
*novelty-skeptic* decision (interrogator ≠ authoring model). The REAL source
of the tutor verdict-fence is the 2026-06-14 note PART 2 + inviolate rule 4 +
D-053. Fix the label; reserve the D-044 cite for the interrogator-independence
context where it applies.

**Two-voice engine.** The two-voice interrogation is a real, open (not
fixed-2-turn), human-directed, LLM-grounded adversarial chat engine in
`orchestrator/finding_session.py` (defender = **Gemma** defends its own
finding; attacker = **Qwen** the independent skeptic; `MAX_TURNS=24`,
fail-open, stateless append-only replay). Its UI pane is a disabled stub. A
dynamic "probe-to-understand" tutor chat is a **small extension** of this
single-voice machinery, not net-new.

**Latent cockpit bug.** `Todo.tsx` passes `selected.id` into ALL six
resolution forms unconditionally — no gating by item `kind`. A `finding_id`
reaches `GateVerdictForm` (which wants an `iteration_id`). Gate the forms by
`selected.kind`.

## Decisions (the human's, this session) → D-054

1. **Verdict fence KEPT.** The tutor stays fenced from the verdict; the
   accept/deny STEER is routed to the **two-voice pane** (built to feed the
   verdict). Rationale: a tutor recommendation would bias the pre-verdict
   calibration signal §6.5.4 measures, and inviolate rule 4 reserves the
   verdict for the human. The tutor renders: overview · neutral **mechanical**
   "what each outcome writes" · neutral **unweighted** pros/cons · a live
   Qwen probing chat. **NO recommendation.**
2. **Tutor scope = static overview + live chat.** Replace the verbatim echo
   with a real finding-detail overview AND add a Qwen-backed no-verdict
   probing chat.

## The build — ~5h Dynamic-Workflow budget

Start `2026-06-17T15:48Z`; soft deadline `20:48Z`. P1–P3 are PRIMARY
(`orchestrator/`) and land first (the UI seam execs them). If P1–P3 finish
inside the budget, **advance to the next PRIMARY phase (P4+)** — NOT the UI
parts (those are the UI session's). Each phase: build via Dynamic Workflow,
then the primary integrator runs the verification gate (framework code-review
+ full suite green + one real `env -u MOCK_LLM` smoke) and commits; workflows
return reports, they do not commit (rule 4). `finding_session.py` is a shared
orchestrator file — its edits are made by the single serial integrator, not
parallel build agents (Dynamic-Workflow rule 2); parallel agents create
disjoint NEW files (tests/modules) only.

**P1 — tutor engine.** A `_build_tutor_seed` + a **no-verdict** tutor session
that reuses the single-voice open-loop (Qwen backend, turn-capped, fail-open).
NO `end_session` verdict path; writes NOTHING to `loop_feedback` /
`surfaced_findings.status`. Must not auto-fill/bias the calibration confidence.
The fence is enforced **by construction** (no disposition path exists), not by
convention.

**P2 — per-turn chat CLI seam.** A blessed one-shot turn CLI (stateless replay
makes per-turn exec clean) so `ui/backend` can exec it — serves BOTH the tutor
and the two-voice pane. This is the seam the cockpit has been missing.

**P3 — tests + smoke.** Tests under `MOCK_LLM=1`; one real `env -u MOCK_LLM`
Qwen turn as smoke.

**Next PRIMARY phases if budget remains (advance in order):**
- **P4 — `calibration_entry` writer** (ARCH §6.5.4): the durable run-log
  writer + schema so the confidence metric stops being stubbed. Unblocks the
  cockpit's calibration capture end-to-end.
- **P5 — cockpit-seam reconciliation (orchestrator side).** Per the
  housekeeping survey: build the PRIMARY-owned one-shot writers and correct the
  seam targets (the directive_signoff finding_id path; resolve the
  spawn_topic/abstain "session-only vs one-shot" question). Produce the
  corrected per-outcome wiring table as a `docs/` detail-doc the UI session
  then wires to.
- **P6 — `ml_intern` advisory re-attach** (~6 lines, D-052 known gap).

## UI session work order

> Co-authored by the primary per the operating contract. The UI session writes
> only `ui/` + `ui_plan.md`. **U1 can start NOW — it is pure data display and
> does not depend on the primary engine.** U2–U5 wait on the P2 CLI seam
> landing on `main` (the primary will note in this file when it lands).

- **U1 — `TutorPanel` rewrite (start now, no dependency).** Replace the
  verbatim title-echo with a real overview: the finding's claim, source
  iteration, blocker/why-deferred, evidence refs (joined **read-only** from
  `loop_memory.jsonl` / `surfaced_findings.jsonl` via a new read-only
  `ui/backend` GET — no writer). Add a neutral **mechanical outcome-effects**
  line ("accept → `valid` row vs iteration X + clears queue; deny →
  `invalid`; in_review → stays queued") and a neutral **unweighted** pros/cons
  enumeration. **Fix the D-044 mis-citation** in the fence note → cite the
  2026-06-14 note PART 2 + rule 4 + D-053 (keep D-044 only where it is actually
  about the interrogator). NO recommendation/steer in the tutor (D-054).
- **U2 — tutor chat pane (waits on P2).** Light up a live probing-chat pane
  (reuse the `TwoVoiceChatPane` stub pattern — the textarea + send UI already
  exist) → POST a new `ui/backend` tutor seam that execs the P2 CLI. Enforce
  the fence in the UI: no verdict props, no confidence auto-fill, session-local.
- **U3 — light up the two-voice pane (waits on P2).** Wire `ui/backend` → the
  P2 CLI; flip `actions.two_voice_chat`. This is now the home for accept/deny
  adversarial support (defender Gemma / attacker Qwen).
- **U4 — `todo_cockpit.py` seams.** Add the tutor + two-voice seam endpoints;
  flip the capability flags in `/available`.
- **U5 — gate resolution forms by `selected.kind`** (the latent
  wrong-keyed-form bug above).
- **Doc backfill.** As part of this round, backfill the thin tutor /
  two-voice / calibration component docs in `ui_plan.md` (today they are
  one-liners) so the cockpit's current state is legible.
- Fold in the carried hygiene nit: `attest.py:168` stale `repo_root`
  cross-reference (1 line).

When ready: print `UI READY TO MERGE`; the primary gates + merges.

## Carried-forward (not this session)

- D-049 ratification (human's call); D-051 sandbox MCP drive (needs a sandbox
  window); Polymarket pre-resolution snapshots.
- The `authorize_fix` resolution-form rewiring (`ui/backend/todo_cockpit.py`
  module target `orchestrator.todo_cli` → `orchestrator.authorize_fix` — UI
  session, separate from the tutor round).
- **Held push:** `4a8f08c` (hygiene merge) goes out bundled with this round's
  commits.

## Execution progress (autonomous run, started 15:48Z)

- **P1–P3 DONE** (`c823686`, local; push held). No-verdict Qwen tutor engine
  (`start_tutor_session` / `tutor_turn`) + per-turn `chat` CLI seam in
  `orchestrator/finding_session.py`, + 12 tests (`tests/test_finding_session_tutor.py`).
  Gate: 3 adversarial auditors 0-blocking (fence verified 3 ways — no disposition
  writer reachable, `mode='tutor'` guard in `end_session`, tests assert no verdict
  artifacts); full suite 1255 passed; real `env -u MOCK_LLM` Qwen tutor smoke
  (coherent neutral reply on `sf-iter-2026-05-26-008`, fence held). Built via
  workflow `wf_ef039239-fef` in ~19 min. **→ U2/U3 UNBLOCKED: the `chat` CLI seam
  (`finding_session chat start|turn --mode tutor|two_voice`) is on `main`.**
  Non-blocking CLI nits for the UI seam: tutor-turn envelope omits `addressee`
  (read with `.get`); argparse arg-errors exit 2 with usage on stderr (not the
  JSON envelope).
- **P4 DONE** (local; push held). Blessed pre-verdict calibration writer
  `orchestrator/calibration_cli.py` (argv `calibration --ref-id --prediction
  --confidence --by` matches the cockpit stub verbatim; confidence ∈ [0,1]
  REJECTED-not-clamped per rule 4; append-only to `run_state/events.jsonl`) +
  `schema/calibration_pre_verdict.schema.json` + the additive `pre_verdict` branch
  reconciling the spine `schema/events.jsonl.schema.json` (**D-055** — integrator
  default that overloads `calibration_entry` via a `phase` discriminator; **ratify
  or redirect**). 24 writer tests + new events-schema pre_verdict tests. Gate: 2
  auditors 0-blocking; full suite 1285 passed; real CLI smoke (valid write +
  out-of-range reject + the written row validates against BOTH schemas). Built via
  workflow `wf_8e84fc89-82e`. **→ Unblocks the cockpit `/calibration` seam: ui/backend
  swaps the stub body for an exec of `orchestrator.calibration_cli` with zero argv
  churn.**
- **P5 DONE** (local; push held). Doc-only cockpit-seam reconciliation:
  `docs/cockpit_seam_wiring.md` — the authoritative per-outcome wiring spec for U4,
  verified against landed code by **real CLI invocation** (every argv checked, audit
  PASS). Finding: **no orchestrator gap blocks U4** — all 6 outcomes + calibration +
  tutor/two-voice chat have landed writers of record. The remaining defects are
  entirely `ui/backend` (the stub's `_SEAM_MODULES` mis-targets
  `authorize_fix`→todo_cli, `calibration`→gate_cli, `directive_signoff`→gate_cli +
  wrong identity key) — all documented as U4 fixes, not orchestrator work.
  **spawn_topic / abstain → render as session-exits** (don't build one-shot writers,
  D-046); the in-UI exit mechanism (terminal REPL vs a narrow chat `end` verb) is
  deferred to a U4 decision. Also de-staled a D-055 reconciliation reference in
  `calibration_cli.py`. Built via workflow `wf_a9ef3b38-59c`.
- **P6 DONE** (local; push held). `ml_intern` advisory re-attach — the D-052 known
  gap named verbatim in `a30f58f`. The gap: `nara.py`'s ml_intern re-retrieval branch
  recomputed relevance but DROPPED the non-gating `topicality_advisory` field, so an
  escalated (ml_intern-augmented) iteration's recorded relevance silently lacked it
  even when armed. Fix: the ~6-line guarded re-attach mirroring the primary site
  (L821-829), at the re-retrieval site, targeting `re_ret["result"]["relevance"]`.
  Spine edit (`nara.py`) applied by the integrator; the build agent (correctly)
  flagged it + wrote the disjoint test `tests/test_ml_intern_advisory_reattach.py`.
  **NON-GATING verified** (`relevance()` never reads the field; attached AFTER
  relevance() finalizes; dark-by-default + fail-open). Gate: audit 0-blocking; full
  suite 1289 passed (+4 new tests); real import smoke clean. Built via workflow
  `wf_a973ef88-bc4`. **P1–P6 (the defined queue) complete.**
