# Session — 2026-06-06

Continuation of the 2026-06-05 push. Plan: `.claude/plans/elegant-bouncing-gem.md`
(Block 1 instrumentation → Block 2 finding-promotion bridge).

## Reconciliation at start
Main had moved: the UI session merged again (`83c61dd`), and the suite was RED — `test_results_summary_resolution`
asserted exp005 had no `results_summary`, but my 2026-06-05 exp005 real-run commit (`92f27b1`) legitimately added
`exp005/results/summary.json` (I'd missed re-running the full suite after committing those results). The UI merge was
innocent (ui/ only). Fixed the stale assertion (exp005 now has a real summary like exp003/exp004), pushed `424c5d7`,
main synced — green at **642** before building.

## Block 1 — observability instrumentation (Dynamic Workflow `wf_93cc6fa0-b39`, 7 agents)

The UI session was blind to non-loop runs. Delivered the 5 instrumentation asks (no `ui/`, atomic writes, DATA_SHAPES
in-commit):
- **`orchestrator/active_run.py` + `schema/active_run.schema.json`** — generalized atomic active-run state
  (`write/update/clear`), written by ALL run modes (exp003-006 run.py, autoresearch, nara — alongside the loop-detail
  `active_iteration.json`). The "biggest win": `/activity` can now show what + how far for any run.
- **`agent_wrapper/worker_activity.py`** — per-call REAL inference internals (`tok_per_s`, `tokens_generated/target`,
  `eta_s`, `synthetic:false`), emitted by the wrapper after each call. Scoped to **per-call** (honest, drops the UI's
  synthetic marker); live per-decode-step streaming is a flagged future upgrade.
- **`run_id` on calls** — `contextvars` `_run_id` in the wrapper (`set_run_id/get_run_id`), set by every run mode; added
  optional `run_id` + `max_tokens` to the call record + `schema/calls.jsonl.schema.json` (additive — validated against
  4701 existing rows).
- **`orchestrator/exp_orchestrator_rows.py`** — experiments emit minimal orchestrator-shaped dispatch/invocation/receipt
  rows so the UI worker table + causal graph light up with zero UI change.
- DATA_SHAPES.md changelog updated for all of the above.

**Verify:** offline tests (22 for the 3 helpers) PASS; adversarial PASS (atomic os.replace; additive schema; best-effort
emit never breaks a call); real tiny-run smoke confirmed `active_run.json` lifecycles live (kind=experiment, progress
advanced, cleared) + `worker_activity.jsonl` real (tok/s ~71, synthetic:false).

**Gate fix (verify caught it):** the smoke flagged `run_id` MISSING on `calls.jsonl`. Root cause: `nara._record_turn`
builds its own record (bypassing `wrapper._record`'s stamp) — it set the run_id but never wrote the field. Fixed (stamp
`get_run_id()` in `_record_turn`); confirmed a real loop_v0 iteration (`iter-2026-06-06-001`) now carries
`run_id` on every nara call. Also surfaced a **deeper routing fact**: experiment `call_sync(log_path=None)` goes to the
in-memory `MEMORY_LOG`, NOT `logs/calls.jsonl` — so experiment inference is attributable via `worker_activity.jsonl`
(which DOES carry run_id), not the calls log. (This is the root of why the UI live-calls signal missed exp005; routing
experiment calls to `calls.jsonl` is a follow-up.)

Suite **642 → 664**. exp006 n=2 smoke results restored to the committed n=40.

## Carried follow-ups
Route experiment calls to `logs/calls.jsonl` (or point UI live-calls at worker_activity); worker_activity live-streaming
(Option C); exp005 `loop_bridge`; ML-Intern query distillation; Step 4 robustness-in-loop; **rotate `SEMANTIC_SCHOLAR_API_KEY`**.
