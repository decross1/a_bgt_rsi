# 2026-09-04 — resume and finish Team 2 cleanup

Primary session: Codex (`codex-main`). The owner invoked `resume-state`;
this continues the bounded four-team completion authorized in
[2026-09-03.md](2026-09-03.md). No new research or UI implementation is
part of this continuation.

## Resume reconciliation

- Main began at `f0c7ea1ddd1859b0a671c72c6d71814941b9c25b`, three commits
  ahead of the local `origin/main` reference. Live frontier-ledger appends,
  six uncommitted spawn-ledger rows, and `docs/project_health_2026-09.md`
  were preserved.
- Teams 1/3/4 and Team 2's audit are complete. The Team 2 builder ledger
  still said `running`, but its isolated worktree at
  `/tmp/codex-team2-parent.AgSuQw/repo` preserved the ten-file, 180-line
  cleanup. Resume starts with review and verification of that patch.
- `week1.state.json` remains historical: `current_day=day_10`,
  `active_plan=LOOP_V0.md`, 106 completed IDs, no pending human gates.
  The current contract identifies `LOOP_V1.md` and dated session notes.
  Six historical completed IDs have no exact matching run-log task ID;
  281 logged IDs are absent from the state list at this resume snapshot.
  These discrepancies do not justify rerunning completed work or rewriting
  historical attestations.
- The September 2 vendor-spend pause was explicitly cleared September 3
  for bounded Codex agents. That authorization remains in force.
- Deferred queue empty; no UI worktree; daemon PID 818128 active since
  September 2; loop alert OK. Both local model services report HTTP 200
  health and the expected Gemma/Qwen model identities.
- Six pytest processes from the interrupted session were still sleeping
  after 39.5–41.9 hours. The primary verified their exact commands and
  working directories, then sent SIGTERM only to those test processes.

## Active practice corrections and brain snapshot

Authority remains in [DECISIONS.md](../../DECISIONS.md):

- D-075: preserve calibrated gates and information in verdicts.
- D-074: PI reproducibility is statistical, not byte-deterministic.
- D-072: verify local hardware/runtime provenance before qualification.
- D-070: size token and retry budgets against measured requirements.
- D-069: isolate frontier configuration from mutable global settings.

Framework snapshot: 9 active rules; 80.7 days since the last proposal closed;
5.0-minute median time-to-resume over its last five sessions. These are
framework observations, not measurements of this project's current session.

## Verification plan and authority

Reuse the preserved patch; independently review it under a ten-minute
read-only spawn contract. Verify the ten-file allowlist, 180-line cap,
unchanged test bodies, local links, targeted tests, mechanical premerge
gate, and identical full-suite failure IDs. The full suite runs in the main
checkout before and after application so the interpreter and required
gitignored research artifacts are the same. The isolated worktree lacks
those artifacts and cannot establish a comparable full-suite baseline.

The September 2 owner-approved plan explicitly says “python suite at
baseline”; September 3 retains the documented baseline comparison. This
specific authorization governs this bounded cleanup despite the generic
full-green rule in `CLAUDE.md`. Failures remain failures; no threshold is
changed and this does not authorize future changes to ship on a red suite.

Production logic and UI are unchanged, so the real smoke checks the live
documented serving topology with `MOCK_LLM` unset; it does not claim an
inference or research-pipeline validation. Frontend/backend revalidation
is conditional on affected code in the September 3 plan and is not needed
for this documentation/test-marker patch.

## Results and next step

Cleanup `2ad38c5` is integrated. Independent review found two documentation
blockers and one historical-wording note; the primary fixed them within the
same ten-file/180-line cap, and the follow-up review passed without findings.
All 108 local Markdown links resolve; 38/38 test bodies are unchanged;
43 targeted checks and the mechanical premerge gate passed.

The same-environment full-suite comparison passed: before, 7 failed / 2,487
passed / 1 skipped / 2 xpassed; after, 7 failed / 2,489 passed / 1 skipped.
The seven failing node IDs are identical; no errors or new skips appeared.
The suite remains red. Full evidence is in
[team2_validation.md](../../notes/research/2026-09-02-teams/team2_validation.md).

All four review teams now have reconciled results, with Team 3's fixed-corpus
coverage limit and Team 4's missing token lens retained. No completed review
or experiment was rerun. The stale builder execution is closed as interrupted;
the primary's recovery and integration are logged separately as successful.

Next work needs an owner-selected focus: D-077 graveyard disposition;
ratification of the draft research/systems decisions; dependency/provenance
and seven-failure repair work; or the separately scoped UI work order below.
Other carried decisions remain the planning-layer ratification, `fa-43df1c54`,
and the dark `NARA_CONSTRAINT_CONDITION` flag. None was cleared by this cleanup.

## UI session work order

The complete future UI work order remains in
[2026-09-03.md](2026-09-03.md#ui-session-work-order--owner-actions-and-weak-surface-repair).
This session makes no UI changes.
