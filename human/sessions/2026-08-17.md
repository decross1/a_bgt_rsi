# Session note — 2026-08-17 (continuation past midnight of the 08-16 session)

One owner decision executed at the top of the day; the rest carries over.

## D-072 filed — Qwen 3.8 qualification design RATIFIED

Owner: *"ratify the design, file the decision entry."*

- **D-072 appended to DECISIONS.md** — the Oracle steward's matched-FP8
  qualification design (lab_channel:15), ratified WITH the five amendments
  the adversarial verification forced (docs/qwen38_role_setups.md §9). Core:
  official `Qwen3.6-27B-FP8 @ e89b16eb` vs `Qwen3.8-27B-FP8 @ 017b9c7a`
  (verified byte-equal, 28.75 GiB each) on the digest-pinned `qwen38` eval
  runtime; MTP off + BF16 KV first; six sentinel cases with hard stop
  conditions; Inferact NVFP4 tested last as a deployment optimization;
  adoption rule pre-declared (quality equal → retain 3.6).
- Ratification row in `run_state/overrides.jsonl`
  (`D072_ratify_qwen38_qualification_design`). Checklist cross-referenced.
- D-072 also closes the 3.8 record-keeping gap (windows #1/#2 history +
  window-#1 citation rule) and folds in the D-070 numeric corrections.
- **Still owed before execution:** weights download go/no-go (2 × 30.87 GB,
  G6-class), owner-scheduled solo windows (FP8 arms cannot co-reside with
  Gemma), budget replenishment. Priority Low per Nara's portfolio
  classification — Liquid Democracy stays the active research focus.
- **NOT changed:** production pins, serve configs, queue state. Cutover
  remains behind G6/D-0zz.

## L1→L2 experiment block — EXECUTED (owner-authorized 2h window, 02:41–~04:10Z)

Owner: *"start the block"* + *"do as much work as you can, set dynamic
workflows and loops to do a wall clock amount of work for 2 hours."*

The three L1 clusters owing synthetic experiments got them — pre-registered,
adversarially critiqued BEFORE lock, built by workflow agents, run real, and
resolved. **Result: three honest nulls, three kills, zero survivors — and the
lab's first L2-graded evidence.** Full chain in `experiments/PREREG_l2block_2026-08-17.md`
+ commit `b20e8bf`.

- **Process worked hard:** all three draft designs came back FIX-REQUIRED
  from the critique panel — exp010 had a state-cardinality confound biasing
  TOWARD confirmation, exp011's attack had a provable ~40% unresolvable-pair
  ceiling making NO predetermined, and exp012 v1's positive outcome was
  **manufacturable in closed form from its own constants**. exp012 was
  redesigned (quantization-only + fixation/cycle detection) and re-critiqued
  before locking as v2.1. Without the pre-lock critique, exp012 would have
  produced a false YES and exp011 a meaningless NO.
- **exp010** (audit-cadence collusion): Verdict=NO — Q-learners converge to
  defection in both audit arms (median gap 0.0); the timing mechanism is
  directionally present (p=8.7e-06) but under the locked 0.10 floor; placebo
  clean; Q1 ("any monitoring reduces collusion") adjudicated REFUTED.
- **exp011** (matching preference reconstruction): Verdict=NO — median tau
  0.674; locked attribution fired case (i) budget-limited ⇒ the null counts
  as **evidence FOR the standing Qwen query-complexity refutation** (0.73
  bits/query vs 28.8 required).
- **exp012** (spectral-radius threshold): Verdict=NO — ln R declines
  smoothly, tracking the pre-registered construction-geometry prediction; no
  threshold. The bridged iteration (iter-2026-08-17-011) **derived L2 — the
  first L2 in the lab's history**; the claim killed with scope limit carried.
- All three original clusters: member_added (bridged iteration) +
  evidence_level_changed (derived, never hardcoded) + cluster_killed
  (experiment_null_effect, reopen on experiment_rerun) — appended before the
  hourly consolidation could found duplicates.
- Hygiene: suite 2060/0 (one earlier flake was load contention from a live
  bridge — clean rerun green); 109 new tests; pinned tier-count test
  deliberately updated 5→8; three live bridges served as the real smokes.

## Open / next session (carried from 08-16)

- Owner decision: D-066 proposer (narrow Gemma's problem source vs amend
  D-061 to let the frontier tier propose); do not arm `NARA_SELF_IMPROVE`
  on a cadence until then.
- Repair or deliberately keep `~/.codex/config.toml` broken.
- Evaluate the first full day of budget pacing (from 00:00Z today) and
  `NARA_DEBATE=1` in live cycles; consider making budget-refused cycles
  recompute `loop_alert.json`.
- Qwen 3.8 execution: schedule the D-072 windows when budget/priority allow;
  the raised-cap stage-3a kill-check re-run can share the first window.
- Unchanged: nara-daemon systemd unit install; two L1 clusters owe N≥30
  synthetic experiments; judge-calibration set revision.
- Housekeeping: prune the 13 stale `.claude/worktrees/agent-*` worktrees
  after checking for unmerged work.
