# Session — 2026-08-14: LOOP_V1 restructure (un-zombie · evidence ladder · idea ledger · frontier tier · micro-org plumbing)

> Primary session. First session since 2026-06-30 (45-day gap). Resume-state found the
> apparatus in a silent zombie: 20 consecutive cron cycles since 08-05 planning only
> `promote_findings(max_candidates=1)`, "passed" every time, promoting nothing; all 31
> surfaced findings unanimously skeptic-refuted (16 redteam fatal_flaw) and promoted
> anyway; **zero human dispositions ever**; S2 literature search blind on ~60% of recent
> iterations; no alerting path anywhere. Owner + session spent the day in an extended
> restructure discussion (frontier-model critiques of the whole program + 5 research/
> exploration agents over this repo, the PoE org, the framework brain, and the 2024–26
> memory/QD literature), converging on **selection-before-the-human**.

## Approved plan

**[`LOOP_V1.md`](../../LOOP_V1.md)** — committed verbatim from the approved plan-mode
artifact. Headlines:

- **P0 un-zombie:** staleness + ladder gaps in `assess_state`; stall detector +
  `run_state/loop_alert.json`; cycle rows persist planner state/attempts; keyed
  near-miss dedup; explicit logged S2→local fallback in ml_intern.
- **P1 evidence ladder (L0–L5), surface bar = L4+** (adversarial vote + redteam become
  the L3→L4 gate — the two signals promotion currently ignores). D-053 advisory flip
  retired (D-059). Consolidation: 31 findings + 101 loop_memory rows clustered,
  junk archived append-only, everything demoted to its honest rung (expected zero at
  L4). Human gates G2/G3.
- **P2 frontier critic tier (priority):** Claude (methods) + Codex (novelty) as an
  opposed-jobs veto screen inside the promotion funnel, env-gated, annotate-only
  firewall, every call ledgered (`run_state/frontier_calls.jsonl` = frontier-vs-local
  calibration dataset); weekly agenda-synthesis cron committed dark behind the G1
  ToS sentinel. `ANTHROPIC_API_KEY` strip guard mandatory (var is set globally —
  metered-API trap confirmed live).
- **P3 idea ledger + memory continuity:** append-only `memory/idea_ledger.jsonl`
  (clusters = niches, MAP-Elites elite rule, LLM equivalence judge gated behind a
  pre-registered calibration experiment, programmatic kill reasons, paper niches
  pre-closed, adopt-or-reject at generation) + deterministic three-section `ideas.md`
  (live / graveyard / agenda). Agenda-first topic selection; arxiv picks demoted to
  candidates. Deep-wiki shape **rejected on measured grounds** (ETH AGENTS.md study,
  poisoning reports, Memora/FAMA) — citations in the research-agent reports.
- **P4 micro-org plumbing, enforcement first:** task-packet schema,
  `tools/premerge_check.sh`, `packet_dispatcher` (attempt-before-invoke,
  dispatcher-owns-ack, red-first), `run_state/packets.jsonl` + `overrides.jsonl`,
  `docs/packet_sdlc.md` + entrenchment tiers (G4).
- **P5 Qwen 3.6→3.8-27B prep only** — checklist + A/B battery; cutover human-gated
  (G6), NOT this build.

## Session log

- Resume-state reconcile: stall + refuted-backlog diagnosis (3 explore agents:
  this repo / PoE org / framework brain). Key artifacts: planner fixed point
  (`coordinator.py:343-368` gaps + temp-0.1 identical snapshot), promote pool empty
  (one candidate blocked by a 2026-06-18 UI **test click** verdict "invalid"),
  health signals structurally silent when stalled, PoE merge robot never merged
  (gate-not-installed lesson), framework proposal loop never closed once.
- Research sweeps (2 agents): agent-memory literature (negative memory must be
  typed + programmatic — Honest Lying 0/121; mandatory adopt-or-reject —
  arXiv:2606.21024; SAGE validates D-058's layered dedup) and QD/novelty archives
  (MAP-Elites elite rule; IDEAAgent 2026; judge calibration precondition).
- Plan designed via 2 Plan agents (code-seam grounding; corrected two established
  facts: JSON is clean — no repr-normalization needed; ml_intern is S2 not arXiv,
  backoff already present).
- Wave 0 (this entry): LOOP_V1.md + D-059/D-060 + spawn contracts committed.

## Owner decisions recorded today

1. Consolidate + demote the 31 (archive junk, keep unique candidates); **no
   human-validation pass yet** — basal system first.
2. Cockpit/UI redesign **parked** (only L4+ view + alert polling as a future UI
   work order). Rubberband cockpit deferred.
3. Frontier subscriptions are a **priority integration** this build (falsifier
   role only; PI stays local Gemma/Qwen).
4. Micro-org plumbing + self-serve SDLC **in this build**; maximize dynamic
   workflows/concurrency.
5. Qwen 3.8 upgrade **prep scoped now**, cutover gated.
6. Surfacing is a **bar, not a quota** (~1/week expectation; zero-survivor weeks
   reported honestly).

## Build log (Waves 0–4, all committed)

- `c235884` Wave 0 plan artifacts · `8f050cd` Waves 1+2 (17 build agents, all
  new modules + integrator edits; suite 1314→1657) · `8b01d79` 21 verified
  review findings fixed (30-agent adversarial review workflow) · `cc2b542`
  first real packet merged through the full dispatcher gate ·
  `c195155` Wave 3 (calibration + e2e fixes). Suite: **1664 green**.
- **Judge calibration: FAILED honestly** (224 real Gemma calls): precision
  0.31 vs bar 0.90, false-equiv 20% vs ≤10% → per D-060's pre-registered
  consequence the LLM equivalence layer STAYS OFF; prefilter-only stands.
  The negative result the literature predicted for open-weight judges —
  re-calibration candidates: post-consolidation corpus, or the frontier tier
  as judge (G1-gated).
- **ml_intern blindness root cause corrected:** SEMANTIC_SCHOLAR_API_KEY is
  absent in the CRON env (not arXiv 429). Fix: `cron/secrets.env` seam
  (git-ignored) + loud warning. Plan's local-Chroma fallback DROPPED as
  masquerading (retrieval already queries papers_recent) — deviation logged.
- **Fixed-point regression check:** two real dry-runs now plan
  `run_loop_iteration` with the staleness gap ("loop has not iterated in 10
  days") visible in the persisted planner_state.
- Real dispatcher e2e caught two traps (relative venv dies in worktrees;
  digest-only ledger opacity) — both fixed + documented.

## Consolidation dry-run (G2 evidence — NOTHING written yet)

132 rows (101 loop + 31 surfaced) → **70 clusters**: rungs **L0=68, L1=2,
zero at L2+** (strict ladder: most elites die on redteam fatal_flaw or fail
L1 before their experiments count); **47 clusters killed** on programmatic
redteam fatal_flaw; **9 paper niches** (rediscovery closures); **31 near-dup
members archived**; **8 leaked-JSON claims repaired** (more than the 2
known); 190 append-only events planned. Source ledgers never rewritten;
demotion is overlay events, reversible by deleting the ledger.

## Human gate queue — BLOCKING, in order

- **G2 (now):** approve the consolidation summary above → I run
  `env -u MOCK_LLM .venv-chroma/bin/python -m workers.consolidate_memory --execute`
- **G3 (now):** supersede the 2026-06-18 UI-test "invalid" verdict — run:
  `.venv-chroma/bin/python -m orchestrator.gate_cli --iteration-id iter-2026-06-05-002 --verdict needs_revision --note "supersedes 2026-06-18 UI test verdict (was a smoke-test click, D-059 migration)" --by human:derrick`
  (exact flags may need `--help`; the apparatus never writes gate verdicts)
- **Secrets (now, 1 line):** create `cron/secrets.env` containing
  `export SEMANTIC_SCHOLAR_API_KEY=<your key>` so cron cycles stop running blind.
- **G1:** per-vendor ToS decision for headless Claude Max (`claude -p`) +
  ChatGPT (`codex exec`) automated screening (hypothesis text leaves the
  box). Clear by: `touch run_state/frontier_tos_ratified`. Everything
  frontier stays dark until then (CLI + cron both refuse).
- **G4:** ratify the entrenchment tier list in `docs/packet_sdlc.md` (DRAFT).
- **G5:** D-061 decision entry + CLAUDE.md stale "second model excluded
  (D-033)" bullet fix + active-plan pointer → LOOP_V1.md (contract edit —
  drafted on your go).
- **G6 (whenever):** Qwen 3.8 weights acquisition go/no-go
  (`docs/qwen38_upgrade_checklist.md`); cutover stays gated.

## UI session work order

Pending Wave 5 (after G2/G3): L4+-only cockpit view + `run_state/loop_alert.json`
red-state polling. No workflow agent touches `ui/`.
