# Session — 2026-06-25: Away-period reconciliation + carried-items clear + S3 UI work order

> Primary session. First working session since 2026-06-19 (six days of
> automated-only operation). Reconciles what the cron automation did while the
> owner was away, clears the two carried owner items, and authors the **S3**
> UI session work order (§5 Experiments interactive + §3 Activity confirm).

## What happened while away (2026-06-19 → 2026-06-25)

No human/primary commits since `f16a967` (the 06-19 S2/S3 batch). The repo ran
on automation only. Cron inventory + health:

| Job | Cadence | Health |
| --- | --- | --- |
| `daily-arxiv.sh` (paper download → embed → `papers_recent`) | 03:00 daily | **DEGRADED** — arXiv `HTTP 429` rate-limit. 06-23 succeeded (+33 papers, collection 539→**572**); 06-24 + 06-25 **both failed** after 6 retries. Transient throttle, store intact. |
| `snapshot-chroma.sh` (chroma backup) | Sun 04:30 | **OK** — last 06-21 (34M, 6 snapshots retained). |
| `run-coordinator.sh` (the **2×/day** coordinator runway) | 09:00 + 15:00 daily | **LIVE** — but starving (see below). |
| `ui-services.sh ensure` (keep UI up) | every 2 min | OK. |

**The 2×/day coordinator — what it's actually doing.** The D-049 continuous
runway went **live 2026-06-18** (the owner ratified it: `run_state/d049_ratified`
exists, no `pause_coordinator`). Each firing runs one
`coordinator --once --execute --budget 6` cycle with the vllm-qwen skeptic armed.
**Every single cycle picks the same action — `promote_findings(max_candidates=5)`
→ "passed" — and promotes ZERO findings.** It is not generating new research;
it is re-voting on the existing surfaced findings twice a day, and the
survive-iff-minority-refute adversarial vote refutes them all. This is exactly
the **D-053 promotion-starvation** symptom. ~29 min of qwen skeptic burn per
cycle for no cargo. → fixed this session (item 2a).

## The "22 open items" — triaged, mostly not fluff but already reclassified

`memory/loop_memory.jsonl` holds **27 `gate_status: pending`** iterations
(06-05 → 06-19). But the **S2 cockpit reframe already triaged them**: the live
`/todo` inbox now surfaces only **9 substantive items** (4 `gate_verdict`
experiment-grounded go/no-go + 5 `finding_review`). The rest are correctly
**observable, non-blocking** literature-stage journeys:

- **Substantive (cockpit-blocking, 9):** experiment-grounded verdicts incl.
  `iter-06-05-004` (exp004 VCG), `06-05-006` (exp006), `06-10-001` (repeated PD),
  `06-19-011` (exp004, the Loop-3 bridge) + 5 finding reviews.
- **Novel literature survivors (~11):** novelty=`novel` but literature-stage
  only (no experiment) — incl. `iter-06-19-003`, the Loop-3 seed. Observable;
  promoted only by an owner decision to drive them to experiment.
- **Routine literature-stage (~10):** rediscovery / nonsense / restated — the
  Loop-1 & Loop-2 demonstrations and similar. Correctly **not** blocking.

**Verdict: not fluff, but already handled by the reframe.** The inbox is honest
(9 real items). No cleanup of loop_memory needed.

## The 3 use-case loops — end-to-end status

All three were **demonstrated live 2026-06-19** with real `env -u MOCK_LLM`
artifacts and remain intact (no code drift since):

1. **Falsification** — `iter-06-19-002` / journal 077: false "TfT is a one-shot
   Nash eq" → `nonsense` + critic `falsified` (cited Osborne-Rubinstein).
2. **Rediscovery** — `iter-06-19-001` / journal 076: "TfT dominance in repeated
   PD" → `rediscovery` + `restated` (cited Hofbauer-Sigmund).
3. **Full novel → experiment → bridge** — Loop-3 plumbing proven
   (`iter-06-19-011`: clean 5-step chain, `experiment_outcome` + cross-tier
   present, bridged finding vetted). Seed for the next build: `iter-06-19-003`.

Backend suite green this session: **`tests/` → 1306 passed, 1 skipped, 2
xpassed**. (Note: bare `pytest` also collects `ui/sampler/` which has 196
out-of-scope import errors — separate component, not the backend suite; worth a
later look but not a regression here.)

## What this session did

- **Item 2a — flipped `NARA_PROMOTION_VOTE_ADVISORY=1`** in
  `cron/run-coordinator.sh` (the D-053 lever, owner-authorized). The promotion
  vote now annotates without gating; the next 09:00/15:00 cycle should start
  letting Path-B novel/`survives` findings reach the cockpit. Reverts by
  unsetting the var. *No new decision — executes ratified D-053.*
- **Item 2b — removed the stale `ui-session` worktree.** It sat at `fba6d7b`,
  fully merged into main (`git merge-base --is-ancestor` = YES; clean tree).
  Branch `worktree-ui-session` kept at the merged commit for reference. The
  next UI session recreates a fresh worktree from current main.
- **Qwen backend (vllm-qwen :8001) was DOWN — restored.** The UI panel was
  right. Root cause: the `vllm-qwen` Docker container **exited gracefully**
  (`Exited(0)`, `OOMKilled=false`, log `Shutdown initiated (timeout=0)` at
  21:11:12Z) during a ~21:11–21:54 docker cycle that also restarted
  `vllm-gemma4` and brought up the `openshell-nara-sandbox`. **Both vllm
  containers are `RestartPolicy=no`**, so Gemma came back only because it was
  manually restarted (21:54) and Qwen simply stayed down. Not a crash, not OOM
  (57 GB free). Fixed with `docker start vllm-qwen` → `:8001` serving
  `qwen3.6-27b-nvfp4-mtp`, generation smoke clean (`PONG`, 116 completion
  tokens). **Aside:** the historical coordinator "qwen empty-content" degraded
  signal is explained — Qwen3.6 is a reasoning model; a too-small `max_tokens`
  (e.g. 16) is consumed by thinking before any answer, yielding empty `content`.
  Not a fault; callers must budget enough tokens.
- **DURABLE GAP — FIXED (model-server auto-restart).** `ui-services.sh ensure`
  manages only the UI services, not the gemma/qwen vllm containers, and both were
  `RestartPolicy=no` — so any docker/host event silently left a model server down
  until a human noticed (exactly what happened here). **Fix applied:**
  `docker update --restart unless-stopped vllm-gemma4 vllm-qwen` (in-place, no
  teardown, no config drift). Both now auto-recover from docker-daemon restart /
  host reboot / crash, while a deliberate `docker stop` still stays stopped.
  *(Note: this is a host docker-state change, not a repo artifact — it does not
  survive a full container re-create from scratch; if either container is ever
  re-run via `docker run`, add `--restart unless-stopped` to that command.)*
- **arXiv 14-day backfill (covering the 429-missed days).** Manual
  `arxiv_scraper --since-days 14` (cs.MA,cs.GT,econ.TH) → 238 pulled, span
  **06-11..06-24 with papers on every day**, then `embed_and_store` (real
  BGE-M3): **88 new + 150 dedup**, `papers_recent` **572 → 660** (persisted
  count verified independently). **No 429 on the manual run** — the throttle is
  specific to the 03:00 cron window, not the box's IP. (06-25 itself wasn't in
  the indexed window yet; tomorrow's 03:00 cron picks it up.)

## Next

- **Watch the next coordinator cycle** (09:00 / 15:00) confirm a finding now
  promotes (advisory annotation present, promotion not blocked). If still zero,
  the candidate-cap (`NARA_PROMOTION_MAX_CANDIDATES`) or threshold is the next
  lever.
- **arXiv 429:** the manual backfill proved the 429 is the **03:00 cron window**,
  not the IP — a manual pull at 22:40 UTC sailed through. If the 03:00 cron keeps
  failing, the durable fix is to **move the cron off 03:00** (or widen the jitter
  window beyond 300s) rather than touch the backoff. Store now at **660 papers**.
- **Loop 3 build (S2–S3 roadmap):** owner reads journals 076/077/078, picks the
  direction (likely `iter-06-19-003`), then drive novel → exp001 → bridge →
  synthetic replicate → bubble → cockpit go/no-go → applied.

## UI session work order — S3 (Experiments interactive + Activity confirm)

> Co-authored by the primary per the operating contract. The UI session writes
> only `ui/` + `ui_plan.md`. Start by `git pull`/FF to `origin/main`, then
> `claude --worktree ui-session` (the prior worktree was removed; this recreates
> it fresh). **Detailed spec: [`docs/ui_reframe_plan.md`](../../docs/ui_reframe_plan.md)
> §5 + §3** (this is the headline).

- **§5 Experiments — interactive applied refinement + card wall (the biggest piece):**
  - **Backend seam** — mirror `chat_seam.py`: a `_exec_blessed` runner over
    `experiments.exp007_polymarket.refine_cli` (argv-array, **no shell**),
    exposing `POST /api/exp007/refine/start` and `POST /api/exp007/refine/turn`.
    The CLI already exists (landed 2026-06-19, zero-trading, re-runs the exp007
    PAPER forecast against the offline fixture + scores Brier): `start
    --session-id <id>` → `{ok, params, tunable:{seed,temperature,n}, note}`;
    `turn --session-id <id> [--param seed:42|temperature:0.4|n:8] [--message
    <intent>]` → `{ok, params, n_forecast, brier, sample[], elapsed_s}`.
  - **Card wall** (`Experiments.tsx`): each card = status + linked finding(s) +
    tier + sign-off state ("awaiting your sign-off" when an applied-tier card is
    bubbled). Click → detail.
  - **Lifecycle + findings in detail** (`ExperimentDetail.tsx`): add the
    lifecycle (when run, which loop iteration seeded it, the bridge, cross-tier
    replications) + the findings it surfaced (join the coordinator-cycle
    `promoted_findings` / `surfaced_findings` for this experiment — currently
    orphaned in Coordinator cycles).
  - **Interactive surface:** param controls (seed / temperature / n) → turn →
    render the new Brier + sample. The owner drives it after signing a finding to
    applied in the cockpit.
- **§3 Activity — confirm only (producer already landed):**
  `agent_wrapper/worker_activity.py` emits `logs/worker_activity.jsonl` (3609
  rows, fresh during the 06-25 cycles) and `ui/backend/activity.py::_real_inference`
  reads it. **Remaining UI work is just to confirm `ActiveWorkersPanel` renders
  the real per-worker block during a live run and the synthetic marker drops.**
  No primary dependency.
- **Acceptance:** Experiments card wall → lifecycle + findings; the applied-paper
  interactive surface works end-to-end; vitest + the UI pytest green; one real
  `env -u MOCK_LLM` smoke against the live `:8700`.

When ready: print `UI READY TO MERGE`; the primary gates + merges.
