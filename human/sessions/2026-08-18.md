# 2026-08-18 — R1a verdict + adoption, cutover day-2 ops, UI substance wave

Primary session (claude-code-main), continuing the 08-17 D-075 arc.

## Focus agreed

Close out D-075's last open piece (R1a redteam calibration battery),
keep the cutover (D-074) healthy on day 2, and work the owner's live
UI feedback (Pulse perf, owe-card pointedness, frontier-panel
substance).

## Done (as of mid-session)

- **R1a battery COMPLETE → D-076.** All three arms on the frozen 24-
  fixture manifest. gemma-current FAIL (constant condemner: 18/19
  parsed condemned; all 5 unscored on known-good). **gemma-revised
  PASS all bars (0 unscored, 9/12, 0/12) → ADOPTED** per the locked
  rule: `REDTEAM_AGENT_SYSTEM_PROMPT` swapped, sha256 byte-identity vs
  the winning arm's artifact asserted, 94 tests green, daemon bounced.
  qwen38 FAIL (18/24 blew the pinned 45s production wall; the 6 it
  parsed — all known-bad — all caught): Qwen 3.8 cannot hold the
  redteam seat inside the production envelope; a re-attempt is a new
  prereg. Disclosure: attempt 1 crashed post-calls on a driver bug
  (relative `--out` vs `relative_to(REPO_ROOT)`), traceback hidden by
  `tail -3` stdout buffering; fixed, re-run clean (temp-0 greedy =
  pure repeat, D-074 determinism precedent).
- **Frontier-screen cron outage found & fixed.** First cron-context
  screen run (06:00Z) failed both vendors: cron PATH misses
  `~/.npm-global/bin` → codex exit 127 (not found), claude resolved a
  stale root 2.1.143 (exit 1). Fix at the single seam
  (`agent_wrapper/frontier_cli.py::_resolve_binary`: env pin → user
  npm install → bare name), verified under cron-like env, 20/20 tests,
  daemon bounced. Owner spotted the symptom in the panel's noise.
- **UI backend redeployed** onto the perf-work code: `/api/lab_todo`
  1.5ms warm from SWR cache (was: wedged >120s inline builds, 60+ CPU-
  minutes), `/api/ladder` 500 healed (new kill codes accepted).
- **loop_stalled alert (06:00) diagnosed benign:** the cycle's only
  action was an empty-pool promote pass, which the detector refuses to
  count as activity by design; 05:07 cycle completed
  iter-2026-08-18-011. No intervention.
- **UI agent lanes** (all uncommitted pending the gate + commit
  train): modelio perf fixes (deadlines/single-flight/eviction, 1210+
  vitest); Pulse residuals 2–6 (churn strip, stale legibility, live
  ages, last-good-sample retention, banner on pollhub, 1224 vitest);
  owe-card pointedness (record-derived what/means/vet; R1a caveat on
  old redteam kills; consequence branching on cluster state) — build
  green, reviewer FIX-REQUIRED (3s/poll ledger reduction uncached;
  hashseed-dependent member→cluster join) → fix agent running, folding
  the owner's typography polish (clamped claim-head title, one type
  scale, declarative chips); frontier panel rebuild as substance-first
  review feed (screen reasoning + agenda + refine digests; decoded
  health strip; plumbing collapsed) — build+verify running.

## Owner interactions

- Pulse issues reported live (lab_todo unreadable, LOOP STALLED,
  generic gate cards) — all triaged above.
- Frontier panel rejected as information-free ("i can't even see what
  their debating issue was") → rebuild dispatched.
- Card polish feedback (title wall, type scale, "superseded?" chip) →
  folded into the running fix agent.

## Next

- Land both running lanes; then the merge gate: framework
  `code-review` skill over the full local range + full suite + one
  `env -u MOCK_LLM` smoke; single commit train + push; final UI
  reload.
- Close spawn-ledger rows at fan-in; EOD memory update.
- Watch next hourly cycle: frontier screen should heal (post-PATH
  fix); stall alert should clear on next substantive action.
- Parked: D-066 proposer (owner's Oracle↔Nara channel), D-073 T1
  license gate, vLLM #52197 / v0.27.2 watch triggers.
