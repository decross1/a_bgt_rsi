# 2026-09-03 — complete the four-team evaluation with bounded subagents

Primary session: Codex (`codex-main`). This is the committed execution plan
for the unfinished 2026-09-02 four-team evaluation.

## Owner authorization

Verbatim owner direction:

> Can you split off teams of subagents to complete the remaining work for all 4 teams?

This clears the 2026-09-02 pause for the work described below. The execution
path uses bounded Codex collaboration agents and reuses the saved sweep and UI
workflow artifacts; it does not restart either metered Claude workflow or call
the local model services from a child.

## State basis and inherited constraints

- Base before this note: `5ebf40be2b4806478391b0d93ba7c504f785edfd`.
- `main` was one commit ahead of `origin/main` at resume.
- `run_state/frontier_calls.jsonl` had live append-only daemon writes; preserve
  them and keep them out of plan commits.
- No `ui-session` worktree was present. Research/UI/systems children are
  read-only and return evidence to the primary; they do not edit the checkout.
- The primary is the only integration and commit authority.
- D-075 research-workflow corrections remain binding. Every factual research
  claim must trace to a fetched primary source; uncertainty stays explicit.
- Every spawn verifies its contracted `HEAD` before work. A mismatch escalates.
- No child writes `run_state/`, `memory/`, `logs/`, `ui/`, `human/`, or the
  shared orchestrator spine. No child invokes vendor CLIs, local LLM endpoints,
  Chroma, service restarts, publication, deployment, or destructive git actions.

## Execution order

1. **Team 1 — research positioning** and **Team 4 — UI/UX synthesis** run in
   parallel from cached artifacts. Team 1 finishes primary-source verification,
   deep reading, positioning synthesis, and draft decision material. Team 4
   finishes the 13-surface synthesis, weakest-three ranking, UI work order,
   design-token direction, and mockup specifications.
2. **Team 3 — adversarial systems review** starts after Team 1 and Team 4's
   first synthesis pass, preserving the order in the 2026-09-02 plan. It finds,
   verifies, and compares relevant agent-system failure controls using primary
   sources.
3. The primary reconciles Teams 1/3/4 and writes:
   `docs/related_work_2026-09.md`, `docs/decisions_draft_2026-09.md`,
   `docs/systems_review_2026-09.md`, evidence notes under
   `notes/research/2026-09-02-teams/`, and the UI session work order in this
   note. Mockups are produced as review artifacts, not as changes to `ui/`.
4. **Team 2 — code quality, documentation, and dead code** runs last. Its first
   pass is read-only and returns a decision-grade candidate list. Any approved
   edits happen in an isolated worktree, remain outside protected paths, pass
   `tools/premerge_check.sh`, and are independently reviewed before the primary
   integrates them.
5. Final verification reports every check independently: targeted checks,
   documented full-suite baseline comparison, frontend/backend checks when
   affected, dead-link check, framework `code-review`, and one real
   `env -u MOCK_LLM` smoke when code changes land.

## Done condition

All four teams have a reconciled, evidence-backed result; promised documents
and UI review artifacts exist; any Team 2 changes are independently reviewed
and verified; spawn/run ledgers close every child honestly; no prior completed
work is rerun merely to recreate it.

## Reconciliation checkpoint

Teams 1 and 4 completed their cached-artifact syntheses. Team 3's first child
hit its 1,800-second cap after producing nine preliminary survivors; a bounded
334-second recovery then completed conservative disposition of that fixed
corpus. Its broad miss-hunt did not reach two dry rounds, so the systems review
states that coverage limit rather than claiming repository-wide exhaustiveness.

Primary-written outputs at this checkpoint:

- `docs/related_work_2026-09.md`
- `docs/decisions_draft_2026-09.md`
- `docs/systems_review_2026-09.md`
- `notes/research/2026-09-02-teams/team{1,3,4}_validation.md`
- three mockups under `notes/research/2026-09-02-teams/mockups/`

Team 2 starts last from the committed version of this checkpoint.

## UI session work order — owner actions and weak-surface repair

Source review base: `56c1b3abae968c8288471b30946000c29ae6f228`.

This is a handoff for a future `ui-session`; it does not authorize this
four-team review to modify `ui/`. Before editing, verify the exact base and read
`CLAUDE.md`, `human/sessions/2026-09-02.md`, and `ui_plan.md`.

### Boundary

- Allowed future-session writes: `ui/**` and `ui_plan.md`.
- Do not modify `memory/**`, `run_state/**`, `human/**`, orchestrator code, or
  producer ledgers from the UI worktree.
- `ui/backend/**` may expose already-present data.
- If correctness requires producer or audit-semantic changes outside `ui/**`,
  append a labeled proposal to `ui_plan.md` and stop that slice.
- Preserve required human notes. Never turn a non-decision into a decision.
- Finish the future implementation session with `UI READY TO MERGE`.

### Review baseline

The cached review reconciled 129 result rows into 114 unique completed keys:
38 page/lens sweeps, three owner flows, thirteen comparable-product passes,
three color/docs audits, and 57 refuter votes. One token lens
(`pattern-color-a11y`) never completed, so token dimensions `c`, `d`, `h`, and
the token composite remain unsupported.

The weakest-three cut is Graph (12.0/48), Cycles (17.3/48), and Experiment
detail (17.3/48). Inspector ties Experiment detail but falls outside the stable
three-surface cut. Cached full-page screenshots measured Cycles at 154,246px
and Experiments at 155,662px; these are 2026-09-02 snapshots, not live counts.

### Package 1 — gate verdict P0

Paths:

- `ui/frontend/src/routes/DossierReader.tsx`
- `ui/frontend/src/components/GateVerdictForm.tsx`
- `ui/frontend/src/components/OweCard.tsx`
- corresponding dossier, cockpit-resolution, attest-form, and owe-card tests

Changes:

- Render `vet`, `doing`, `approval_means`, and `triage_reason` in a compact
  Decision context block beside the verdict station.
- Give the station a stable anchor and first-viewport jump.
- Replace raw endpoint/kind copy with owner language; retain technical detail
  in a disclosure.
- Autofocus the required note when invoked and add an explicit accessible
  keyboard submission path.
- After success, offer “Next owed item” and “Back to owed queue.”

Acceptance:

- Context is visible before a verdict.
- Required note and backend semantics are unchanged.
- The action is discoverable without manual scrolling.
- Next-item progression does not require a manual return to Pulse.
- Loading, error, disabled, success, and no-next-item states are tested.

### Package 2 — agenda ruling P0

Paths:

- `ui/frontend/src/App.tsx`
- `ui/frontend/src/design/CommandPalette.tsx`
- `ui/frontend/src/routes/Pulse.tsx`
- `ui/frontend/src/routes/Ladder.tsx`
- `ui/frontend/src/routes/ModelIO.tsx`
- `ui/frontend/src/components/FrontierReviews.tsx`
- `ui/backend/frontier_reviews.py`
- relevant palette, Model I/O, frontier-review, and backend tests

Changes:

- Add a direct “Rule on agenda proposals” owner entry and command-palette
  route.
- Use one authoritative pending/unruled count across Pulse, Ladder, Model I/O,
  and empty states.
- Default to unruled-first and add paging or bounded virtualization; never
  truncate silently at twenty.
- Keep proposals visually separate from model-call telemetry.
- Autofocus the note, support explicit keyboard submit, and advance to the
  next pending proposal after success.

Acceptance:

- Every pending proposal is reachable.
- Empty copy cannot disagree with the authoritative count.
- The first proposal requires no scrolling.
- The palette exposes Model I/O and Inspector.
- A proposal remains pending unless the required note and decision succeed.

### Package 3 — bubble acknowledgment P0

Paths:

- `ui/backend/human_todo.py`
- UI backend schemas/tests
- `ui/frontend/src/routes/DossierIndex.tsx`
- `ui/frontend/src/routes/DossierReader.tsx`
- `ui/frontend/src/components/BubbleAckForm.tsx`
- frontend API/types and dossier tests

Changes:

- Return and render bubble question, context, and finding IDs.
- Generate meaningful fallback titles from the question rather than “bubble
  with no note.”
- Replace six-word title-stem grouping with stable semantic grouping.
- Add direct filter/deep-link support from Pulse.
- Retain individual acknowledgment and its required note.

`[needs primary seam]`: do not introduce a new atomic batch-ledger event
without primary approval. If approved, a frontend fan-out over existing
single-item actions must expose per-ID progress and partial failures.

Acceptance:

- Question and context are visible before acknowledgment.
- Identically titled but semantically distinct bubbles do not merge.
- URL filters survive reload.
- Errors and partial failures remain visible.
- No acknowledgment is recorded for a failed request.

### Package 4a — Graph / Recent work map

Paths:

- `ui/frontend/src/routes/Graph.tsx`
- `ui/frontend/src/components/ActivityGraph.tsx`
- `ui/backend/activity.py`
- `ui/backend/chain.py`
- activity graph, chain tree, route-console, and endpoint-skew tests

Acceptance:

- Node rows expose meaningful task identity, time, and text status.
- Absolute “as of” time and stale/error state are visible.
- A chain link is interactive only when its source can resolve it.
- Each node contributes one keyboard stop, not duplicate hidden controls.
- Error state offers retry/back navigation.
- A compact list provides an equivalent non-canvas representation.

Mockup:
`notes/research/2026-09-02-teams/mockups/2026-09-03-graph-recent-work-map.png`.

### Package 4b — Cycles / Audit timeline

Paths:

- `ui/frontend/src/routes/Cycles.tsx`
- `ui/frontend/src/components/CoordinatorCycleCard.tsx`
- `ui/frontend/src/design/ListRow.tsx`
- `ui/frontend/src/design/PeekPanel.tsx`
- cycle/card/phase tests

Acceptance:

- Render compact bounded rows with one selected full detail in a peek panel.
- Show all/status/date/search filters, labeled result count, and URL-backed
  active-filter chips.
- Keep the DOM bounded to at most fifty feed rows per page/window.
- Link dispatched iterations and only resolvable requests.
- Maintain one `h1` and honest loading/error/empty/stale states.
- Support keyboard row navigation.
- A normal dataset does not recreate the cached 154,246px document.

Mockup:
`notes/research/2026-09-02-teams/mockups/2026-09-03-cycles-audit-timeline.png`.

### Package 4c — Experiment detail / Outcome reconciliation

Paths:

- `ui/frontend/src/routes/ExperimentDetail.tsx`
- experiment-detail tests and shared chart/table primitives as needed

Acceptance:

- Label lifecycle, preregistered hypothesis result, and derived
  robustness/exploitation diagnostic as three distinct concepts.
- Put the authored/preregistered result first; keep the derived diagnostic as
  supporting evidence.
- Make opponent rows real keyboard-operable buttons or links.
- Direct-label chart values and use shape/pattern/text in addition to color.
- Use shared tokens rather than hard-coded chart hues.

Mockup:
`notes/research/2026-09-02-teams/mockups/2026-09-03-experiment-outcome-reconciliation.png`.

### Package 5 — cross-cutting tokens and remaining surfaces

- Add Tailwind role aliases in `design/tokens.css`/`index.css`.
- Migrate low-contrast zinc text first; do not combine this with a wholesale
  visual rewrite.
- Consolidate status/chip usage and add visible state words.
- Add `prefers-contrast: more`; retain reduced-motion behavior.
- Add a catch-all route and verify active navigation/`aria-current` behavior.
- Bound Dossier resolved history and expose its polling errors.
- Remove or bound the duplicated cycle-card stream on Experiments and link to
  Cycles.
- Channel preserves last-good content on polling failure and distinguishes an
  empty filtered result from an empty source.
- Inspector gains retry/back and source copy matching actual backend sources.
- Ladder keeps newest/owed activity high and links resolvable identifiers.
- Pulse exposes agenda/bubble counts and deep links without adding a competing
  queue vocabulary.

Token direction:

- Keep the R0 background, surfaces, borders, foreground, accent, spacing, and
  geometry.
- Normal text uses `--fg` or `--fg-muted`; hierarchy comes from size/weight,
  not sub-AA gray.
- Keep accent for interaction and status tokens for status only.
- Every state gets text/shape redundancy; hue alone is insufficient.
- Inventory, alias, and retire the legacy `src/tokens.css` only through the
  primary-managed mirror process.
- Do not add a light theme in this pass.

Candidate status colors, pending implementation and visual regression:

| Token | Candidate | Hex | Contrast on `--bg` |
| --- | --- | --- | ---: |
| `--status-ok` | `oklch(0.84 0.15 170)` | `#42e9bc` | 12.64 |
| `--status-warn` | `oklch(0.86 0.12 90)` | `#efce6f` | 12.67 |
| `--status-bad` | `oklch(0.62 0.19 10)` | `#df466c` | 4.83 |
| `--status-info` | `oklch(0.70 0.12 200)` | `#00b4bc` | 7.65 |
| `--status-idle` | `oklch(0.60 0.016 285.9)` | `#7f7f8a` | 4.90 |

### Documentation cleanup inside the UI session

- Correct the absent `.venv-ui` command in `ui/README.md:41-44`.
- Replace phantom backend endpoints in `ui/backend/README.md:20-26` and
  acknowledge the existing websocket.
- Update the route inventory in `ui/frontend/README.md:6-9`.
- Update the obsolete “active LOOP_V0” statement in `ui_plan.md:8`.
- Mark `OweStrip` retired or remove it and its tests in a separate proven
  cleanup.
- Reconcile `index.html:6` with the application wordmark.
- Reduce duplicate body-font ownership in `index.css:11-14`.
- Do not commit generated `dist/`.

### Future-session validation

- `cd ui/frontend && npm test`
- `cd ui/frontend && npm run typecheck`
- `cd ui/frontend && npm run build`
- `PYTHONDONTWRITEBYTECODE=1 .venv-chroma/bin/python -m pytest -q -p no:cacheprovider ui/backend/tests`
- Add focused keyboard, contrast-state, pagination, partial-failure, and
  route-console tests.
- Visually check 1440×900 and narrow/mobile layouts, all
  loading/empty/error/stale/success states, and keyboard-only completion of all
  three owner acts.

Definition of done: every P0 acceptance check passes; Graph, Cycles, and
Experiment detail match the intent of their reviewed briefs; no normal text is
below 4.5:1; no state depends only on color; long feeds are bounded; no write
crosses the path boundary; and every external seam is proposed rather than
silently implemented.
