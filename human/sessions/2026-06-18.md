# Session — 2026-06-18: push the 06-17 bundle + U1+U5 merge; U2–U4 work order (primary)

> Continues the 2026-06-17 autonomous run. That run's P1–P6 (orchestrator tutor
> engine + per-turn `chat` CLI seam, calibration writer + D-055, the cockpit seam
> wiring doc, the ml_intern advisory re-attach) **plus the UI session's U1+U5** are
> now MERGED and PUSHED to `origin/main` at **`b8ca85f`**. This note carries the
> remaining UI work order (U2–U4) for the UI session to pick up.

## State on `origin/main` (`b8ca85f`, in sync)

- **Orchestrator (P1–P6, 2026-06-17):** the no-verdict Qwen tutor engine + per-turn
  `chat` CLI seam (`finding_session chat start|turn --mode tutor|two_voice`); the
  `calibration_cli` pre-verdict writer (+ D-055 schema reconciliation); the
  authoritative `docs/cockpit_seam_wiring.md`; the ml_intern advisory re-attach.
  Orchestrator suite **1289** green.
- **UI (U1+U5):** `TutorPanel` rewritten to a real finding-overview (via the new
  read-only `GET /api/finding/{finding_id}` in `ui/backend/finding_detail.py`) +
  neutral mechanical outcome-effects + **unweighted** pros/cons + the D-044
  mis-citation fixed in the user-visible fence note; the `/todo` forms gated by
  `selected.kind` (the wrong-keyed-form bug). UI suite **1306 / 532** green. Gated
  (4 adversarial reviewers, 0 blocking) and merged at `b8ca85f`.

## Decisions awaiting the human (ratify or redirect)

- **D-054** (tutor verdict-fence) — affirmed + built enforced *by construction*.
- **D-055** (`calibration_entry` overloaded via a `phase` discriminator) — integrator
  default; redirect to a distinct event_type is small + reversible.
- **D-049** (β-interim bounded autonomy) — still DRAFT; ratify via
  `touch run_state/d049_ratified` + the crontab line.

## UI session work order — U2, U3, U4 (the cockpit's live seams)

> **Start by fast-forwarding the worktree to `origin/main` (`b8ca85f`)** — it FFs
> cleanly (`1556fbf` is now an ancestor of main). U1+U5 are already merged. The
> authoritative wiring spec is **`docs/cockpit_seam_wiring.md`** (verified against
> landed code by real CLI invocation) — wire to it, NOT to the old stub guesses.
> The UI session writes only `ui/` + `ui_plan.md`.

- **U2 — tutor chat pane.** Light up a live probing-chat pane (reuse the
  `TwoVoiceChatPane` stub pattern — the textarea/send UI exists) → a new `ui/backend`
  chat exec path that runs `finding_session chat start|turn --mode tutor` and parses
  the single-line JSON stdout envelope (`ok`/`mode`/`action`/`session_id`/
  `replies[].reply`). Enforce the fence in the UI: no verdict props, no confidence
  auto-fill, session-local. (Tutor mode rejects `--addressee`; the tutor-turn
  envelope omits `addressee` — read with `.get`. argparse arg-errors exit 2 with
  usage on stderr rather than the JSON envelope — tolerate that.)
- **U3 — two-voice pane.** Same chat exec path with `--mode two_voice` (+
  `--addressee defender|attacker|both`); flip `actions["two_voice_chat"]` True (the
  seam landed P2). This is the home for accept/deny adversarial support
  (defender = Gemma, attacker = Qwen).
- **U4 — cockpit seam execs (the `_SEAM_MODULES` corrections).** Per the wiring doc:
  `authorize_fix` → `orchestrator.authorize_fix`; `calibration` →
  `orchestrator.calibration_cli`; `directive_signoff` →
  `orchestrator.finding_session --set-status <FINDING_ID> … --directive` (keyed on
  **finding_id**, not iteration_id). `spawn_topic` / `abstain` → render as
  **session-exits** off the chat seam (NO one-shot writers); the in-UI exit mechanism
  (terminal REPL vs a narrow chat `end` verb) is the open U4 decision (wiring doc,
  open question 1). Preserve the SAFE-STUB invariant: the UI execs blessed CLIs, it
  never writes ledgers directly (D-046).
- **Doc backfill** in `ui_plan.md` (the thin tutor / two-voice / calibration component
  docs) as part of this round.

### Non-blocking nits from the U1+U5 review (fold into this round)

- Residual **D-044 mis-citation in a CODE COMMENT** at `Todo.tsx:329-330` (pre-existing;
  the user-visible note was already fixed) → cite D-053/D-054/rule 4.
- `CalibrationCapture` receives `selected.id` as `findingId` for `gate_verdict` items
  (an iteration_id under a finding-named prop) — reconcile when the calibration
  write-seam lands.
- Decide whether the tutor finding-overview should be visible **pre-calibration**
  (currently in the post-calibration aux section).
- Pin the `ui/backend` test-harness venv (tests pass under `.venv-chroma`; `.venv`
  lacks pytest, `.venv-staging` lacks fastapi).
- Optional: `_safe`-wrap the echoed `finding_id` / `source_iteration_id` in
  `finding_detail.py` if the path arg ever becomes row-derived.

When ready: print `UI READY TO MERGE`; the primary gates + merges.

## Carried-forward (human's call)

Interrogate the 5 cargo findings via the cockpit (the D-053 calibration question);
the D-051 sandbox MCP drive; Polymarket pre-resolution snapshots.

## Governance this session (2026-06-18)

- **Ratified D-054 + D-055** (human explicit sign-off): the tutor verdict-fence and the
  `calibration_entry`-via-`phase` overload stand as the integrator defaults.
- **Ratified + ARMED D-049** (β-interim bounded autonomy): sentinel
  `run_state/d049_ratified` created + crontab `0 9,15 * * * cron/run-coordinator.sh`
  installed. Bounded host-side coordinator cycles now LIVE (gated by pause-switch +
  daily budget cap + mem preflight + `NARA_SKEPTIC=1`); first cycle at the next
  09:00/15:00. Halt: `touch run_state/pause_coordinator`. Un-arm: `rm
  run_state/d049_ratified` and/or remove the crontab line.
- **Skill-signals handoff — reviewed + housekept (D-056).** A framework-side handoff
  (emit a runtime `skill_signals.jsonl` friction stream) was adversarially reviewed
  (4 reviewers + synthesis). Verdict **ADOPT-WITH-RECONCILIATIONS**: 2 hard inviolations
  pushed back (trigger-(a) imports the framework run-log enum → rule 6/4; firewall cited
  the framework `BOUNDARY.md` which we diverge from per D-032 → re-sourced to D-014) + 2
  reconciled (rule-7 swallow guard; D-048 `_no_live_artifacts` wiring). The apparatus-side
  contract is [`docs/skill_signals_contract.md`](../../docs/skill_signals_contract.md);
  **implementation of the `emit_skill_signal` helper is GATED** behind the human's go.
  (Framework replied "cleared to build after refinements"; our contract verified
  **self-standing** — no dependency on any unverifiable framework claim. Refinements
  still HELD pending the human's go: a real `.gitignore` gap for
  `run_state/skill_signals.jsonl`; commit trigger (a) to form (i); non-dropping
  skill-name validation; a self-standing acceptance line.)

## U2–U4 merged (e2b3b66) — cockpit live seams

The UI session's **U2** (live tutor chat) + **U3** (live two-voice) + **U4** (cockpit
exec corrections) merged after a 4-dimension adversarial gate (**MERGE, 0 blocking**) +
UI suites (vitest 1352 / pytest 605+) + a real `env -u MOCK_LLM` chat-seam smoke (tutor
turn → Qwen, D-046 no-write + the fence hold verified). Pushed `d87bbfd..e2b3b66`.

**3 UI-session flags ruled:**
- **spawn/abstain deferral — APPROVED.** Reshaped to honest session-exits (exec
  nothing, `available=false`). The in-UI-exit mechanism = **(i) terminal REPL for v1**
  (no new code); revisit the narrow chat `end` verb (option ii) only on a real need.
- **single-slot `calibratedId` → use a per-id `Set`.** A real latent double-write (a
  second blind `calibration_entry` on switch-away-and-back-resubmit — `calibration_cli`
  appends unconditionally). NON-BLOCKING today (the calibration POST is still a stub),
  but **must land before the P4 calibration seam goes live** — a per-id Set (UI) and/or
  read-time dedup (primary).
- **`calibration_cli` iteration_id `ref_id` — fine by design.** `ref_id` is the generic
  surfaced-item id (finding_id OR loop_memory iteration_id); calibration is an ordering
  gate before ANY verdict. No reject, no bad write.

**Pre-go-live follow-ups (next round):** the flag-2 Set/dedup before the calibration
seam goes live; a **research-design ruling** on overview-pre-calibration vs
uncontaminated-prior (does the static overview, shown before the blind prediction,
contaminate the §6.5.4 signal? — the UI session chose "overview = prediction basis,
pre-calibration"; needs a human/primary ruling); the shared tutor/two-voice
availability flag may need to fork if those seams ever split.

**UI copy refresh (next round — the seams are LIVE now, the copy lags):** the `/todo`
page header (`ui/frontend/src/routes/Todo.tsx:188-193`) still reads "NEW resolution
outcomes are STUBS until the cockpit seams land … surface their would-run argv
read-only and write nothing" — STALE after U2–U4. Update it to the current state:
tutor/two-voice chat live; authorize_fix/directive_signoff/sign_off/reject/defer/
gate_verdict exec real blessed CLIs; calibration wired (durable write lights when
`actions.calibration` flips); spawn/abstain are session-exits. Also sweep the now-stale
`// stub / would-run argv read-only / writes NOTHING` comments in `api/todo.ts`,
`AuthorizeFixForm.tsx`, `DirectiveSignOffField.tsx`, `CalibrationCapture.tsx`. (Bundle
with the flag-2 Set + the D-044 `Todo.tsx:329-330` comment nit as one small round.)
