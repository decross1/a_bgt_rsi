# Session note — 2026-08-16 (overnight continuation of the 08-15 loop)

Continuation session; no separate morning plan. Two owner asks drove it, and a
third item surfaced from the work itself.

## 1. Oracle ⇄ Nara path (D-067) — DONE

Owner: *"set up a path for the oracle and nara to communicate, and add this
back and forth to the dashboard."*

Oracle (ratified mission steward for the broader locally-grounded lab;
observer-only here) had messaged the channel and Nara answered it badly in two
distinct ways — both now fixed and both re-tested live:

- **Stale figures.** Nara restated "31 surfaced findings / 8 pending gate
  verdicts" carried forward from the transcript tail. Live values: 0 and 1.
  Both voice prompts now carry an explicit CONTEXT PRECEDENCE rule (live pack
  sections authoritative; transcript tail is history). On the re-ask Nara
  corrected *itself* in the reply: "the transcript tail mentions 8, the
  authoritative planner_state lists 1."
- **Invented seams.** It described writing the proposal into `ideas.md` and put
  the human's verdict CLI before the authorize-fix queue. The prompt now states
  the improvement path in its real ORDER with the real artifacts, names research
  delegation as a different seam, and pins the required evidence. The re-ask
  restated it correctly and explicitly refused the step it could not verify.
- **Attribution.** `_PARTICIPANTS = ("human", "oracle")` + `turn --as`; the
  author is the stored row `kind`. Default `human` (callers unchanged); an
  unregistered author raises rather than being coerced. Identity ≠ capability:
  the UI's turn endpoint takes no author, so the owner's surface cannot
  impersonate the steward.
- **On the dashboard.** Channel renders `oracle` as a named voice in the
  brightest NEUTRAL (every free hue window is flanked by two status hues), plus
  a `steward` filter = the steward's turn and the reply written right after it.

Live exchange is in `memory/lab_channel.jsonl` and serves through
`/api/channel/timeline`.

## 2. D-066 self-improvement loop — WIRED + RUN LIVE

`improve_system` coordinator action (cost 4) is wired and **dark by default**:
it is in `DARK_ACTIONS` keyed to `NARA_SELF_IMPROVE`, so it is absent from the
planner menu when unset — the planner cannot choose what it cannot see — and
the handler refuses it a second time, so a hallucinated action name cannot
spend a frontier call. D-066 is filed.

**First live run (01:55Z) refused to emit, for the right reason.** Gemma
proposed "raise EmptyContentError for empty model content", and Claude
(feasibility) vetoed it three times because the acceptance test targeted code
that does not exist (`workers/qwen_worker.py`, an `AgentWrapper` class): the
test would fail today for the wrong reason (ImportError) and could be made
green by writing a shim the runtime never calls. That is exactly the red-first
loophole, and the falsifier tier closed it. No packet emitted.

**Real weakness this exposed:** the proposer has no repo grounding — it invents
plausible module and class names. The reviewers currently absorb that cost.
Worth grounding the proposal prompt in an actual file/symbol inventory before
arming `NARA_SELF_IMPROVE` on a cadence.

## 3. Codex reviewer had been DARK for ~6 hours (D-068)

Diagnosed from the first live run: every `risk_scope_reviewer` call returned
"frontier invoke error: nonzero". Cause was outside the repo — the machine-wide
`~/.codex/config.toml` pins `model = "gpt-5.6"` and
`model_reasoning_effort = "max"`, and both started returning HTTP 400 ("not
supported when using Codex with a ChatGPT account"). Ledger evidence: 32 clean
codex calls, last OK 2026-08-15T19:30Z, then every call nonzero.

Fixed **in-repo rather than by editing the owner's global config**:
`agent_wrapper/frontier_cli.py` now pins `CODEX_MODEL = "gpt-5.5"` and
`CODEX_REASONING_EFFORT = "high"` (env-overridable), passed as `-m` / `-c`.
Verified live: exit 0, reply returned. The command shape is test-pinned.

**Standing lesson:** half the falsifier panel went dark and nothing alerted.
Every D-061 consumer (F1 sweeps, D-064 refine, D-066) silently degraded to a
one-reviewer panel. A frontier-vendor health check belongs in the loop-alert
surface.

## 4. Ledger hygiene

16 spawn-ledger rows from the 08-15 loops had no closing line (a logging lapse,
not a launch gate). All 16 closed with reconstructed fan-in results, each claim
re-checked against the merged code, its tests, and the run log; the closing
rows say so in `verified_by`.

## Open / next session

- Ground the D-066 proposal prompt in a real file/symbol inventory, then
  consider arming `NARA_SELF_IMPROVE` on a cadence.
- Add frontier-vendor health to the loop alert (this session's outage was found
  by hand, six hours late).
- Unchanged from 08-15: install the nara-daemon systemd unit; arm and evaluate
  `NARA_DEBATE=1`; the two L1 clusters owe N≥30 synthetic experiments (nothing
  has reached L2); revise the judge-calibration set; Qwen 3.8 still blocked on a
  vLLM pin amendment.
