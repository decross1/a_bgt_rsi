# 2026-08-19 — cockpit test-drive feedback: make the loop's own criticism feed back

Primary session (claude-code-main), continuous from the 08-18 arc.
Session model switched to Opus 5 mid-session (Fable 5 monthly spend
limit killed 8 workflow agents mid-research; no partial writes — tree
verified clean at 63ba00b — and all lanes relaunched).

## Focus agreed

The owner began test-driving the cockpit (a real two-voice finding
session on exp006) and reported four things. All four became build
lanes, plus one research lane the session proposed:

1. **Model I/O reads as noise for sessions** — 3 questions rendered as
   6 cards, each repeating the whole replayed stack (the finding-session
   engine is stateless replay). → thread cards with stance lanes and
   per-turn deltas.
2. **"How does frontier criticism feed back into the system?"** — the
   screen's veto REASONING (which names the exact missing controls)
   conditions nothing today; the verdict only gates promotion.
   → deterministic constraint distiller + DARK conditioning seam +
   `--propose` follow-up research questions (D-061 intact: frontier
   annotates, humans accept, nothing generates).
3. **"If we don't kill an idea, how does it yield a follow-up?"** — the
   plumbing exists end-to-end (end_session spawn_topic →
   finding_followups.jsonl → daemon → coordinator topic) but is
   invisible in the UI, and the 10 frontier agenda proposals have NO
   consumer. → agenda accept/dismiss CLI + cockpit close-out strip.
4. **Two live defects** — a second false-positive `loop_stalled` RED
   (fired 03:32 while the owner's own session held the apparatus and
   cycles were iterating normally at 02:54/03:00), and the two-voice
   defender answering "Since I am playing two roles…" instead of
   defending. → gated-idle signal separated from genuine stalls;
   defender seed tightened to one voice.

## The research lane this session proposed (owner ratification pending)

Funnel state measured today: **132 clusters, 20 open (18 L0, 2 L1), 112
killed — and 89 of those kills are `redteam_fatal_flaw`, every one of
them made BEFORE the D-076 swap**, i.e. by the instrument the R1a
battery measured condemning 6 of 7 parsed sound claims. Every one of the
89 carries `reopening_condition: {requires: new_evidence, evidence_kind:
"redteam_proceed_on_revision"}` — a redteam proceed IS the ledger's own
specified reopening evidence, so the mechanism needs no governance
change; only the decision to use it does.

→ Lane 5 pre-registers and builds (does NOT run) a **paired old-vs-new
re-adjudication battery** over the 89 claims with spiked known-bad
controls from the R1a frozen manifest. Measurement is read-only; the
reopening is a separate owner-ratified step with disposition options
pre-stated before any number exists.

## Notes / carry-forward

- The frontier screen has been healthy since the PATH fix (all exit-0
  overnight, 19:05–20:02Z).
- Budget 3/60 today; loop iterating (iter-2026-08-19-001/002).
- Parked: D-066 proposer (owner's Oracle↔Nara channel), D-073 T1
  license gate, vLLM #52197 / v0.27.2 watch triggers.
