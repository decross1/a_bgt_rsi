# Session — 2026-06-28: P1 un-stall autonomy (memory) + P3 Loop-3 + P2 sign-off (UI work order)

> Primary session, **ultracode** (xhigh + dynamic-workflow orchestration). Executes the
> approved plan: **P1** 100%-fix the stalled autonomy with the memory constraint figured
> out, then **P3** close Loop 3 to applied and **P2** validate the cockpit sign-off UX
> (a UI-session back-and-forth). Live model/coordinator/experiment runs are done **inline**
> (serial, memory-bound — the Dynamic-Workflow discipline bars workflow agents from
> run_state/spine, and parallel model calls would recreate the very OS-starvation P1 fixes).
> A read-only Dynamic Workflow (`wf_124300a7-105`, 4 agents) produced the P2/P3 prep below.

## P1 — Un-stall daily autonomy

### Memory constraint — DIAGNOSED + FIXED (D-057)
The D-049 runway refused every cycle 2026-06-26 → 06-27 (`MemAvailable=22GiB < 30GiB`).
**Structural cause, not just the stray sandbox:** the two NVFP4 27B servers pin the
GB10 unified pool via `--gpu-memory-utilization` — Gemma 0.40 (~49 GiB) + Qwen 0.25
(~30 GiB) ≈ 79 GiB; **weights are ~18.6–18.9 GiB each (incompressible)**, only KV is slack.
Per the vLLM startup profiles: Gemma@0.4 carried **26 GiB / 21.87× KV** (oversized for
single-stream orchestration); Qwen@0.25 only 6.87 GiB (its weights dominate).

**Fix:** Gemma **0.40 → 0.30** (now 12 GiB KV / 10.1× concurrency — ample), Qwen untouched;
stopped the orphaned unhealthy `openshell-nara-sandbox`. → **MemAvailable 22 → 35 GiB**,
`preflight_mem.sh 0` PASSES. MARLIN MoE re-verified (rule 2); **30 GiB margin HELD, not
lowered** (rule 7). Captured the exact production launch for both servers in
[`cron/serve-models.sh`](../../cron/serve-models.sh) (closes the un-scripted `docker run`
gap; both `--restart unless-stopped`). Logged as **D-057**.

### Promotion fix verification (the 06-25 advisory flip)
Ran one real `coordinator --once --execute --budget 6` cycle inline
(`NARA_PROMOTION_VOTE_ADVISORY=1`) to confirm a finding actually promotes now (the 06-25
flip had never run — the cycle was dead). **Result: ✅ FIXED — `surfaced_findings` 5 → 10
(5 promoted)** (`run_id=coordinator_ba1afd05`). All 5 are `novel`+`critic=survives` that the
adversarial vote refuted 3/3 — promoted anyway with the refutation **annotated**
(`promotion_vote_advisory: {n_refuted:3, n_voting:3, survived:false}`), exactly D-053's
design: real cargo reaches the cockpit, the human is the calibration. (Quality note: 3 of
the 5 — `sf-iter-2026-05-27-012/013/014` — are near-duplicate "noise accelerates breakdown"
hypotheses from 05-27; triage in P2, not a blocker.) The daily 09:00/15:00 cron will now
promote rather than starve.

## P3 — Close Loop 3 to applied (READY; from the prep workflow §3)
Seed `iter-2026-06-19-003` (novel; critic survives; redteam flagged unfalsifiable but
novelty stands) verified present; exp001/003/004/007 bridges + results all present;
`autoresearch`/`replication_driver`/`nara.run_iteration` wiring complete. **No orchestrator
blockers.** Ordered plan (real runs `env -u MOCK_LLM`, inline/serial):
1. Dry-run (no model): `python -m orchestrator.autoresearch --tier synthetic --experiment exp001_repeated_pd --replicate`
2. Live Phase 1: add `--live` (exp001 + exp003×exp004 cross-rung) → new `iter-…` + journal.
3. Live Phase 3 (applied): `… --tier applied --experiment exp007_polymarket --live`, then
   `experiments.exp007_polymarket.refine_cli start|turn` (design-only, zero trading, D-018).
4. Cockpit review → P2 sign-off.

**⚠ Investigate before relying on exp007:** its summary reports `verdict=BELOW_MARKET`,
`bss=-4.53e9` — an absurd Brier Skill Score (degenerate/divide bug, not a real metric).
The applied-step forecast is suspect until this is run down. Does **not** block the
synthetic Loop-3 chain (steps 1–2); flagged as a P3 caveat.

## P2 — Cockpit sign-off UX

### UI-touching backend / spine items — **NONE required** (the headline you asked for)
The prep workflow flagged one BLOCKING-HIGH candidate: does `calibration_cli` accept an
`iteration_id` (gate-verdict items), not just a `finding_id`? **Adversarially verified —
false alarm.** `orchestrator/calibration_cli.py` treats `ref_id` as **opaque** (validates
non-empty only, no finding lookup); `schema/calibration_pre_verdict.schema.json` types it
`{string, minLength:1}` with **no pattern** (description: "surfaced finding / loop_memory
id"). Live probe: `append_calibration('iter-2026-06-19-011', …)` → **ACCEPTED**. So the
cockpit calibration surface already works for all 9 items.
- **Spine:** untouched — `schema/events.jsonl.schema.json` D-055 reconciliation already
  landed; `iteration_record.schema.json` / `nara.py` / `tool_registry.py` need nothing.
- **ui/backend:** `_SEAM_MODULES` already corrected (`todo_cockpit.py:56-63`). The three
  writers (`gate_cli`, `finding_session`, `calibration_cli`) verified SOUND, round-trips
  intact, rule-4/6 enforced. **No primary-side backend work blocks the sign-off.**

### Apparatus-side P2 checklist (primary) — verified, no code change
Gate verdict (`gate_cli` → `loop_feedback.jsonl`), finding interrogation (`finding_session`
→ `surfaced_findings.status.jsonl` + `loop_feedback`), blind calibration (`calibration_cli`
→ `events.jsonl`, confidence∈[0,1] rejected-not-clamped) all sound and tested
(`tests/test_calibration_cli.py`, `test_finding_session*.py`, `test_events_schema.py`).

## UI session work order — cockpit sign-off UX (S2 polish, `ui/` only)

> Co-authored per the operating contract. Start with `git pull`/FF to `origin/main`, then
> `env -u MOCK_LLM claude --worktree ui-session`. Writes only `ui/` + `ui_plan.md`.
> **This is polish/clarity/trust — NOT a rebuild** (the S2 reframe landed in `782fc87`).
> **No backend/spine dependency** (verified above) — the UI session is unblocked.

**Priority 1 — clarity & messaging:**
- `ui/frontend/src/routes/Todo.tsx:363` — fix the D-044 citation: "D-044 governs the
  two-voice interrogator's independence (Qwen attacker independent from Gemma defender)."
- `Todo.tsx:248-270` (header explainer) — add: "Optional blind calibration — record a
  prediction before interrogation if you want to, but you can decide without it."
- `ui/frontend/src/components/todo/CalibrationCapture.tsx:128-135` — rewrite the
  `available=false` banner: "calibration capture is not available in this environment (the
  CLI is not reachable). Your input below is a preview only and will not be recorded."
- `ui/frontend/src/components/todo/TwoVoiceChatPane.tsx:306-312` — unify the capability-off
  banner with the same "not available … preview" wording.
- `ui/frontend/src/components/todo/PipelineJourney.tsx` — add a stage banner above the
  ribbon labeling **literature-stage** vs **applied-tier** (infer from
  `iteration.experiment_outcome` present→applied / absent→literature). *(Frontend inference
  is fine; no backend stage field needed — primary confirms.)*
- `ui/frontend/src/components/GateVerdictForm.tsx:90-125` — outcome guidance: "valid =
  approved, loop advances · needs_revision = paused for refinement · invalid = rejected."
- `ui/frontend/src/components/FindingReviewForm.tsx:94-129` — outcome guidance: "validated
  = sign off · in_review = keep interrogating, stays in queue · rejected = dismiss."

**Priority 2 — comment hygiene:** remove stale "would_run"/"stub" envelope language
(simplify to "preview-only") in `AuthorizeFixForm.tsx`, `DirectiveSignOffField.tsx`,
`AbstainForm.tsx`, `api/todo.ts`.

**Acceptance:** D-044 fixed; capability-off messaging consistent; calibration-optional
surfaced; stage label rendered; verdict/status guidance rendered; cruft removed; vitest
green; one real `env -u MOCK_LLM` smoke against `:8700` (select a gate_verdict item →
calibrate → reveal interrogation → submit verdict → item leaves the inbox).

When ready: print `UI READY TO MERGE`; primary gates + merges `--no-ff`.

## Status / next
- P1 memory: ✅ done (D-057, serve-models.sh). Promotion: ⏳ cycle running (result above).
- After the cycle: commit P1 (cron/serve-models.sh, DECISIONS D-057, this note) → then the
  owner spawns the UI session for P2 polish, and primary runs P3 Loop-3 inline.
- The 9 backlog sign-offs remain the owner's calls once the UX polish lands.
