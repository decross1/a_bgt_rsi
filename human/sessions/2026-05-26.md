# Session — 2026-05-26

## Focus

Reset the operating model. The track-A/B/C/D parallel-execution
framework, autonomy-tier machinery, and 90-day phase roadmap had
grown into ~10,600 words of process governing a ~2,300-LOC product
that has not yet run the eight-step intelligence loop end-to-end as
a chained iteration. Drop it. Move to one primary session + an
optional concurrent UI session, with a per-session working note
(this file) as the live plan.

Lock in the next slice: **LOOP_V0** — the literature-only half of
the intelligence loop. Spec at [`../../LOOP_V0.md`](../../LOOP_V0.md).

## Plan

Documentation reorg only, this session. No code. The reorg lands
the new operating model so subsequent sessions can build LOOP_V0
without re-litigating scope.

- Archive the retired scaffolding under `archive/`:
  - `agent/orchestration.md`, `collision_protocol.md`, `ownership.yaml`,
    `autonomy.md`
  - `agent/prompts/track_{a,b,c,d}.md`, `dispatched_task.md`
  - `PHASE_1_ROADMAP.md`, `current_day.md`, `week2_plan_seed.md`
  - `plan.yaml` (whole)
  - Per-day and per-track notes in `notes/`
- Rewrite `CLAUDE.md` — drop track rules + tiers, keep inviolate
  basics, point at LOOP_V0 + this session note.
- Rewrite `START_HERE.md` — new doc map, no phase roadmap.
- Write `LOOP_V0.md` — active build plan replacing `plan.yaml`.
- Write `agent/prompts/main.md` (replaces `track_a.md`) and
  `agent/prompts/ui_session.md` (the concurrent UI session).
- Log decision in `DECISIONS.md` (D-030 or next available).
- Update agent memory with the new operating model.

Parallel UI session this session: **no.** The doc reorg is sequential
work that touches files outside `ui/`.

## Outcomes

- Archive structure created: `archive/{agent,agent_prompts,notes,roadmap,plan}/`.
- All 9 track/tier scaffolding files moved (orchestration, collision_protocol,
  ownership, autonomy, 4 track prompts + dispatched_task).
- `PHASE_1_ROADMAP.md`, `current_day.md`, `week2_plan_seed.md`,
  `plan.yaml` (4,037 lines) moved to `archive/`.
- 24 per-day / per-track notes moved to `archive/notes/`.
  `notes/research/` retained (real research material, not planning).
- `CLAUDE.md` rewritten: 10 inviolate rules retained, track/tier
  framework removed, single-session operating model documented.
- `START_HERE.md` rewritten with new doc map.
- `LOOP_V0.md` written: 6-step iteration spec, iteration_record schema
  sketch, build-order session ladder, exit criterion.
- `agent/prompts/main.md` (new primary session prompt) and
  `agent/prompts/ui_session.md` (new UI session prompt) written.
- `agent/README.md` updated to point at new structure.
- `DECISIONS.md` D-030 logged: scrap parallel-track / tier framework;
  move to single-primary + optional-UI sessions; LOOP_V0 is active
  slice.
- Memory saved: operating-model shift.

---

# Part 1 build — Nara substrate (later in same day, 2026-05-26)

After the doc reorg the user redirected to start LOOP_V0 immediately
with two new requirements: (1) Nara should run on NemoClaw if possible
(eventually); (2) Nara should inherit the local `agent_system`
framework. The session re-planned and built the substrate Part 1
described — runtime abstraction + tool registry + Nara hello-world.
The 5 LOOP_V0 workers slip to Part 2.

## Plan (Part 1)

(See `/home/decross1/.claude/plans/idempotent-spinning-sonnet.md` for
the full plan; approved after two rounds of user push-back on linear
vs. tool-using orchestration and UI parallelism.)

Decisions baked in:

- NemoClaw: 90-min cap; if install works, build NemoClawRuntime; else
  PyRuntime only with stub.
- agent_system: full skill set (24 skills + 4 agent profiles), symlinked.
  Diverges from BOUNDARY.md — D-032 logs why.
- UI session in parallel from the start (separate worktree); UI builds
  panels against fixtures, swaps to real files at end of day.
- Trigger button: UI's NaraPromptForm POSTs to /api/loop_v0/start which
  shells out to `python -m orchestrator.loop_v0_cli`.
- Nara emits brief narration between tool calls; UI renders it.

## Outcomes (Part 1, primary session)

- **agent_system installed**: `agent_system/install.sh --target-path
  /home/decross1/projects/a_bgt_rsi/.agents/skills --filter all`
  symlinked 24 skills. Agent profiles linked manually under
  `.agents/agents/`. `.agents/skills/` and `.agents/agents/`
  gitignored; `.agents/README.md` tracked.

- **NemoClaw deferred** (D-031). 10-min investigation: NemoClaw is no
  longer alpha-broken; installer at `nvidia.com/nemoclaw.sh` works on
  DGX Spark and all prerequisites are met on this host (Docker 29.2.1,
  Node 22.22.2, NVIDIA_API_KEY in env, GPU + CUDA 13.0). The blocker:
  the installer needs sudo for `nvidia-ctk cdi generate` and this host
  requires a password (not passwordless). Autonomous session can't
  enter passwords. Documented in D-031 with the install command for a
  future human-attended session. NemoClawRuntime stays a stub.

- **Runtime abstraction** (`orchestrator/runtime.py`): Protocol +
  PyRuntime + NemoClawRuntime stub. PyRuntime dispatches via in-process
  callable registry; atomic-write state I/O; appends to
  `run_state/week1.run.jsonl`.

- **Tool registry** (`orchestrator/tool_registry.py`): 4 hello-world
  tools as both Python callables (TOOL_REGISTRY) and OpenAI specs
  (TOOL_SPECS):
  - `summarize_paper` (existing)
  - `play_pd_match` (existing)
  - `query_chroma` (new helper)
  - `journal_writer_stub` (new finalizer)

- **Chroma query helper** (`orchestrator/chroma_query.py`): queries
  `papers_recent` + `osborne_rubinstein` in one call, merges by score,
  SHA-256 content_hash at query time. Lazy-loads + caches BGE-M3
  embedder.

- **Schemas** (`schema/iteration_record.schema.json`,
  `schema/active_iteration.schema.json`): iteration_record has minimal
  required set for Part 1 (`iteration_id, started_at, ended_at, seed,
  nara_summary, tool_calls_made, journal_entry_path, model_version,
  wrapper_call_ids`), with optional fields ready for Part 2
  (`hypothesis, retrieval, novelty, critique`). novelty.class ∈ {novel,
  rediscovery, nonsense, unclear}; critique.verdict ∈ {survives,
  falsified, restated, malformed}.

- **Nara orchestrator** (`orchestrator/nara.py`): custom multi-turn
  tool loop using wrapper's `_sync_client` + `_emit` (not
  `call_with_tools`, because we need text_content separately for
  narration). System prompt today constrains Nara to: pick ONE of
  {summarize_paper, play_pd_match, query_chroma}, emit narration
  before the call, end by calling `journal_writer_stub`. Writes
  `run_state/active_iteration.json` atomically at every step;
  deletes on completion.

- **CLI** (`orchestrator/loop_v0_cli.py`): `env -u MOCK_LLM
  ./.venv-chroma/bin/python -m orchestrator.loop_v0_cli --topic "..."`.

- **Tests pass**: 11/11 in `tests/test_runtime_interface.py` +
  `tests/test_tool_registry.py`. (No new test added for nara.py
  because the smoke test below is the integration check.)

- **End-to-end smoke**:
  ```
  env -u MOCK_LLM ./.venv-chroma/bin/python -m orchestrator.loop_v0_cli \
      --topic "Tit-for-Tat dominance in repeated Prisoner's Dilemma"
  ```
  Result `iter-2026-05-26-001`, 24 seconds wall-clock:
  - Nara picked `query_chroma` (correct — retrieval-shaped topic).
  - Retrieved 10 real O&R Ch. 8 chunks; summarized intelligently
    citing Nash, Grim Trigger, subgame perfect equilibrium.
  - Called `journal_writer_stub` to finalize.
  - Wrote `journal/iterations/001.md` (readable).
  - Appended schema-valid row to `run_state/loop_memory.jsonl`.
  - 3 wrapper calls in `logs/calls.jsonl`, chained by
    `parent_request_id` from `iter-2026-05-26-001`.
  - 5 `loop_v0_*` events in `run_state/week1.run.jsonl`.
  - `narration_log` captures Nara's per-turn commentary.

- **DECISIONS.md**: D-031 (NemoClaw deferred) + D-032 (BOUNDARY
  divergence for full-skill install).

## Known quality issues (not breaking, not in scope today)

- Gemma 4 leaks `<channel|>` markup into summary text occasionally.
  System-prompt tighten in Part 2 should clean it up.
- Cold model load adds ~3s to first wrapper call. Cached on
  subsequent calls.
- Nara's narration_log second entry was `"thought\n<|channel>thought\n<channel|>"`
  — same markup-leak issue.

## Next session — Part 2 (build the 5 LOOP_V0 workers)

The substrate is ready. Part 2 mechanically extends:

1. Update `NARA_PROMPT_V0` to mandate the 5-step chain
   (hypothesize → retrieve_literature → novelty_classify →
   critic_loop_v0 → journal_writer).
2. Build the 5 workers (~100-150 LOC each):
   - `workers/hypothesize.py`
   - `workers/retrieve_literature.py` (wrapper around the existing
     `orchestrator/chroma_query.py`, with a worker-contract shape)
   - `workers/novelty_classify.py`
   - `workers/critic_loop_v0.py` (DO NOT import the salvaged
     `workers/critic.py` — Phase-2 contract, different verdict shape)
   - `workers/journal_writer.py` (replaces the stub; accepts the full
     iteration_record)
3. Add them to TOOL_REGISTRY + TOOL_SPECS.
4. Tighten the system prompt to suppress `<channel|>` markup.
5. Run 3 real iterations on 3 different topics; eyeball quality.

UI session (parallel, separate worktree, today):

The UI session is expected to land alongside this work — agent prompt
at `agent/prompts/ui_session.md`. Today the UI builds panels against
fixtures (the schemas are now stable so fixtures won't churn) and the
backend POST endpoint that shells out to the CLI. End of day: human
types topic in dashboard, watches Nara hello-world run.

## Open questions still

- `run_state/week1.*` filenames remain stale; rename in a small
  cleanup session.
- The salvaged `workers/critic.py` (Phase-2) and the LOOP_V0
  `workers/critic_loop_v0.py` collision: rename Phase-2 to
  `critic_phase2.py` later, or live with `_loop_v0` suffix.
- NemoClaw: run `curl -fsSL https://www.nvidia.com/nemoclaw.sh | bash`
  in a normal terminal (typing the sudo password); then implement
  `NemoClawRuntime.dispatch_tool` (~150 LOC).
