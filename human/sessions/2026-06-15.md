# Session — 2026-06-15: cockpit seams + D-053 reframe + UI cockpit merge (autonomous run)

> Primary session, continued from 2026-06-14. Human directive: do the work as
> dynamic workflows, max fan-out, ~60 min wall for the C task; ultracode on. A UI
> session ran concurrently and is now merged. Everything below is committed +
> pushed to `origin/main`.

## What landed (all committed + pushed, origin in sync at `8d74171`)

1. **Doc consolidation** (`0ca6445`, earlier) — one home per concern; `~/.claude/plans/`
   policy; handoffs → the session note; archived the executed `docs/*handoff*`;
   imported load-bearing stranded plans → `archive/plans/`. (The UI session later
   merged its deferred `ui_plan.md` link fixups — now consistent.)
2. **`/todo` cockpit PRIMARY seams 1–4** (`2f4ceaf`) — two-voice interrogation +
   directive sign-off (`finding_session.py`), generalized escalation
   (`coordinator.py` + `schema/escalation.schema.json`), authorize-fix + spawn-contract
   enqueue (`orchestrator/authorize_fix.py`). Built via workflow; full suite green;
   real smoke (attacker 2753-char attack at `max_tokens=4096`).
3. **Over-gating/promotion analysis** (`2d2862c`, `docs/overgating_promotion_analysis.md`) —
   verified: R0 over-gating never starves cargo (downgrade fired 0/59); the binding
   constraint is the **promotion stage** (the adversarial vote refused 3/3 +
   `max_candidates`).
4. **D-053 reframe** (`1817171`) — human chose **option C**: demote BOTH the primary
   R0 topicality gate (`NARA_R0_ADVISORY`) and the adversarial promotion vote
   (`NARA_PROMOTION_VOTE_ADVISORY` + `NARA_PROMOTION_MAX_CANDIDATES`) to non-gating
   advisories, **env-gated DARK + reversible** (byte-identical default, proven 3 ways).
5. **UI cockpit merge** (`8d74171`, `--no-ff` from `worktree-ui-session`) — dashboard
   reframe (system-overview center; idle-hero coupled to the A+B pending count;
   HumanTodoPanel → `/todo`; HostMemoryTile; TopicalityAdvisoryBadge) + the `/todo`
   uncertainty-resolution cockpit (two-voice pane, 6 resolution forms, pre-verdict
   calibration capture, tutor fenced from the verdict, concurrency warn). 3 adversarial
   auditors (no blockers) + full UI gate on merged main (frontend 1255, backend 517).

## Cargo experiment result (D-053) — the cockpit now has cargo + a calibration question

Ran both gates ON (real): **5 findings surfaced** to `memory/surfaced_findings.jsonl`
(local, gitignored). **All 5** carry `promotion_vote_advisory = {n_refuted:3/3,
survived:False}` — the adversarial vote refuted **5/5** while the threshold passed
**5/5**: a fully discordant gate (consistent with the suspected refute-everything /
register bias). The advisory unmasks the disagreement instead of hiding it. **The
calibration is now the human's, via the cockpit** — interrogate the 5 and decide
which signal to trust. The default pipeline stays dark.

## Follow-ups (none blocking)

- **Wire the cockpit backend stub → exec the seam CLIs.** `ui/backend/todo_cockpit.py`
  is a SAFE STUB today (validates + echoes `would_run`, execs/writes nothing). The
  seam CLIs now exist on main; wire the forms to exec them (correct the `would_run`
  argv: `orchestrator.authorize_fix authorize-fix`; `finding_session --set-status
  … --directive`; etc.). This makes the cockpit functional end-to-end. (UI-session +
  the contract already in `docs/human_writeback_contract.md`.)
- **Interrogate the 5 cargo findings** via the cockpit (the calibration question above).
- `ml_intern` advisory re-attach (~6 lines, D-052 known gap) — still open.
- D-049 ratification — human's call. D-051 sandbox MCP drive; Polymarket pre-resolution
  snapshots — still open.

## Note on the operating discipline this run

A UI session ran concurrently the whole time; every workflow + commit avoided `ui/`,
`ui_plan.md`, and the named spine. The UI session left its work uncommitted under the
READY-TO-MERGE sentinel; the primary committed it on its branch (`c36d799`, ui-scope
only) and merged after the reframe landed (clean, disjoint).
