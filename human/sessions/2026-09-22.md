# 2026-09-22 — Flash at 262K context; Oracle ⇄ Nara mailbox phase 1

## Goals set by the owner

1. Bring up Flash with the best context length inside the 10 GiB headroom. **Done.**
2. Then set up a mailbox between Oracle and Nara, with Nara as the lab's
   implementor and Oracle as planner and lab/UI/Nara-capability developer.
   **Phase 1 built; Nara's lane not run or armed (permission gate).** See D-082.

## Goal 1 — context

- Built `bench/flash_context_eval` (driver, probes, tests) and eval-only helper
  and adapter copies; the owner ran the helper generator after the classifier
  blocked it.
- 64K and 131,072 never start: the pinned container runtime admits only 32,768
  or 262,144.
- 262K first failed on the kernel guard (driver big-page retry lines during the
  load). Adding a startup page-cache evictor removed the lines; 262K then passed:
  lowest MemAvailable 21.5 GiB with a 241K prefill, 100% needle recall at
  8K/97K/241K, cold TTFT 3.7/48/130 s, 1.05 s cached, ~40 tok/s decode (same as
  32K). A short request queued behind a 241K prefill waited 128 s.
- Production cutover v7 → v8 at 11:05 UTC; ready 11:18 with no driver lines. Nara
  restarted with current code and its preflight passes.

## Goal 2 — mailbox

- `orchestrator/oracle_mailbox.py`: attributed, hash-chained, append-only
  Oracle ⇄ Nara log with a CLI. Its tests pass.
- `orchestrator/nara_lane.py`: Nara's implementor lane (fence, red-first test,
  local builder, bubblewrap sandbox, scope check, branch, receipt; never merges).
  The owner ran `tests/test_oracle_nara_mailbox.py`; three bugs were fixed (sandbox
  mount order; git cwd bound at import time, which put test branches in the lab repo,
  since removed; builder calls not persisted). Lane tests 8/8; full suite 5578 passed.
  One real item was validated end to end (local Flash builder, bwrap sandbox) in a
  throwaway clone. Units `systemd/nara-lane.{service,path,timer}` are written but
  installing them was blocked by the permission classifier. The owner installed and
  enabled them at 16:48 UTC; the lane is armed and idles until Oracle posts an item.
- Protocol: `docs/ORACLE_NARA_MAILBOX.md`; pointer in
  `oracle_system/docs/NARA_MAILBOX_2026-09-22.md` (Oracle's AGENTS.md untouched).
- Design review and mapping: `notes/research/2026-09-22-oracle-nara-mailbox/`.

## Owner decisions pending

1. Allow the lane to run (tests, then a supervised first item, then a service).
2. Oracle-side changes in `oracle_system`: protocol doc, charter amendment,
   Pi mailbox rebind, planner 20 GiB floor and 32K context sizing, Pi 262K metadata.
3. UI work order: an Oracle ⇄ Nara panel, and work cards/accomplishments projected
   from lane receipts.
4. Guard rule for driver retry lines (carried from 2026-09-21).
5. Commit: the running v8 pin, 262K config and all of the above are uncommitted.

## UI session work order

Add a read-only Oracle ⇄ Nara mailbox panel to "Today's research path" (fold of
`run_state/oracle_nara_mailbox.jsonl` via `python -m orchestrator.oracle_mailbox fold`),
and later project work-card status and accomplishments from Nara receipts plus the
integrator's merge, never from prose.
