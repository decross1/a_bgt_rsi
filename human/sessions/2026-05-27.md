# Session — 2026-05-27

## Focus

Continuation of 2026-05-26's reference-passing priority, but the human asked for a real investigation of two-model serving before more LOOP_V0 work landed — the Co-Scientist insight (critic on a different model than the generator) is load-bearing for the apparatus and the architecture's silence on multi-model co-residency had become a hidden risk. Outcome: substrate slice from yesterday committed, then a deep research + adversarial-strawman pass on Option A, then Phase 0 cohabitation verified empirically, then the reference-passing refactor landed on top of the verified substrate.

## Plan (agreed)

1. Commit the uncommitted multi-backend substrate (D-035) + `research_program_v2.md` from 2026-05-26.
2. Plan mode — `peaceful-brewing-seal.md` — to resolve the two-model serving question with evidence rather than commit Option A blindly.
3. Phase 0 — verify joint Gemma + Qwen serving on the Spark.
4. Phase 1 — reference-passing refactor (independent of model decision).
5. Halt for the human to drive Phase 2 (3 real iterations on Gemma-only baseline).

## Outcomes

### Substrate slice landed (commit `b50e5e3`, morning)

- `agent_wrapper/backends/{base, vllm_openai, ollama_openai, anthropic}.py` — Backend protocol + 3 adapters; `vllm-gemma` lazy-reads `_sync_client` so existing test patches still flow through.
- SubAgent + Nara accept `backend=` kwarg (default unchanged).
- `calls.jsonl` schema's `host_metadata` relaxed for per-backend shapes.
- `D-035` logged in `DECISIONS.md` (supersedes D-033).
- `docs/sources/research_program_v2.md` committed; four PDF refs rewired; `CLAUDE.md` mandatory reading sequence now includes it as step 3.
- 23 new unit tests; 76 wrapper-touching tests pass.

### Plan-mode research (3 agents + 1 adversarial strawman)

- Quant landscape — confirmed `sakamakismile/Qwen3.6-27B-Text-NVFP4-MTP` (~21 GiB at runtime, native MTP via `qwen3_5_mtp`, community-measured 10 → 37 tok/s on GB10 with MTP).
- Scaffold-controlled benchmarks — **the 77.2% SWE-bench Verified figure is scaffold-inflated**; model-only on `mini-swe-agent` is **67.8%** (Qwen's own disclosure, [discussion #1846](https://github.com/QwenLM/Qwen3/discussions/1846)). Also surfaced GLM-4.7-Flash as a strong critic-specialist alternative if the Qwen path ever falls through.
- Co-resident serving recipe — `--gpu-memory-utilization 0.55–0.4` + `--enable-prefix-caching` + optional `--enable-sleep-mode` (confirmed present in v0.21.0 via EngineArgs source); Ollama's `GGML_CUDA_ENABLE_UNIFIED_MEMORY=1` required on GB10; sequential decode discipline (273 GB/s bandwidth is the binding constraint).
- Adversarial strawman — the load-bearing critique was *"you're risking the working Gemma baseline for a community quant on a forward vLLM whose benchmarks are scaffold-inflated, bundling a now-relevant critic problem with a Phase-2+ coder daydream"*. The synthesis honored the timeline-decoupling critique; the build-risk critique was cheaply testable (smoke first); decided to proceed with a smoke-first sequencing.

### Phase 0 = PASS (no code change committed; operational state)

- Pre-vllm-restart baseline confirmed (`BASELINE-OK` at 605 ms).
- `vllm-gemma4` restarted with `--gpu-memory-utilization 0.4 --enable-prefix-caching` — `RIGHTSIZED-OK` at 2.6 s (cold prefix-cache; warms after a few requests).
- Memory: `free -h` 117 → 59 GiB used; 30+ GiB headroom freed.
- `sakamakismile/Qwen3.6-27B-Text-NVFP4-MTP` pulled to `/mnt/models/qwen3.6-27b-nvfp4-mtp/` (19 GiB; human-authorized via plan-mode AskUserQuestion).
- `vllm-qwen` launched on `:8001` with `--gpu-memory-utilization 0.2 --quantization modelopt --kv-cache-dtype fp8 --speculative-config '{"method":"qwen3_5_mtp","num_speculative_tokens":3}'`.
- 20-prompt smoke: 18/20 produced real answers (5 coding + 5 critic + 5 reasoning + 5 long-context); 2/20 hit max_tokens=4096 mid-reasoning (Qwen is a reasoning model — tokens went to `model_extra.reasoning`, not gibberish).
- MTP acceptance rate: **76–83% average** across the run (way above the 30% threshold). Mean acceptance length 3.3–3.5 tokens per step.
- Cohabit confirmed: `COHABIT-OK` from `vllm-gemma` returned in 696 ms while `vllm-qwen` was loaded.
- **vLLM #36872 Gated DeltaNet rollback risk did NOT materialize on this build** — zero gibberish across the 20-prompt battery.

### Phase 1 = PASS (commit `39ba954`)

- `orchestrator/iteration_cache.py` (~50 LOC) — atomic write/read by `iteration_id`, reuses `PyRuntime.write_state` pattern.
- `workers/novelty_classify`, `workers/critic_loop_v0`, `workers/journal_writer` — drop heavy args, accept `iteration_id`, fetch from cache.
- `orchestrator/tool_registry.py` — schemas updated; novelty/critic require `(hypothesis_text, iteration_id)`; journal requires `(topic, iteration_id, nara_summary)`.
- `orchestrator/nara.py` — writes each tool_result to cache after successful dispatch; `NARA_PROMPT_V0` rewritten to mandate iteration_id-passing and forbid re-emitting captured payloads; iteration_id injected into the user message header so Nara has it.
- Fallback path (when Nara skips `journal_writer`) writes placeholder cache entries before calling the writer.
- 190 tests pass on the default backend path.
- **End-to-end smoke**: `iter-2026-05-27-001` ran the full 5-step chain on the previously-stuck topic ("conversation history as list vs narrative in repeated PD") — **34.5 s wall-clock**, novel/survives verdict, 1 reprompt at novelty_classify (vs. 10 each on iter-010/011's truncation failures), all four substructures populated, four `.json` files in `run_state/iteration_cache/iter-2026-05-27-001/`. Journal entry at `journal/iterations/012.md`.

## What's deferred (Phase 2 + later)

- **Phase 2**: three real iterations on three real topics, Gemma-only baseline. Human picks the topics deliberately and reads each journal — these are research artifacts, not test outputs.
- **Phase 3**: critic flip decision per concrete rule (did the critic mark its own homework on the three? if yes → flip to `vllm-qwen` or `anthropic`; if no → LOOP_V0 done per exit criterion).
- **Anthropic credit balance** is currently zero; Option C (Anthropic as critic) is wired in the substrate but blocked on top-up until you decide.
- **Watch list**: vLLM #36872 (Gated DeltaNet rollback gibberish) didn't bite the 20-prompt smoke; watch for it across higher-volume workloads on `vllm-qwen` before declaring "stable for production".

## Operating state at session end

- `main` is two commits ahead of `origin/main` (`b50e5e3` + `39ba954`). No push attempted.
- `vllm-gemma4` container (port 8000) at `--gpu-memory-utilization 0.4 --enable-prefix-caching` + existing MTP/MoE flags. Original launch command saved in this session's transcript if rollback is needed.
- `vllm-qwen` container (port 8001) running NVFP4-MTP build. Staged but not yet wired into any worker — substrate (`agent_wrapper.backends.OllamaBackend` with `OLLAMA_BASE_URL=http://127.0.0.1:8001/v1`) makes the flip a one-line change at `workers/critic_loop_v0.py`'s `run_subagent(...)` call.
- Plan file: [`/home/decross1/.claude/plans/peaceful-brewing-seal.md`](../../../.claude/plans/peaceful-brewing-seal.md) — approved by human at end of plan mode.
- `run_state/iteration_cache/iter-2026-05-27-001/` exists on disk; gitignored.

## Phase 2 + 3 + UI work — same-day continuation (later 2026-05-27)

The session continued through Phase 2 (three real iterations on real game-theory topics, Gemma critic baseline), a UI enhancement slice (model attribution in the active iteration panel + a parallel Qwen panel), and Phase 3 (critic-flip experiment ending in D-036 — the Co-Scientist insight empirically falsified for these three topics).

### UI enhancement — model attribution (commits `007e701`, `1956b1e`, `287ecca`)

The human asked for a small live-UI enhancement before running the final iteration: surface which model is driving the current step, plus a Qwen diagnostics panel paralleling the existing VllmPanel. Per the operating contract that `ui/` writes go through a UI session, the primary session handled the backend half (schema + Nara model-attribution) and delegated the UI half to a `builder` sub-agent in a worktree, then code-reviewed + merged.

- **A1+A2 (primary, commit `007e701`)** — `schema/active_iteration.schema.json` gained optional `orchestrator_backend` / `orchestrator_model` (top-level) + `backend` / `model` / `subagent_backend` / `subagent_model` per tool entry. `orchestrator/nara.py` populates them per dispatch and reads optional `backend_used` / `model_used` / `subagent_backend` / `subagent_model` overrides from tool_results so workers can surface a non-default backend (the Phase-3 critic flip uses this mechanism).
- **B1–B7 (delegated builder agent, commit `aa8226b`, merged via `1956b1e`)** — `ui/frontend/src/types/schemas.ts` extended, `ActiveIterationPanel.tsx` renders the prominent emerald orchestrator chip + divergence-only per-tool chip + sky-accented subagent chip on critic_loop_v0, fixtures updated with a uniform-inherit case and a divergent case for tests, sampler gained a second `VllmMetricsReader` for the Qwen `:8001/metrics` endpoint with graceful degradation, new `QwenPanel.tsx` mirrors VllmPanel with three empty states (unreachable / dropped-on-latest / data), telemetry schema extended additively. 91 tests pass (28 frontend + 58 backend + 5 sampler). `/code-review` clean before merge.
- **VllmPanel header tweak (delegated, commit `287ecca`)** — human asked the existing VllmPanel header to mirror QwenPanel's model-identity convention; 1-line change to `vLLM internals · gemma-4-26b-a4b`. Delegated agent's worktree was branched from a stale base; cherry-picked rather than merged to avoid reverting recent work.
- **Process notes worth flagging.** The delegated builder agent twice initially wrote edits into the *main* repo checkout instead of its worktree path before catching and reverting (visible in its self-report; main was always clean after). Both delegated branches were also forked from older HEADs and required recovery (merge + cherry-pick). For tiny UI tweaks the contract-override path is likely faster than the delegated path; for the full UI slice the delegation was the right call.

### Phase 2 — three real iterations on Gemma critic (iter-002 / 003 / 004)

Human committed to topics + pacing + a pre-committed Phase-3 decision rule in advance ("flip-critic-if Topic 2 survives without surfacing KMR/Young OR Topic 3 survives on the deliberately-wrong claim"; "keep-critic-if both critics correctly catch with named citations / backward-induction logic spelled out"; if-mixed clause anticipated retrieval-vs-critic divergence).

| Iter | Topic | Hypothesize chose | Novelty | Critic verdict | Wall |
|---|---|---|---|---|---|
| 002 | Topic 2 (rediscovery probe — KMR/Young coordination on networks) | "scale-free network" version (added topology specificity to the user's "fixed network") | `novel` (top: O&R chunk-345 fictitious play) | `survives`, 2 turns | 4.1 s |
| 003 | Topic 3 (deliberately-wrong PD claim — cooperation in penultimate round if payoff > 2× defection) | rewrote into "deviate if payoff ≤ 2× defection" (flipped both verb AND inequality — sanitized the wrongness) | `rediscovery` (correctly named backward induction) | `survives` (defensible given the rewritten claim it received), 1 turn | 2.1 s |
| 004 | Topic 1 (open / Bayesian-PGG calibration — asymmetric updating mechanism) | generated 3 candidates incl. #2 with the exact asymmetric-updating mechanism the human probed for ("inflating posterior of 'defector' types"); **selected #1 (generic restatement)** | `novel` | `survives` | 31.0 s |

**The diagnosis after Phase 2 (before flipping the critic):** in all three cases the critic followed its contract correctly (*only retrieved literature, no outside knowledge*) and produced defensible verdicts given the inputs it received. The binding constraints surfaced upstream:
- **Topic 2 → retrieval gap.** KMR 1993 / Young 1993 / Ellison 1993 / Blume 1995 are not in Chroma; critic correctly noted "retrieved set doesn't cover this."
- **Topic 3 → hypothesize sanitization.** The deliberately-wrong claim got rewritten before the critic ever saw it.
- **Topic 1 → hypothesize selection bias.** The mechanism-engaged candidate existed; the worker selected the generic restatement.

### Phase 3 — critic flip empirically tested (commits `4f2459c`, `4068511`, `9f713ac`)

Per the human's "do both" directive: flip the critic to local Qwen AND surface Track B (retrieval expansion) prerequisites.

- **`vllm-qwen` backend registered** (in `agent_wrapper/wrapper.py`) pointing at `:8001/v1` with `qwen3.6-27b-nvfp4-mtp` as the served model.
- **`CRITIC_BACKEND` env var** read in `workers/critic_loop_v0.py`; passes through to `run_subagent(backend=...)`; surfaces `subagent_backend`/`subagent_model` in the result so Nara writes them to `active_iteration.json` and the UI sky-chip lights up automatically.
- **Two substrate failures surfaced + fixed**:
    1. `vllm-qwen` was launched in Phase 0a' without `--enable-auto-tool-choice` / `--tool-call-parser qwen3_coder`. First Topic-2 re-run hit 400s, SubAgent caught them, critic_loop_v0 returned status=error 6× before chain hit max_depth=12. **Fix**: relaunched vllm-qwen with the tool-call flags + lowered `--max-model-len 32768→16384` + raised `--gpu-memory-utilization 0.2→0.25` (KV cache headroom issue surfaced during the relaunch — Gemma's prefix cache had grown).
    2. SubAgent's JSON extraction only looked at `msg.content`; Qwen's reasoning-mode separation routes the answer to `msg.model_extra["reasoning_content"]` on some prompts. **Fix (commit `4068511`)** — additive fallback: when content has no extractable JSON, try `reasoning_content` / `reasoning`. Also fixed pre-existing `tests/test_subagent.py` breakage hidden by D-035's backend refactor (fixture patched the now-removed `_sync_client` attr).

**Final 3-way comparison (commit `9f713ac`, D-036 in DECISIONS.md)**:

| Topic | Gemma critic | Qwen critic | Same verdict? |
|---|---|---|---|
| 1 (iter-004 vs 009) | `novel/survives` | `novel/survives`, 59.3 s, 2 turns | **yes — identical** |
| 2 (iter-002 vs 007) | `novel/survives`, 4.1 s | `novel/survives`, 52.6 s, 2 turns | **yes — identical** (Qwen rationale slightly more specific — named O&R explicitly) |
| 3 (iter-003 vs 008) | `rediscovery/survives`, 1 turn, 2.1 s | substrate failed: `schema_mismatch → fallback survives` (Qwen's reasoning placement varied this run) — but chain still caught the wrongness: novelty classified `nonsense` AND Nara's summary explicitly engaged with backward induction | inconclusive at critic step; chain-as-a-whole correct |

**Phase 3 conclusion (D-036).** The Co-Scientist insight (different model for critic → different critic verdict) is **empirically refuted for these three topics**. Both critics follow their contract correctly; the binding constraint is upstream (retrieval gap + hypothesize selection bias + hypothesize claim sanitization). Cost: Qwen critic 12–30× slower, 1/3 substrate failure rate even with the fallback. **Don't permanently route the critic to a non-Gemma backend. Substrate stays — it works and reopens cleanly when actual critic-marks-own-homework evidence appears under conditions where retrieval + hypothesize are not the bottleneck.**

### Hypothesize fixes (commit `652b0ef`)

Two D-036-surfaced fixes shipped:

1. **`HYPOTHESIZE_SYSTEM_PROMPT` tightened** to prefer mechanism-engaged candidates over linguistic restatements; includes a worked example contrasting Topic-1-shaped candidates ("X happens faster under noisy conditions" vs "X happens faster under noisy conditions *because asymmetric Bayesian updating inflates posterior of defector types*"). Adds explicit guidance that the topic itself, when already a sharp claim with a stated mechanism, should be included verbatim as a candidate and preferred.
2. **`HYPOTHESIZE_AS_STATED=1` env var** — bypasses the LLM call entirely and returns the topic verbatim as the chosen hypothesis. For testing deliberately-wrong claims end-to-end without hypothesize's rewrite step. Operational toggle matching the `CRITIC_BACKEND` pattern.

13 hypothesize tests pass, including 2 new ones (env-var bypass returns topic verbatim; env unset uses normal LLM path).

### Operating state at this update

- **`main` is ~12 commits ahead of `origin/main`** (Phase 1 + UI + Phase 3 + D-036 + hypothesize). No push attempted.
- **`vllm-gemma4`** (port 8000) at `--gpu-memory-utilization 0.4 --enable-prefix-caching`. Stable.
- **`vllm-qwen`** (port 8001) at `--gpu-memory-utilization 0.25 --max-model-len 16384` with tool-call flags + `--reasoning-parser qwen3` + `--tool-call-parser qwen3_coder` + MTP. **Stays running** even though critic isn't flipped — coder-tier workloads (Phase 2+) will use it; substrate is ready.
- **Iteration journals 012–020 on disk** (gitignored): one Phase-1 smoke, three Gemma-critic Phase-2 iterations, six iterations across the critic-flip experiment (three Qwen-critic re-runs + three failed runs from before the substrate fixes landed).
- **Plan file**: [`/home/decross1/.claude/plans/peaceful-brewing-seal.md`](../../../.claude/plans/peaceful-brewing-seal.md) — approved at start of plan mode; outcome diverged from the rule's strict letter (which would have flipped the critic) but matched the rule's spirit (the diagnosis pointed upstream and the if-mixed clause explicitly anticipated this).

### Track B — retrieval expansion (next session)

D-036's primary recommendation: ingest the foundational evolutionary-GT literature into Chroma so future Topic-2-class questions correctly classify as rediscovery. Sources prioritized:

- **Kandori, Mailath, Rob 1993** ("Learning, Mutation, and Long Run Equilibria in Games") — Econometrica
- **Young 1993** ("The Evolution of Conventions") — Econometrica
- **Ellison 1993** ("Learning, Local Interaction, and Coordination")
- **Blume 1995** ("The Statistical Mechanics of Best-Response Strategy Revision")
- **Weibull, *Evolutionary Game Theory*** (1995/97) — Cambridge UP
- **Hofbauer & Sigmund, *Evolutionary Games and Population Dynamics*** (CUP 1998)
- **Camerer, *Behavioral Game Theory*** — Princeton UP (load-bearing for novelty evaluation per research_program_v2)

Workflow once PDFs are available:
1. Human drops PDFs into `/mnt/models/papers_to_ingest/` (or alternate path).
2. Primary session runs `./.venv-chroma/bin/python ingest/textbook.py --pdf <path> --collection <name>` per source, one collection per source (e.g. `kandori_mailath_rob_1993`, `young_1993`, `weibull_egt`, ...).
3. Update `orchestrator/chroma_query.py` (~5 lines) so `retrieve_literature` queries the new collections alongside `papers_recent` + `osborne_rubinstein`.
4. Re-run Topic 2 (rediscovery probe) — expected: KMR / Young appear in top-K neighbors; novelty flips `novel → rediscovery`; critic produces a `restated` or `survives` verdict citing them.
5. Three real iterations on three new topics if Track B passes its verification.

Light coordination note: a parallel Claude session has been editing `pipeline/arxiv_scraper.py` + `cron/daily-arxiv.sh`. Different files than `ingest/textbook.py`; concurrent Chroma writes against different collections are safe but a full arxiv refresh + textbook ingest at the same instant should be serialized.

### Commits today (full list)

```
652b0ef hypothesize: prefer mechanism-engaged candidates + HYPOTHESIZE_AS_STATED bypass
9f713ac D-036: critic-flip (Co-Scientist insight) tested empirically — no observed benefit
4068511 subagent: fall back to reasoning_content when content has no JSON
4f2459c critic flip: env-var-driven backend override for critic_loop_v0 sub-agent
287ecca ui/VllmPanel: tag heading with served model (gemma-4-26b-a4b)
1956b1e Merge ui-model-chip-and-qwen-panel: model-chip + Qwen panel UI slice
aa8226b ui: model-chip in ActiveIterationPanel + QwenPanel for vllm-qwen :8001
aa2990b merge main into UI worktree to pick up schema delta + nara model attribution
007e701 active_iteration: per-step model attribution (A1+A2 of UI model-chip slice)
39ba954 loop_v0: reference-passing — workers fetch heavy payloads from per-iteration cache by id
b50e5e3 multi-backend wrapper substrate (D-035) + research_program_v2 doc
```

### What this session deliberately accomplished

- Landed the multi-backend substrate cleanly (Phase 1 morning).
- Resolved the architectural budget question via empirical Phase-0 smoke rather than blind commitment.
- Built the live-UI surface to display which model is driving each step.
- Ran three real research-shaped iterations and read all three journals.
- **Empirically falsified the Co-Scientist application** (different model for critic) on its first test, on the cheap, before building elaborate planner-tier infrastructure around it. The negative result IS the research output for this slice.
- Documented the finding durably (D-036) and shipped the upstream fixes (hypothesize prompt + as-stated bypass) the same evidence justified.

### Next session was anticipated as Track B — same-day execution instead (later 2026-05-27)

The human dropped foundational PDFs at `/home/decross1/projects/game_theory_pdfs` mid-session; Track B executed end-to-end the same day rather than waiting.

#### Track B ingest (commits `c9623a5`, `0567f57`)

- **Three PDFs ingested directly** (text-extractable):
  - `kandori_mailath_rob_1993` — 216 chunks (KMR 1993, THE risk-dominance result)
  - `ellison_1993` — 164 chunks (coordination on local networks)
  - `hofbauer_sigmund_1998` — 188 chunks (Hofbauer & Sigmund proceedings article)
- **Two more PDFs ingested in a second batch** (also text-extractable):
  - `blume_1995` — 213 chunks (Blume's "Population Games", SFI version)
  - `weibull_egt` — 1405 chunks (Weibull's full evolutionary GT textbook, 293 pp)
- **Three PDFs OCR'd via tesseract** (human installed `tesseract-ocr` mid-session) and ingested as plain-text:
  - `young_1993` — 50 chunks (Young 1993 SFI working paper, 33 pp)
  - `van_damme_1994` — 10 chunks (van Damme EGT review in EER, 13 pp)
  - `camerer_bgt` — 166 chunks (Camerer/Ho/Chong Behavioral Game Theory, 70 pp)

`ingest/textbook.py` was switched from `HttpClient(localhost:8001)` to `PersistentClient` (matching `orchestrator/chroma_query.py`'s read path; port 8001 is now vllm-qwen and no chroma server runs). A companion `ingest/text_file.py` was added for OCR / text-file ingest. `orchestrator/chroma_query.py`'s `COLLECTIONS` dict grew from 2 entries to 10 — the canonical risk-dominance + EGT + BGT canon research_program_v2 § "Foundational Literature" called load-bearing.

#### Three-topic re-run with the expanded retrieval (iter-2026-05-27-010, 011, 012)

Same critic backend (Gemma) to make these apples-to-apples vs the original iter-002 / 003 / 004:

| Topic | Original | Expanded retrieval |
|---|---|---|
| 2 (rediscovery probe) | `novel/survives`, top=O&R chunk-345 | **`rediscovery/survives`** with `learning_mutations_and_long.pdf-chunk-59` (KMR) cited; critic distinguishes "general principle is rediscovery" from "specific combination is gap" |
| 3 (deliberately-wrong PD claim) | `rediscovery/survives` (hypothesize sanitized the wrong claim) | **`nonsense/falsified`** with `osborne_rubinstein-chunk-852` cited + backward-induction spelled out; hypothesize prompt-tightening from `652b0ef` kept user's claim VERBATIM this run |
| 1 (open Bayesian PGG) | `novel/survives` | `novel/survives` (no shift; expected — new collections don't cover noise-in-PGG, chain correctly distinguishes "general framework exists" from "specific result doesn't") |

**Both Topic 2 and Topic 3 now satisfy the pre-committed `keep-critic-if` *spirit*** without any critic flip:

- Topic 2: chain flagged rediscovery + cited the risk-dominance literature (Track B retrieval was the lift)
- Topic 3: critic falsified with backward-induction citation (hypothesize prompt tightening was the lift — preserved the user's wrong claim instead of sanitizing it)

D-036's diagnosis fully confirmed empirically: **the binding constraint was upstream (retrieval + hypothesize), not the critic.** Adding 8 collections to Chroma + tightening one prompt did what flipping the critic to a different $$$/sec model couldn't.

#### Retrieval verification (final state)

Three diverse queries against the 10-collection knowledge base:

- "symmetric 2x2 coordination games risk-dominant equilibrium fictitious play network population" → top neighbor `blume_1995-chunk-92` (0.711)
- "cognitive hierarchy level-k thinking experimental games behavioral deviations" → 4 of top 4 are `camerer_bgt` chunks (top 0.636)
- "evolution of conventions stochastic stability long run equilibrium adaptive" → `young_1993-chunk-11` top (0.676)

The canon is reachable.

### Final session state (later 2026-05-27)

- **`main` is 14 commits ahead of `origin/main`** including the parallel arxiv-cron session's `52e24a0`. Pre-push housekeeping cleaned three stale delegated-agent branches (`ui-model-chip-and-qwen-panel`, `ui-vllm-header-model-tag`, `worktree-agent-a76c191abd365c4af`) — all work preserved in main's history via merge or cherry-pick.
- **Chroma store** holds 10 collections totaling ~5000 foundational chunks + the existing arxiv `papers_recent` collection.
- **Apparatus is durably better than at session start**: multi-backend substrate (D-035), reference-passing chain reliability, model-attribution UI surface, Co-Scientist insight tested empirically (D-036), retrieval expansion across the full GT canon.

### Phase 2 — next-session anchor

LOOP_V0's exit criterion #4 ("at least one of the iterations was useful enough to keep going") is now the human's reflective call. If yes, Phase 2 per research_program_v2 § "Program Arc" opens up the **Loop v1** scope: full autoresearch loop across synthetic + semi-synthetic sandbox tiers. Concrete next-session starting points in the proposed Phase 2 directions doc.

## Phase 2 / Slice 1 — same-session continuation (late 2026-05-27 → 2026-05-29 close-out)

Plan-mode chose Slice 1 (Tier-2 Vickrey-rediscovery experiment + LOOP_V0 bridge) per `.claude/plans/peaceful-brewing-seal.md`. Slice 2 (ML-Intern install) was attempted in parallel via a delegated builder sub-agent in a worktree; the API hit `Error: Overloaded` and both Slice-1 (Agent α) and Slice-2 (Agent β) agents died mid-task. Agent α had produced 3 high-quality uncommitted files (bidder/auctioneer/run) which were salvaged from the worktree; Slice 2 ML-Intern fetcher deferred to a future session.

### Commits landed (6)

- `81dfcea` — phase 2 / slice 1+2 shared dep: optional `experiment_outcome` field on `iteration_record.schema.json` + `experiment_outcome=` kwarg on `nara.run_iteration` + 9 schema tests
- `e0081db` — exp003 Vickrey experiment (auctioneer + neutral-prompt bidder + run + analyze + loop_bridge + notes + 8 auctioneer tests)
- `2df2a80` — 50-trial run results + headline notes
- `63a2560` — paraphrased-seed retrieval probe (4 phrasings)
- `a28ab31` — paraphrase-probe threshold-evaluation extension
- `079fc29` — `workers/retrieve_literature.py` compound ML-Intern escalation trigger + 8 new tests

### Headline empirical findings (factual)

**Tier-2 (exp003, 50 trials × 4 bidders against vllm-gemma):**

- 200/200 LLM calls bid = valuation to floating-point precision (residual mean +0.00 / sd 0.00)
- 0 parse failures, 0 errors, 0 tie-breaks
- Wall-clock 4m52s
- Bidder reasoning on trial 1 (unprompted): *"This is a second-price sealed-bid auction (Vickrey auction). In such auctions, the dominant strategy is to bid one's true private valuation."* The system prompt deliberately omits "Vickrey", "second-price", "dominant strategy", "report your value" — Gemma is inferring the mechanism + naming the theorem from the mechanics description alone.

**Tier-3 (LOOP_V0 `iter-2026-05-27-028`, `journal/iterations/054.md`):**

- novelty: `rediscovery`, top neighbor `osborne_rubinstein-chunk-127` (the chunk that states bidding one's valuation is a weakly dominant action in second-price auctions)
- critic: `survives` — engaged with the literature, noted theory is established but *no empirical literature exists on LLM-bidder convergence in this setting*, so the claim cannot be falsified or restated
- `experiment_outcome` correctly threaded into the iteration_record (`value=1.0`, `trials=50`, summary, results_path)
- First LOOP_V0 iteration with a bridged `experiment_outcome` — the Slice-1 contract works end-to-end on the first real attempt

### Retrieval probe (paraphrase_probe.py)

Direct Chroma queries on 4 phrasings of the same Vickrey-rediscovery claim, bypassing `hypothesize`:

| seed | phrasing | max_score | Camerer top-10? | distinct foundational books |
|---|---|---|---|---|
| A | Tier-2 experimental (original) | 0.6450 | no | 2 |
| B | behavioral-econ / Camerer | 0.6174 | **yes** (rank 5) | 4 |
| C | mechanism-design / Myerson | 0.6833 | no | 2 |
| D | textbook minimal | 0.7534 | no | 1 |

Osborne & Rubinstein dominates every phrasing's top-10. Camerer BGT only surfaces under behavioral-econ vocabulary. The embedder is highly vocabulary-sensitive.

### Compound ML-Intern escalation trigger (workers/retrieve_literature.py)

Bumped from Agent β's original spec (`max_score < 0.55` — fires on 0/4 seeds, dead trigger) to a compound:

`max_score < 0.70 AND distinct_foundational_books_in_top_10 < 3`

End-to-end run against the 4 seeds:

| seed | max_score | distinct_books | should_escalate |
|---|---|---|---|
| A | 0.6450 | 2 | **True** (weak signal AND narrow coverage) |
| B | 0.6174 | 4 | False (diverse coverage saves it) |
| C | 0.6833 | 2 | **True** (weak signal AND narrow coverage) |
| D | 0.7534 | 1 | False (strong signal saves it) |

Only A + C — weak signal AND narrow coverage — warrant ML-Intern's external-API round-trip. The trigger only computes + reports the decision today via `result.escalation`; the actual `workers.ml_intern` dispatch is the still-deferred half of Slice 2.

### Code-review (close-out pass, 2026-05-29)

Adversarial review of `81dfcea..079fc29` (6 commits). **No blocking issues.** Non-blocking items carried forward as next-session inputs:

1. `_evaluate_escalation` is brittle to neighbors with `score=None` (today's `chroma_query` never emits these). Fix: `(n.get("score") or 0.0)`.
2. `_foundational_book_name` returns empty string on malformed `"-chunk-7"` doc_ids (no prefix). Today's chunks always have a prefix.
3. `RETRIEVAL_ESCALATION_TOP_K=10` is hardcoded; caller `k<10` interacts with the diversity gate in untested ways. Today's Nara uses k=10.
4. **`analyze.py` does not gate verdict on parse_failure_rate.** If every bid were a parse-failure fallback (bid := valuation), the 75% threshold would silently pass with verdict=YES. The 50-trial run had 0 parse failures so the result stands, but a high-failure run on a different model/temperature could yield a false-positive YES. Worth a verdict gate.
5. `experiment_outcome.value: oneOf [number, object]` accepts un-validated objects (the multi-metric branch is leaky). Acceptable tradeoff documented in schema tests.
6. The 4 paraphrase seeds are not strict paraphrases — they emphasize different sub-claims (experimental / behavioral / theorem / textbook). Acceptable for the diversity-of-vocabulary probe, but the docstring's "same underlying claim" is slightly loose.
7. `loop_bridge._truthful_fraction_from_summary` tightly couples to one specific summary-line shape. Would IndexError if analyze.py format changes.
8. `tests/test_dispatch_coding_agent.py` is fully stale (Track-A/B/C/D-era; `agent/ownership.yaml` was deleted in `08fc327`). Either delete or move under `archive/`. Pre-existing; not introduced by this session.

### Operating state at session end (2026-05-29)

- `main` is at `079fc29`, fully pushed to origin.
- Working tree clean. No worktrees from this session's delegated agents remain.
- Tests: 465/465 pass (excluding the pre-existing stale `test_dispatch_coding_agent.py`).
- Slice 2 fetcher (`workers/ml_intern.py` + threshold-gated dispatch) is the only Phase-2 task still pending; the `result.escalation` seam is wired and waiting for a consumer.

### Phase 2 — next-session anchor (revised)

Slice 1 + retrieval-trigger half of Slice 2 are landed. Next session in planning mode should consider:

- **Slice 2 completion**: `workers/ml_intern.py` (Semantic Scholar fetcher + BGE-M3 embedding into a new `ml_intern_fetched` Chroma collection) + the call site in `orchestrator/nara.py` that reads `result.escalation.should_escalate` and dispatches.
- **Slice 3**: Karpathy autoresearch install (bandit-driven hyperparameter sweep tool) — separate decision-tree branch.
- **Slice 4**: full Loop v1 with Step 1.5 meta-review, Step 2.5 red-team critic, Step 5 cross-tier replication, Step 8 human-gate feedback. The largest commitment.
- **Carryover from code-review**: the `analyze.py` verdict-vs-parse-failure gate (#4 above) is worth folding into any future Tier-2 experiment driver.
