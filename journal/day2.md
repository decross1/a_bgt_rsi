# What does it mean for a research apparatus to "work"? — reading Sakana skeptics on Day 2

> **STUB** — generated by the Day-2 run with measurements pre-filled.
> The researcher writes the final prose (target 200–300 words). Data
> inserts are marked `[DATA]`; prose to write is marked `[WRITE]`.
> Remove this notice and all markers before publishing.

**What shipped today.** [WRITE: 2–3 sentences — every model call now
goes through a thin wrapper that records it as a structured research
observation. The JSON schema was locked first, then the wrapper, then a
50-call test sweep exercised both.]

The numbers from the day's 50-call sweep:

- Malformed log lines: `[DATA: 0 of 50]` — every call wrote a
  schema-valid record.
- Determinism at temperature 0: `[DATA: PASS]` — three runs of the same
  prompt, three identical completions.
- Determinism at temperature 1 with a fixed seed: `[DATA: PASS]` —
  likewise identical across three runs.
- Aggregate throughput: `[DATA: ~56 tokens/sec]`.

**On what "work" means — the Sakana thread.** [WRITE: the heart of the
post. The wrapper passed every correctness and reproducibility check on
the first honest try. But the sweep also had a speed bar, and the run
first came in under it — the apparatus's own hard checkpoint stopped the
day rather than wave a near-miss through, and the day only continued
after a real fix, not a lowered bar. Tie this to the Sakana skeptics:
the gap between a system that runs and a system that contributes, and
why a checkpoint that won't round "close" up to "pass" is part of what
"working" should mean.]

**Tomorrow.** [WRITE: one sentence — Day 3 stands up the retrieval layer
and ingests the first textbook.]

---
_Draft length target: 200–300 words. Private — do NOT publish: your own
taxonomy of failure modes, and which apply to this design._
