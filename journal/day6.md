# Day 6 — orchestrator + worker, the smallest possible loop

> **STUB** — generated by the Day-6 run with measurements pre-filled.
> The researcher writes the final prose (target 200–400 words). Data
> inserts are marked `[DATA]`; prose to write is marked `[WRITE]`.
> Remove this notice and all markers before publishing.

**What shipped today.** [WRITE: 2–3 sentences — an orchestrator that
dispatches a worker process, a worker that summarizes an arXiv paper
through Gemma 4, and a 60-second wall-clock timeout that terminates
hung workers without leaving orphans. The architecture is one task type
(`summarize_paper`), one worker per task, sequential — but the
contract that says "an agent is a thing that takes a `worker_input` and
returns a `worker_output`" is now load-bearing.]

**The numbers.**

- Sequential success rate: `[DATA: 5/5]` (`day6_orchestrator_5_of_5`).
  Per-task durations `[DATA: 2.6–3.7 s]`; total wall-clock for the
  5-task suite + 5 malformed-input rejections `[DATA: ~15 s]`.
- Malformed-input rejections: `[DATA: 5/5]` cleanly handled
  (`missing_task_id`, `unknown_task_type`, `payload_missing_arxiv_id`,
  `payload_wrong_type`, `not_an_object`) — each returned
  `status="error"` with a populated `errors` list; no exceptions, no
  hangs, no orphan worker processes.
- Isolation backend: `[DATA: Python multiprocessing]`
  (`state.fallbacks_taken.day6_orchestrator_isolation = "multiprocessing"`).
  `[DATA: NemoClaw was never installed on Day 1 (state.fallbacks_taken.day1_nemoclaw),
  so the router skipped the sandbox path and selected the multiprocessing
  fallback — sandbox isolation is re-added in Week 2.]`

**The call chain, end-to-end.** For task `seq-1` (one of the 5):

```
[orchestrator_dispatch]   req=96bb2042  parent=-
  └─ [worker_invocation]  req=f7890721  parent=96bb2042
     ├─ [wrapper_call]    req=ebf4ce83  parent=f7890721
     │     model=gemma-4-26b-a4b  tokens=341/170  dur=3.10 s
     │     completion="This paper investigates the approximability
     │                  of EFX allocation..."
     └─ [orchestrator_receipt]
                          req=458b5c17  parent=f7890721
                          status=passed  dur=3.67 s
```

Reconstructed by `tools/inspect_run.py --task-id seq-1` from
`logs/orchestrator.jsonl` (3 entries) + `logs/day6.jsonl` (1 wrapper
entry, parent-linked across files by `parent_request_id`). The
reproducibility promise is now operational: given a `task_id`, you can
see exactly what happened, including the prompt and the model
response.

**Why the contract matters.** [WRITE: 2–3 sentences — the worker
contract (`schema/worker_contract.schema.json`) is four input fields
and five output fields, but every architectural promise about "agents"
hinges on it. Get it wrong now and pay forever. The hard checkpoint at
the start of the day is the discipline that keeps that promise from
being papered over.]

**Tomorrow.** [WRITE: one sentence — Day 7 is the repeated
Prisoner's-Dilemma experiment: four fixed strategies (TFT, grim
trigger, all-C, all-D), the LLM as the fifth player, this orchestrator
spawning the workers.]

---
_Draft length target: 200–400 words. Private — do NOT publish:
architectural decisions you regret from today, and any Week-2 planning
items derived from those regrets._
