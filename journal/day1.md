# Day 1 of the apparatus — what I shipped, what I'm afraid will break

> **STUB** — generated by the Day-1 run with measurements pre-filled.
> The researcher writes the final prose (target 200–500 words). Data
> inserts are marked `[DATA]`; prose to write is marked `[WRITE]`.
> Remove this notice and all markers before publishing.

**What shipped today.** [WRITE: 2–3 sentences — the hardware came
online, the inference stack stood up, the first generations came back.]

The numbers from the day:

- Inference server: vLLM `[DATA: vllm/vllm-openai:v0.20.0]` serving
  Gemma 4 26B (NVFP4 quantization).
- Single-stream throughput: `[DATA: ~32 tokens/sec]` — the median of a
  five-prompt micro-benchmark.
- Sandboxed-execution path: `[DATA: plain-Docker fallback]` — the
  primary sandbox runtime was not onboarded today; the hardened
  fallback was verified instead.

**What I'm afraid will break.** [WRITE: expand the risk below in plain
language, and why it worries you going forward.] The one on my mind:
the inference stack is a tight chain of pinned versions — the model
image and the model checkpoint have to move in lockstep, and a
mismatch doesn't fail loudly, it fails at load time.

**Tomorrow.** [WRITE: one sentence — Day 2 builds the logging wrapper
so every model call becomes a recorded research observation.]

---
_Draft length target: 200–500 words._
