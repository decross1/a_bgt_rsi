# Apparatus v0 — Week 1, what worked, what didn't

> **STUB** — generated by the Day-7 run with measurements pre-filled.
> The researcher writes the final prose (target **600–1000 words**;
> longer than the daily 200–400 because this is the weekly synthesis).
> Data inserts are marked `[DATA]`; prose to write is marked
> `[WRITE]`. Remove this notice and all markers before publishing.
>
> **Publication scope.** This is the *weekly retrospective*. The
> Day-7 experiment results are **not** announced here — that post
> happens on a follow-up day, after the human clears
> `day7_publication_review_gate` (see "Day 7 — the experiment" below).

---

**The frame.** [WRITE: 2–3 sentences — apparatus v0 is one DGX Spark
in a closet that, on Day 1, was a box of cables; by Day 7 it
self-hosts vLLM serving Gemma 4, ingests papers nightly, dispatches
sandboxed worker processes, and ran its first multi-condition
behavioral-game-theory experiment. The week 1 deliverable is *the
apparatus*, not the findings — and that distinction is going to
matter for how I publish Day 7's result.]

## The numbers, in chronological order

- **Day 1 — hardware + vLLM serving.** Gemma 4 (NVFP4) up at
  `[DATA: 32.03 tok/s]` (`day1_tokens_per_sec`) on the v0.21.0 image,
  MARLIN MoE backend confirmed in the startup log
  (`Using 'MARLIN' NvFp4 MoE backend`). MTP was deferred to Week 2+
  per `state.fallbacks_taken.day1_block2_vllm_serve`. NemoClaw never
  came online (`state.fallbacks_taken.day1_nemoclaw`), so the
  hardened plain-Docker path took over.
- **Day 2 — wrapper + JSONL logging.** 50-call sweep, `[DATA: 0]`
  malformed records (`day2_jsonl_malformed_count`). The wrapper logs
  every model call to a schema-validated JSONL, with
  `parent_request_id` ready to thread causality across files
  (Day 6 would cash in on that).
- **Day 3 — ChromaDB + textbook ingest + needle.** Needle-in-haystack
  retrieval score `[DATA: 0.7221]` (`day3_needle_score`) with BGE-M3
  embeddings, single 1024-d vector space.
- **Day 3.5 — schema amendments.** A *planned* slip into a half-day
  that added `retrieval_context` + events schema + wrapper
  passthrough. The fact that this slipped explicitly (rather than
  silently consuming Day 4) is the architecture working: declared
  slips are first-class entries in the run log.
- **Day 4 — first tool call.** Tool-call invocation rate
  `[DATA: 1.0]` (`day4_tool_call_invocation_rate`) — the LLM emits a
  well-formed `tool_calls` payload every time the prompt warrants it.
- **Day 5 — arXiv pipeline.** `[DATA: 138]` recent papers ingested
  into `papers_recent` (`day5_papers_ingested`; cs.MA 76, cs.GT 44,
  econ.TH 18, dates 2026-05-14..2026-05-20). Source switched from
  Semantic Scholar to arXiv mid-day (D-027); the ML-Intern
  middle-tier was skipped entirely (`state.fallbacks_taken.day5_ml_intern`).
  Retrieval sub-second on the bigger corpus.
- **Day 6 — orchestrator + first worker.** `[DATA: 5/5]` sequential
  worker tasks passed (`day6_orchestrator_5_of_5`), `[DATA: 5/5]`
  malformed-input cases rejected cleanly, **zero** orphan worker
  processes. NemoClaw → multiprocessing fallback expected and logged
  (`state.fallbacks_taken.day6_orchestrator_isolation = "multiprocessing"`).
  The four-level causal chain
  (`orchestrator_dispatch → worker_invocation → wrapper_call →
  orchestrator_receipt`) reconstructs end-to-end via `inspect_run`.

**Full `fallbacks_taken` dict (the architectural truth-table):**

```
[DATA:
day1_block2_vllm_serve         -> MTP deferred to Week 2+ (NVFP4 baseline)
day1_nemoclaw                  -> NemoClaw onboarding skipped (binary not installed)
day5_ml_intern                 -> direct_api
day6_orchestrator_isolation    -> multiprocessing
day6_end_of_day_artifacts      -> user_crontab_instead_of_sudo_crontab
]
```

The pattern is consistent: every fallback is named, logged, and
time-capped at the day it was selected; none of them silently
degraded the apparatus.

---

## Day 7 — the experiment

> **⚠️ PRELIMINARY — RESULTS NOT YET REVIEWED**
>
> The day-7 numbers below are *preliminary*. The publication review
> gate (`day7_publication_review_gate`) is open and the results
> announcement is a separate, later post. The weekly synthesis you're
> reading is allowed to mention measurements; it is *not* the result
> announcement.

[WRITE: 2–3 sentences — what was actually run: 100 rounds of repeated
matrix Prisoner's Dilemma against each of five opponents (tft,
grim_trigger, all_c, all_d, mirror_llm = a second Gemma 4 instance);
600 LLM calls; 0 parse failures; full chain logged through the Day-6
orchestrator (its first real use, on a non-summarize-paper task).]

LLM-vs-TFT cooperation rate: `[DATA: 1.000]`
(`day7_coop_rate_vs_tft` — *preliminary; subject to
`day7_publication_review_gate`*). The original pre-computed expected
range was `[0.60, 0.95]`; the actual result was above the upper bound.

A 4-run diagnostic ladder followed (declared as slips per
`PHASE_1_ROADMAP.md` §2):

```
[DATA:
                  T     prompt              LLM-vs-TFT  LLM-vs-all_d
baseline          0.0   baseline               1.000        0.120
day_7.1           0.2   baseline               1.000        0.110
day_7.2           0.7   baseline               1.000        0.120
day_7.3           0.0   exploitation_hint      1.000        0.020
]
```

[WRITE: 3–4 sentences — what this told me. The cooperation lock-in is
robust to sampling AND framing. The LLM is responsive to opponent
data (defects ~88–98% against constant defection) — it just doesn't
defect first. This is a property, not a bug, but it's the kind of
property the published-paper-version of this experiment needs to
state up front.]

After the 4-run diagnostic the pre-computed range was amended to
`[0.60, 1.00]` (see `notes/day7_expected_range.md`).

---

## Seven specific things that took longer than planned

[WRITE: seven specific items, one or two sentences each. Examples
from this week: the schema-amendment day inserted between Day 3 and
Day 4; the S2 → arXiv source switch on Day 5; the MOCK_LLM-in-env
moment that silently stubbed an embed run; the `--via-orchestrator`
needing a small orchestrator generalization on Day 7; the
quicklook-CSV-format mismatch; the pre-computed-range safeguard
firing and forcing the 4-run diagnostic; the cron-path divergence
(user crontab vs sudo crontab). Be specific about the *cost in
minutes*, not just the fact.]

## What I'll change for Week 2

[WRITE: 3–5 bullets. Candidates: (1) extend the orchestrator's
task-type dispatch so future workers don't need the same surgical
edit Day 7 needed; (2) standardize the per-opponent CSV output from
the experiment runner so quicklook is one-shot; (3) introduce a
critic agent + meta-review to second-guess "the result is just
right" cases like Day 7's 1.000 vs TFT; (4) ship UI v1 (Track D) so
runtime state is visible without grepping JSONL; (5) the
Phase-2-architecture re-run of the PD experiment on Day 43.]

---

_Draft length target: 600–1000 words. Private — do NOT publish:
specific bug tickets you owe yourself, architectural debts to address
in Week 2, and the published-paper drafting plan for the Day-7
findings. Those live in `notes/`, not here._
