# Bounded utility mechanism follow-on campaign

`v2-utility-mechanism-followon-20260915` is registered as a prepared, immutable campaign. Its exact manifest is `experiments/research_campaign_v2_utility_mechanism_followon_20260915.json` (SHA-256 `2ecf817d0b78a2568cc1096759831a3fe625b1de59d3865a23cefb9052047b3c`). Registration does not activate it. The separate hash-bound active-campaign pointer still names the known-opponent campaign until an operator changes it after the current work closes and source checks pass.

The prior single-model utility pilot admitted 12 complete episodes, with 3/12 zero-regret episodes and 0/12 combined comprehension passes. Its admission receipt is `lab-eight-hour/known-opponent-utility/qfn-followon-known-opponent-lab8h-a/admission.json` (SHA-256 `824403c23c6969eeb1e2cbc11c4ec2c5222cf7ff2123cbc867427868042bcee1`). These counts motivate three falsifiable questions; they do not confirm an agent mechanism or show a new theorem, human behavior, or trading edge. Each question requires source-bound attempted and valid denominators and an analytical scripted comparator or negative control.

The registered questions separate exact payoff arithmetic from full-horizon action regret, seat/representation binding from payoff correctness, and immediate payoff from grim-trigger lookahead. Ordinary coordinator dispatch passes the exact topic text to Nara, while it does not pass an `experiment_outcome` or the manifest's claim limits. Each topic seed therefore includes the prior receipt path, raw hash, counts and scope as motivation; the model must label that observation unknown if it cannot verify the source. The critic receives Nara's hypothesis and context, so these facts reach the critic only if Nara carries them forward. `study_manifests` is empty because no successor study has been preregistered or run. A later study needs its own immutable manifest and source-bound publication; the campaign topic queue alone is not an empirical result or scientific promotion.

The existing known-opponent manifest and its linked rows remain in their original campaign. The successor is a finite three-topic queue: ordinary hourly cycles could consume all three overnight, so registration does not promise endless new topic work. The next already frozen empirical measurement is payoff-decomposition B, whose bounded timer starts at 2026-09-16 03:35 UTC. B uses its own 17-file source bundle and does not read the active-campaign pointer; campaign activation is not a prerequisite for B. The hourly coordinator and B can still contend for resident resources, so operational scheduling must use their existing leases and admission checks without changing B's frozen files.
