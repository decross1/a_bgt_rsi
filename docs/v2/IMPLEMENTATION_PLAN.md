# V2 preparation and benchmark followthrough

The authorized scope is a repository-wide v2 preparation, including implementation, archival, architecture and data-model cleanup, and evaluation. Research remains game theory and agent behavior / agentic game theory. The working question is how incentives and access to information affect cooperation, delegation, and strategic behavior among agents. The declared campaign question concerns own/shared payoff objectives and player-identified history in repeated public-goods games. The first controlled model phase is refined to objective-only comparisons against known scripted opponents; identity/history follows later. CPU calibration is complete; model-game execution remains separately preregistered work.

## Delivered implementation scope

These items describe the implemented scope and must not be treated as an
instruction to repeat settled trials. [Delivery](DELIVERY.md) records validation;
the canonical deployment receipt records subsequent runtime adoption.

1. Complete structured-output diagnostics, fresh diversity-selection fixtures, role-aware adaptive-effort evaluation, end-to-end research-pipeline measurement, and actual historical coding repairs.
2. Audit runtime, data contracts, dependencies, docs, research evidence and operator workflows in independent waves; cross-review recommendations before edits.
3. Preserve prior research and evidence with manifests and indexes; retain operationally required paths and immutable historical scores.
4. Implement evidence-supported code removal, documentation consolidation, architecture/data-model improvements, and isolated fresh-topic setup.
5. Validate, adversarially review, deliver through public Git history, adopt without losing canonical dirt, and record v2 operation/rollback guidance.

## Trial envelope

The existing cap is 7,200 Spark seconds per UTC ISO week. At planning, W38 has approximately 3,744 seconds remaining. New maximum reservations: diversity 880 seconds; role effort 1,660 seconds; historical repairs 1,050 seconds. Combined maximum 3,590 seconds; actual available balance is rechecked atomically by the controller. No paid frontier API calls, autonomous trial schedule, production policy promotion, model replacement, or runtime cutover is part of this cleanup.

## Evidence discipline

Legacy development scores are not rewritten. Fresh tasks and new graders get distinct identities. Adaptive routing never sees hidden grading answers. Archive manifests preserve hashes and source locations; live append-only ledgers and human research records are not deleted. A code path is removable only after static references, dynamic registration, launchers, tests and live usage have been checked. Active deployment compatibility is checked before changing a data model.

## Measured execution ledger

Four supervised followthrough trials completed their declared schedules: diversity, role effort, historical JSON-wire repairs, and a raw-diff follow-up. The role-effort execution retained all outcomes but is correctly marked incomplete because of one timeout. Together these trials charged 705.49 seconds; W38 has approximately 50.65 minutes remaining as of 22:49 UTC. See [benchmark findings](BENCHMARK_FINDINGS.md) for failure-inclusive results. No upgrade is established.
