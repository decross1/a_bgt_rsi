# V2 foundation delivery

The source change delivers a fresh agentic-game-theory campaign, exact research
lineage, campaign progress on the benchmark page, measured benchmark
follow-through, and a retrievable v0/v1 archive. The existing models, serving
runtime, default inference policy, weekly cap and human scientific gates remain
the baseline.

## Delivered source and evidence

| Work | Evidence and scope |
| --- | --- |
| Structured output and selector | Versioned strict parser/diagnoses, fresh diversity tasks, receipt replay; 25 real calls, control 1/5 tasks versus diversity 0/5 |
| Reasoning effort by role | Six tasks per arm; xhigh 5/6, medium 2/6, adaptive 5/6; one adaptive timeout preserved as incomplete execution |
| Actual coding repairs | Six JSON-wire and six raw-diff attempts against historical base files; zero applicable patches; no generated repair reached the correctness grader |
| Research progression | Exact campaign/topic/cycle/iteration/finding lineage, source coverage, continuing downstream observations across week and closure boundaries |
| Fresh research setup | Immutable campaign `v2-agentic-game-theory-20260914`; known-opponent model-study design, CPU mechanics and exact best-response controls |
| V0/v1 preservation | 20,310 files / 959,334,485 bytes, every captured checksum verified; originals retained |
| Dead-code retirement | Fourteen confirmed obsolete files moved byte-for-byte into inert archive history |
| Documentation | Current front doors, architecture, operator guide, data-contract registry, lessons and review reports |
| Corpus portability | Five explicitly marked local-corpus checks; required mode fails missing inputs; public/synthetic tests remain mandatory |

The four live follow-through trials charged 705.49 seconds. W38 had 50.65 minutes
remaining at the last trial receipt. Budget is checked atomically at execution;
this dated balance is not an authorization for a future run. No candidate
upgrade or controlled model-game result was established.

## Validation

- Core suite in required-corpus mode: **2,950 passed**, 33 subtests; the newly
  developed projector was checked separately while its final review completed.
- Projector/API adversarial suite: **52 passed**, including 24 projector tests.
- Full UI backend suite: **918 passed**.
- Full frontend suite: **1,532 passed** across 95 files; final focused copy/state
  checks additionally passed 16 tests.
- TypeScript checking, production frontend build, scoped lint and diff checks
  passed. The final focal/group control refinement passed all four oracle tests.
- Two independent reviewers checked the finite-horizon oracle; exhaustive
  horizons 1–8, three opponent groups, two utility rules and four focal seats
  matched its values and regret telescoping.
- The archive verifier checked every captured file. A separate Claude
  subscription review informed the scientific design; provenance and corrections
  are in [cross-review](research/CROSS_REVIEW.md).

The local-corpus release command is:

```bash
REQUIRE_CANONICAL_CORPUS=1 MOCK_LLM=1 .venv-chroma/bin/python -m pytest -q tests
```

A clean public checkout can run the synthetic/public suite without copying
private evidence into Git. Its five corpus-only checks explicitly skip when the
named local files are absent. Required-corpus mode makes that absence a failure.

## Runtime adoption and rollback

Source delivery and live adoption are separately recorded. The canonical
`run_state/v2_preparation/deployment_receipt.json` identifies the public and
private integration commits, prior dirty-file preservation, adopted services,
health checks, campaign pointer, and rollback. `/benchmarks` displays campaign
activation and research progression; it does not read that separate deployment
receipt.

Campaign activation binds manifest SHA-256
`3357cb3a7b4bcc2c45af991e186c7f19abfa27ec0e18d1d172f9a03e2fd0f4dd`.
Rollback restores the recorded previous pointer/code and reloads affected
services without deleting campaign history or changing human pause files.

## Remaining research work

- Observe eligible fresh campaign iterations through the evidence ladder. Daily
  ideation/research budgets were exhausted on September 14; that is a recorded
  gate, not a reason to raise or reset limits for a successful screenshot.
- Preregister and implement the controlled one-model-seat game study, including
  payoff comprehension, repeat/analysis design, objective framing and resource
  admission. The current campaign registers CPU calibration only.
- Evaluate a purpose-built editing/tool interface on fresh coding tasks. The
  failed historical output contracts do not establish semantic code failure.
- Confirm any future inference-policy benefit on untouched tasks and repeated
  runs. The current small panels do not justify a global policy change.
- Qualify experiment/refinement writers for campaign identity before enabling
  those actions, and extend cross-page campaign navigation when useful.

These are next research/development phases. They do not erase the completed
foundation work or turn its engineering checks into validated scientific claims.
